import assert from 'node:assert/strict';import test from 'node:test';import {createServer} from 'vite';
test('distribution manifest references frozen approved sources and distinguishes missing and superseded members',async()=>{const vite=await createServer({configFile:false,appType:'custom',logLevel:'silent',server:{middlewareMode:true}});try{const m=await vite.ssrLoadModule('/app/lukas/lib/workflow-transmittals.ts');const doc={id:'a',title:'현재 제목',source:{name:'new.pdf',sha256:'b'.repeat(64)},shapes:[],reviewRounds:[{phase:'approved',revision:1,source:{name:'approved.pdf',sha256:'a'.repeat(64),pages:2},objects:[{id:'one'}]}],deliveryHistory:[{sequence:1,revision:1,batchId:'batch',reference:'OUT',issuedOn:'2026-09-11',recipient:'감리',status:'received'}],delivery:{sequence:2,revision:1,batchId:'other',recipient:'다른 수신자',status:'prepared'}};const missing={id:'b',title:'누락 근거',shapes:[],delivery:{sequence:1,revision:9,batchId:'batch',reference:'OUT',issuedOn:'2026-09-11',recipient:'감리',status:'prepared'}};const rows=m.transmittalRows([doc,missing]);assert.equal(typeof m.distributionManifest,'function');const manifest=m.distributionManifest(rows,'batch');assert.equal(manifest.simulated,true);assert.equal(manifest.includesOriginalFiles,false);assert.equal(manifest.drawings.length,2);assert.equal(manifest.drawings[0].source.sha256,'a'.repeat(64));assert.equal(manifest.drawings[0].current,false);assert.equal(manifest.drawings[1].source,null);assert.equal(manifest.drawings[1].available,false);assert.equal(m.distributionManifest(rows,'absent'),null);}finally{await vite.close();}});
