import {chromium,expect} from '@playwright/test';
const browser=await chromium.launch();try{
 const page=await browser.newPage();page.setDefaultTimeout(7000);const errors=[];page.on('pageerror',e=>errors.push(e.message));const base='http://127.0.0.1:4181/workspace-preview/flow';
 await page.goto(`${base}?page=library`,{waitUntil:'networkidle'});await page.getByLabel('템플릿 작업 이름',{exact:true}).fill('표준 원본');await page.getByRole('button',{name:'이 템플릿으로 작업 만들기',exact:true}).click();await expect(page).toHaveURL(/blank=/);const sourceUrl=page.url();await page.goto(`${base}?page=library`);
 const region=page.getByRole('region',{name:'내 도면 재사용 템플릿'});await region.getByLabel('보관할 템플릿 이름',{exact:true}).fill('회의실 기본형');await region.getByRole('button',{name:'선택 페이지를 템플릿으로 보관',exact:true}).click();await expect(region).toContainText('회의실 기본형');await page.reload();await expect(region).toContainText('회의실 기본형');
 await region.getByLabel('재사용 작업 이름',{exact:true}).fill('회의실 A');await region.getByRole('button',{name:'내 템플릿으로 새 작업 만들기',exact:true}).click();await expect(page).toHaveURL(/blank=/);const copyUrl=page.url();expect(copyUrl).not.toBe(sourceUrl);await page.getByRole('button',{name:'회의실 구상 객체',exact:true}).click();await page.getByLabel('구상 객체 이름',{exact:true}).fill('수정한 회의실');
 await page.goto(`${base}?page=library`);await region.getByLabel('재사용 작업 이름',{exact:true}).fill('회의실 B');await region.getByRole('button',{name:'내 템플릿으로 새 작업 만들기',exact:true}).click();await expect(page).toHaveURL(/blank=/);expect(page.url()).not.toBe(copyUrl);await expect(page.getByRole('button',{name:'회의실 구상 객체',exact:true})).toBeVisible();await expect(page.getByRole('button',{name:'수정한 회의실 구상 객체',exact:true})).toHaveCount(0);
 await page.goto(sourceUrl);await expect(page.getByRole('button',{name:'회의실 구상 객체',exact:true})).toBeVisible();await page.goto(`${base}?page=library`);await page.setViewportSize({width:390,height:844});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);await region.screenshot({path:'/tmp/1hk-saved-templates.png'});expect(errors).toEqual([]);console.log('PASS capture template, session reload, independent copies, source isolation and mobile');
}finally{await browser.close();}
