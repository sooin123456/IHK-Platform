# Universal Drawing Workspace production deployment evidence

Updated: 2026-09-05 (Asia/Seoul)

Scope: deploy the current Universal Drawing Workspace implementation without claiming completion of the remaining product roadmap.

## Production state

- Supabase project `naubrijesaqnnbfaehpy`: the required drawing-workspace migrations remain applied. Drawing-share creation now uses migration `20260904122447 drawing_share_server_issued_authority`; its old seven-argument browser-callable signature is absent and the new eight-argument signature is executable only by `service_role`.
- Supabase Edge Function `lukas-qto-upload-verify`: version 5 is `ACTIVE` with JWT verification enabled. It enforces exact PDF content type, streaming `%PDF-` magic validation, and project Editor/Admin authority before finalization.
- Railway collaboration service `lukas-qto-collaboration`: the last recorded check found deployment `2b21138a-0030-4b53-882f-a2bfa4aaca31` running image `sha256:6ce5541a9a6e2d009deafdab9a8a9d989d9ca6ab097fb45d291b5c560715f1ca`, with public `/healthz` returning `live=true` and `ready=true` for instance `lukas-collab-apne2-1`. This Vercel-only increment neither changed nor revalidated Railway.
- Vercel application: deployment `dpl_DXrtemKPhhYktJdethjnmL458ZVd` is `READY` and promoted at `https://lukas-qto-platform.vercel.app`. Its immediate schema-compatible rollback target is `dpl_J3VEdL9G5nnX9bhK5sMfthqvWRTH`. The 2026-09-05 M5 transaction-origin exact-BOQ-tuple correction described below is included in this production artifact.

## Exact authentication return and response-cookie release

- Protected deep links now carry the exact internal pathname and query through `/login`, `/auth/magic-link`, and confirmation. External, protocol-relative, and backslash-based `next` values fail closed instead of becoming redirects.
- Protected loaders and actions authenticate before UUID, query, method, format, or form-body validation. Anonymous Supabase users are rejected before takeoff/preflight data access, and authentication failures preserve every session cookie.
- All matched React Router loaders for one incoming `Request` share one Supabase client, one mutable response-header collector, and one zero-argument `auth.getUser()` promise. Explicit-JWT lookups bypass that memo, rejected lookups remain single-flight only for the current request, and distinct requests remain isolated.
- Authenticated success, redirect, download, and error responses preserve status, body, cache/content headers, and multiple `Set-Cookie` values without duplicating cookies already present. Notification database errors now return bounded cookie-preserving 500 responses.
- Material CSV and verified BOQ resource routes delegate through recognized route-loader exports, reuse one authenticated context, and fail closed on non-attachment results. This also removes server-only helpers from the browser module graph; the local and Vercel production builds both completed successfully.
- The final focused auth/entry/workspace/organization/lineage/public/BOQ union passed 232/232. Typecheck, production web build, collaboration build, Prettier checks, and `git diff --check` passed. Independent final review reported zero Critical, Important, or Minor findings and returned `Ready to deploy: Yes`.
- The final disposable production-shaped browser gate passed 16/16 in 1.7 minutes. It covered real authentication, empty-project start, verified PDF/DXF upload and exact workspace return, IFC linkage, immutable source SHA evidence, editing and relogin restoration, collaboration and offline replay, Viewer denial, approval, BOQ/material lineage, export, performance, and legacy regressions.
- Before promotion, the protected candidate returned `/`, `/robots.txt`, `/sitemap.xml`, and the exact magic-link form successfully through authenticated Vercel access. `/workspace?entry=dashboard` and the canonical split-workspace deep link redirected to login with their exact encoded return addresses; a malicious external return was removed. Candidate-scoped `error` and HTTP 500 log queries were empty.
- After promotion, `https://lukas-qto-platform.vercel.app` resolved to `dpl_5Jff9iWG1NaqPqS4tcwyjgkY8TLv` in `READY` state and repeated the same public, deep-link, open-redirect, and magic-link body gates. Deployment-scoped `error` and HTTP 500 queries remained empty.

## Selected-object business-lineage release

- A selected drawing object now exposes one explicit trace record across the facts already held by the product: immutable object lineage and approved snapshot, confirmed quantity, exact BOQ version and line, allocation, and material-handoff state. The card links directly to the exact BOQ quantity/amount row, the five-cause comparison when one exists, and the exact material handoff detail.
- The UI stops the completed lineage at the material handoff boundary. It does not claim that purchase-order, receipt, installation, waste, or carbon records exist; their existence is explicitly described as something the user must verify on the detail screen. A missing handoff is shown as `자재 인계(대기)`.
- BOQ comparison fragments use collision-resistant UTF-8 hexadecimal row identities. An exact, rendered comparison row receives focus; first-version, review-only, or otherwise unavailable comparisons fall back to the exact BOQ calculation row instead of landing on a missing anchor.
- Fragment styling is SSR/hydration stable: the server and initial client markup do not branch on the browser-only URL fragment. CSS `:target` provides exact linked-row highlighting, imperative focus runs after hydration, and the dark-theme target colors plus `min-h-9` evidence links preserve readable and tablet-sized interaction targets.
- The final quantity/BOQ/material regression union passed 210/210. The disposable production-shaped M1 gate passed Playwright 16/16 plus 5 database prerequisites, with 2 explicitly skipped environment-bound checks and no unexpected browser or page errors. App and collaboration typechecks, app and collaboration builds, Prettier, compiled `:target` CSS inspection, and `git diff --check` passed. Independent final review reported zero Critical, Important, or Minor findings.
- Production-environment candidate `dpl_8B1RWLzyL3CLGKt8TpT9u5vkN82x` returned `/` 200, `/workspace?entry=dashboard` 302 with its exact internal return path, `/auth/magic-link` 200, `/robots.txt` 200, and `/sitemap.xml` 200. A canonical split-workspace deep link preserved its exact login return address, while an external `next` value failed closed to `/workspace`. Candidate-scoped `error`, `fatal`, and HTTP 500 log queries returned zero records.
- After promotion, `https://lukas-qto-platform.vercel.app` resolved to `dpl_8B1RWLzyL3CLGKt8TpT9u5vkN82x` in `READY` state and repeated the same route, deep-link, and open-redirect gates. Deployment-scoped `error`, `fatal`, and HTTP 500 queries remained empty. No database schema, Supabase authority, or Railway collaboration code changed in this increment, so those services were not redeployed.
- This increment was fully replayed against disposable production-shaped authority. A credentialed hosted replay of a real customer object through the complete BOQ/material/site/carbon chain remains unexecuted and is not represented as completed evidence.

## IFC replay, exact source handoff, and raw DXF preflight release

- Exact IFC derivative requeue replay now returns the already committed audit decision before purge-state rejection, while new requests remain blocked after purge preparation. Nullable job, source, and source-SHA identities use null-safe `IS DISTINCT FROM` comparisons, so malformed replay payloads cannot borrow a prior decision.
- The consolidated database scenario proves failed v1 evidence and the immutable original coexist with a converter-fenced v2 claim, ready derivative, and completed job. The hosted function remains `SECURITY DEFINER`, has an empty `search_path`, and is executable only by `postgres` and `service_role`.
- Blank workspaces now preserve their exact canonical return path through verified PDF upload, and DXF source-start handoff carries the selected source file ID. Hosted generated types include the persisted DXF source metadata without widening client insert/update authority.
- Raw DXF preflight validates parser-dropped coordinate groups before import. Generic coordinate families and HATCH pattern coordinates 43–46 are checked for finite/range limits; HATCH code 43 contributes each pattern base point exactly once to the point budget.
- The final adjacent DXF regression union passed 108/108, and the focused IFC requeue/estimator union passed 8/8. Two consecutive disposable production-shaped M1 browser gates passed 16/16; the final post-review run completed in 1.8 minutes and covered authenticated project creation, exact PDF/DXF workspace return, IFC linkage, immutable source SHA lineage, Viewer denial, collaboration/offline replay, approval, BOQ/material lineage, performance, and legacy regressions.
- Independent release review reported no remaining Critical or Important finding after the null-safe replay and HATCH 43–46 corrections.
- Preview deployment `dpl_5YUG7JV2fWRBsV5ypBJynA9wAcQF` was deliberately not promoted. Its source and build were valid, but Vercel Preview has only the three public Revit variables and omits the Supabase/auth configuration; the root loader therefore failed fast with `Missing Supabase environment variables`. A production-environment candidate was created with primary-domain assignment withheld and was promoted only after verification.
- Candidate and post-promotion production checks both returned `/` 200, `/auth/magic-link` 200, `/workspace` 302 to login, exact canonical workspace and PDF-upload return URLs 302 to login, and `/robots.txt` plus `/sitemap.xml` 200. Deployment-scoped `error`, `fatal`, and HTTP 500 queries were empty after both checks.
- The Railway IFC worker code did not change in this increment, so its already verified deployment was not rebuilt or restarted.

## DXF existing-workspace handoff release

- A source-free workspace can now send the user to the verified DXF upload flow and return to the exact same canonical workspace with the newly finalized immutable source preselected. The user confirms the DXF unit before import; the upload does not create a duplicate workspace.
- A draft Editor keeps a visible `새 DXF 업로드` action after the first DXF exists, so later/revision DXF files use the same handoff. Viewer and immutable revision surfaces do not expose this mutation entry.
- The upload action accepts only a canonical same-project workspace return path. Before consuming the verified upload it rechecks project Editor/Admin capability, resolves the target document's latest revision through the exact project/document tuple, and requires `draft`. Missing, stale, locked, non-DXF, cross-project, and external return targets fail without invoking finalization.
- Focused route, action, SSR, and DXF contracts passed 108/108. Typecheck, production build, collaboration build, Prettier, and `git diff --check` passed. The production-shaped browser suite passed 16/16 in 1.7 minutes against a fresh disposable Supabase stack, including real authentication, TUS/Edge verification, same-workspace DXF return/import, immutable SHA lineage, Viewer denial, collaboration, offline replay, approval, BOQ/material lineage, performance, and legacy regressions.
- The repository-wide drawing suite reported 1,368 pass, 11 skip, and the same 2 commit-bound stale P7 performance-evidence failures described below. No new product regression was present.
- Preview deployment `dpl_D93fLiMZj5rSodBaZtt1VwiWUpXH` was deliberately not promoted: its SSR routes returned 500 because the Vercel Preview environment contains only the three public Revit variables and no Supabase/auth authority. A production-environment candidate was then created with domain assignment withheld, and only that candidate was promoted after verification.
- Candidate and post-promotion production checks both returned `/` 200, `/workspace` 302 to login, `/auth/magic-link` 200, `/robots.txt` 200, and `/sitemap.xml` 200. Deployment-scoped `error`, `fatal`, and HTTP 500 scans were empty after each check.
- Collaboration service code did not change in this increment, so the already healthy Railway deployment was not rebuilt or restarted.

## Collaboration recovery release

- Repeated byte-identical recovery contributions now converge idempotently without consuming a per-operation duplicate quota. The global 10,000-operation capacity, raw/unique multiplicity checks, operation-size limits, and mismatched-envelope quarantine remain enforced.
- DXF import timestamps now use one deterministic server/client initial value and update only after hydration, eliminating the production React hydration mismatch.
- A server-verified HTTP acknowledgement can recover a stale protected `pending` projection without rewriting the Yjs server-status map. Durable acknowledgement tombstones are considered across the complete local ledger, not only the bounded recent-outcome window.
- A trusted checkpoint absorbs differing ACK sequence evidence only when both values are `acked` and at or below the already captured checkpoint boundary. The same rule is symmetric for local-first/server-later arrival and for subsequent older-receipt revalidation. `rejected`, `conflicted`, digest/result mismatches, and ACKs ahead of the captured boundary still fail closed.
- The final relevant regression union passed 242/242, including positive recovery and negative boundary/security controls. Root typecheck, collaboration build, Prettier check, and `git diff --check` passed.
- The latest production-shaped M1 browser gate passed 15/15 in 1.7 minutes against an isolated disposable Supabase project after the final ACK correction.
- Before promotion, the protected candidate returned `/` 200, `/workspace` 302, `/auth/magic-link` 200, `/robots.txt` 200, and `/sitemap.xml` 200. Candidate-scoped `error`, `fatal`, and `5xx` log queries were empty.
- After promotion, the production alias resolved to `dpl_E4RQbNK9aYEdPFNXN3HXMqUggaLx` and returned the same `200/302/200/200/200` contract. A fresh tab using an existing authenticated production session opened the real drawing workspace with `저장됨`, `실시간 연결됨`, `공동 편집 연결됨 · 1명`, and the drawing canvas present. It showed no local-save failure, contribution-cap failure, unexpected-error surface, hydration error, or application-origin console entry; only unrelated browser-extension warnings were observed.
- Post-promotion deployment-scoped `error`, `fatal`, and `5xx` queries were empty, and Railway remained live and ready.

## Deployment correction

The first unpromoted Vercel candidate failed its root smoke check with `FUNCTION_INVOCATION_FAILED`. Production logs showed that `dxf-parser` was loaded only through a dynamic `require.resolve`, so Vercel's server dependency tracer omitted it.

A regression test now requires a statically traceable `DxfParser` import. The server keeps the terminable-worker boundary while importing the package statically and checking that the runtime dependency is available. The corrected candidate alone was promoted.

## Verification

- The follow-up increment redirects a newly created project directly to `/projects/:projectId/workspaces/new` instead of traversing the legacy workspace loader. It also discovers prior TUS sessions, adopts the prior object path only after a successful resume `HEAD`, falls back to the newly generated path for a stale session, and rejects foreign project, origin, or parallel-upload resume state before an authenticated request can use it.
- Follow-up entry/upload regression suites: 39/39 passed. An independent review found one bearer-token exposure path through tampered TUS `parallelUploadUrls`; the RED regression and final implementation now reject all non-nullish parallel resume state while preserving tus-js-client v4 single-upload records where that property is omitted.
- Combined focused suites: 279/279 passed.
- Drawing database contract: 27/27 passed.
- DXF, license, and P6 regression union: 59/59 passed.
- Latest production-shaped M1 browser run after this follow-up: 15/15 passed against a newly created disposable Supabase stack, including real magic-link authentication, direct canonical project entry, TUS upload, Edge verification, immutable finalization, blank-workspace source attachment, SHA preservation, Viewer denial, collaboration, offline replay, export lineage, and legacy route regressions.
- The browser gate now proves a real interrupted TUS upload survives a browser-context restart. A deterministic valid `7,344,038`-byte PDF persisted one `tus::` record after the creation request accepted the first `6,291,456` bytes. A new authenticated browser context received the saved state, reselected the identical on-disk file, sent `HEAD` first to the exact saved upload URL, sent no second creation `POST`, and completed with one remainder `PATCH` at offset `6,291,456`. The completed database and Storage path matched the persisted `objectName`, byte size and SHA-256 matched the source, the TUS key was removed, and the PDF was attached and rendered after reload and relogin.
- Resume/upload/security contract suites: 51/51 passed. Mutation sensitivity was also demonstrated: temporarily disabling the same-file resume candidate caused the restart browser assertion to fail because `POST` appeared before the required `HEAD`; the implementation was restored and the full 15/15 M1 gate passed again.
- Upload verifier and finalization authority suites: 14/14 passed. Route/shell/structure/performance focused suites: 156/156 passed.
- Canonical 10,000-object evidence: first usable `1394.24 ms`; zoom p95 `8.90 ms` (`119.94 fps`); pan p95 `8.90 ms` (`119.94 fps`); selection p95 `10.10 ms` (`119.17 fps`); selection workspace-loader request count `0`; overall status `PASS` against `2500 ms`, `16.7 ms`, and `60 fps` targets.
- Root typecheck, production build, Vercel build, collaboration build, and `git diff --check`: passed.
- Before promotion, the protected candidate `https://lukas-qto-platform-c7i5nvf3j-lukas-projects-a540c097.vercel.app` returned `/` 200, `/workspace` 302 to login, `/auth/magic-link` 200, and `robots.txt`/`sitemap.xml` 200 through authenticated Vercel access. Its Vercel build completed successfully and its deployment-scoped runtime scan emitted no `error` or `fatal` logs.
- After promotion, the production alias returned the same `200/302/200/200/200` contract and resolved to deployment `dpl_35DgDAVTE87CD9b6haTkbHWeP9bi` in `READY` state. A post-promotion deployment-scoped error/fatal scan was empty.
- The M4 closeout added proof-only coverage for a source-free authenticated workspace selecting IFC, entering a visible `ready` 3D canvas, reopening the exact deep link in a new authenticated browser context, verifying the return to the real 2D surface, attaching PDF, and preserving both source SHA records with zero request failures. The final post-review disposable canonical gate passed 15/15 in 1.8 minutes, and the updated source-contract suite passed 9/9.
- A current-state recheck on 2026-09-03 resolved the production alias to the same `READY` deployment, returned `/` 200, `/workspace` 302, `/auth/magic-link` 200, `robots.txt` 200, and `sitemap.xml` 200, and found no `error` or `fatal` entries in the deployment's latest one-hour runtime-log window. Railway still reported collaboration deployment `cfb929e3-fef6-4686-baf8-db9ae1ab4f0a` as `SUCCESS` with its instance `RUNNING`; `/healthz` returned `live=true` and `ready=true`.
- Authenticated production smoke on the preceding release opened the workspace dashboard, an existing PDF drawing workspace, and an existing IFC workspace. Save, realtime, collaboration, page/layer/tool/inspector UI, and view switching rendered without a repeatable console or page error. This follow-up was fully replayed against disposable production-shaped authority, but authenticated production entry/upload and the full production M5 chain were not replayed and remain explicitly unexecuted.
- The local full-feature preview remains available at `http://127.0.0.1:61253/workspace-preview/drawing-workspace` for visual comparison.

## Known non-blocking debt

- Supabase migration history contains older remote-only Lua rows and historical timestamp mismatches. Runtime schema and current migration effects are correct; history normalization is deliberately deferred because it is destructive administrative work rather than a release blocker.
- The inspected IFC file does not yet have a verified GLB derivative. Its workspace and 3D empty state render correctly, but source conversion must complete before model geometry appears.
- Supabase advisor warnings/information findings and dependency-audit warnings remain tracked; neither advisor reported an error for this release.
- The earlier one-off hydration warning was traced to DXF import time initialization and corrected in this release; fresh authenticated production loads did not reproduce it.
- Safe, stale, foreign-origin/project, parallel-URL, and unavailable-local-storage TUS resume branches remain covered by deterministic regressions; the safe branch is additionally proven across a real browser-context restart against disposable Supabase Storage.
- The repo-wide drawing Node union reported 1,365 pass, 11 skip, and 2 evidence-only failures: the checked P7 performance capture is bound to source commit `6bf871cc6d5985529fa82f8e673dc65749c33ae6` while the current base commit is `9f5f56d93db325ff935772252f9d4fb64d69f98c`. Current typechecks, production builds, the 39 focused regressions, and the 15 production-shaped browser gates pass; the older standalone P7 performance artifact is not represented as current-commit evidence.

## Source-free collaboration and public fixture-attribution release

- Vercel production-environment candidate `dpl_Ca9rQ4paG2qouw8KLTvL4e6GBR5A` reached `READY` at `https://lukas-qto-platform-h5iyfr5p6-lukas-projects-a540c097.vercel.app` with primary-domain assignment withheld until verification. The prior production deployment `dpl_8B1RWLzyL3CLGKt8TpT9u5vkN82x` is the immediate rollback target.
- Browser Realtime now installs the authenticated access token before channel creation/subscription. A fresh production-shaped disposable run passed 17/17 Chromium scenarios, including a true source-null canonical document, cross-browser comment and explicit mention delivery, history display, and checkpoint restore with the post-checkpoint object retained only as an audit tombstone.
- The redistributed IFC example now has a production-visible `/examples/` index, pinned upstream source and commit, exact SHA-256 records, an MPL-2.0 notice, and an explicit statement that the GLB geometry is first-party synthetic rather than source-faithful. Focused Realtime/license tests passed 18/18 and the 35-package permissive-license policy returned `PASS`.
- A Vercel dry manifest initially exposed that local Playwright traces and generated outputs would be uploaded. `.vercelignore` now excludes environment files, local route/build outputs, Playwright reports, and test results. The verified manifest fell from 5,439 files / 295,495,232 bytes to 919 files / 16,467,629 bytes, with no trace/result payload included.
- Before promotion, the protected candidate returned `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` with HTTP 200. `/workspace?entry=dashboard` returned HTTP 302 to the exact candidate-local encoded login return. Candidate-scoped `error`, `fatal`, and `5xx` log queries returned no records.
- After promotion, `https://lukas-qto-platform.vercel.app` resolved to `dpl_Ca9rQ4paG2qouw8KLTvL4e6GBR5A` in `READY` state and repeated the `200/302/200/200/200` core route contract plus both public attribution pages at HTTP 200. Deployment-scoped `error`, `fatal`, and `5xx` queries remained empty.
- Independent final review reported no actionable Critical or Important finding and approved promotion. This delta did not change hosted Supabase schema/Edge authority or the Railway collaboration service, so neither service was redeployed.
- Credentialed hosted Owner/Editor mention, checkpoint restore, and full upload-to-approval replay remain explicitly unexecuted; the 17/17 evidence is a local production-shaped replay against a fresh disposable authority stack.

## Ruling

The current implementation and upload/security/performance corrections are deployed and operationally smoke-tested. This evidence covers the deployed increment only; it does not mark the complete M2-M5/product roadmap goal as finished.

Before the exact material-to-BOQ row follow-up below, the exact-authentication-return, session-cookie, export-boundary, collaboration-recovery, hydration, selected-object lineage, source-free social/history, public fixture-attribution, and orphan material deep-link corrections were deployed as `dpl_7PpPofaToH8Vd8fCt54ZARmjGEkJ`. The broader M2-M5/product roadmap remains active and is not marked complete by either release.

## Exact material deep-link release

- The material route now rejects `boqLineId` without its BOQ `version` at the HTTP loader boundary and again before `listMaterialBoqLineage` performs any database query. Version-only filtering remains supported for the approved-BOQ handoff collection, and canonical version-plus-line links are unchanged.
- TDD evidence changed from 83 passed / 2 expected failures to 85/85. Controller verification repeated 85/85, `npm run typecheck`, scoped Prettier, `git diff --check`, and the full production build; all exited 0. Independent no-edit review reported Critical 0, Important 0, Minor 0.
- Production-environment candidate `dpl_7PpPofaToH8Vd8fCt54ZARmjGEkJ` reached `READY` at `https://lukas-qto-platform-mjlwufi6h-lukas-projects-a540c097.vercel.app`. Through authenticated Vercel candidate access, `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned 200; `/workspace?entry=dashboard` returned 302 to the exact encoded login return.
- Candidate-scoped error/fatal logs were empty. After promotion, `https://lukas-qto-platform.vercel.app` resolved to the same READY deployment and repeated the six 200 responses plus the exact 302 login contract. Post-promotion error/fatal and `5xx` queries were empty.
- No Supabase migration, Edge Function, Storage authority, or Railway collaboration code changed in this increment, so those services were not redeployed. Credentialed hosted M2/M3/M4/M5 flows, an approved customer DXF fixture, managed backup/restore, and the three-user production workflow remain unexecuted.

## Exact material-to-BOQ row release

- Material lineage now returns to `/projects/:projectId/boq?version=:versionId&line=:lineId` instead of dropping the originating BOQ row. The BOQ screen focuses that exact row before the existing reverse Drawing lineage continues.
- TDD observed the missing-line SSR failure, then 31/31 focused route tests passed. The final route/server/browser-contract union passed 94/94; typecheck, scoped formatting, and `git diff --check` passed. A fresh disposable production-shaped stack passed all 17/17 Chromium scenarios in 1.9 minutes.
- Candidate `dpl_9sZVTYqzjVcZPgSkqwKiLtjwa6UG` reached READY and passed protected candidate checks for six HTTP 200 routes plus the exact workspace-to-login HTTP 302. Candidate error/fatal/500 scans were empty.
- After promotion, `https://lukas-qto-platform.vercel.app` resolved to `dpl_9sZVTYqzjVcZPgSkqwKiLtjwa6UG` and repeated the same checks with empty error/fatal/500 scans. Rollback target: `dpl_7PpPofaToH8Vd8fCt54ZARmjGEkJ`.
- No Supabase or Railway code changed. Hosted authenticated mutation suites remain unexecuted because the current P2/P3/P6/P7 production fixtures do not guarantee residue-free cleanup under retention, immutable audit, organization-owner, and IFC derivative constraints.

## Organization invitation recovery and price-book preflight release (2026-09-04)

- Organization invitations now preserve a successfully committed invitation when email delivery is unavailable and return a copyable manual invitation URL. The fallback URL is derived from the configured canonical application origin in production instead of trusting the request host.
- Company price-book CSV/XLSX imports now provide a full preflight report, an exact error CSV, and an explicit apply step. Invalid mixed files produce zero published rows, and apply rechecks the immutable source identity and parsed contents. Upload and parser limits now cover authoritative Storage size, actual downloaded bytes, CSV row/cell bounds, XLSX entry count, expanded size, worksheet paths, and spreadsheet row/column bounds.
- Focused price-book tests passed 48/48, organization route tests passed 10/10, and the final affected union passed 94 with one expected environment-gated skip and zero failures. App typecheck, app production build, collaboration typecheck/build, and `git diff --check` passed. The final production-shaped disposable Supabase browser run passed 17/17 Chromium scenarios in 1.7 minutes.
- Production-environment candidate `dpl_AECv7DTyfx8yTt1xJwQGc2Wq4xgj` reached `READY` at `https://lukas-qto-platform-dxwkjcfm4-lukas-projects-a540c097.vercel.app`. Before promotion, `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned HTTP 200; `/workspace?entry=dashboard` returned HTTP 302 to the exact encoded login return. Five candidate runtime entries contained zero server errors.
- After exact-artifact promotion, `https://lukas-qto-platform.vercel.app` resolved to `dpl_AECv7DTyfx8yTt1xJwQGc2Wq4xgj` in `READY` state and repeated the same six HTTP 200 responses plus the exact HTTP 302 login contract. The ten post-promotion runtime entries were all informational with zero server errors. Immediate rollback target: `dpl_9sZVTYqzjVcZPgSkqwKiLtjwa6UG`; previously verified fallback: `dpl_7PpPofaToH8Vd8fCt54ZARmjGEkJ`.
- Credentialed hosted invitation delivery, price-book mutation, and the full three-user production workflow remain unexecuted. Their current release evidence is the residue-free disposable production-shaped replay; this increment does not mark the broader universal-workspace roadmap complete.

## Parser, invitation, and upload-resume hardening release (2026-09-04)

- XLSX import now retains its central-directory preflight but also streams selected entries in bounded input chunks and counts actual emitted output before retaining it. A selected ZIP entry whose local and central headers both claim one byte but expands beyond 32 MiB is rejected before workbook parsing.
- Invitation actions now resolve the canonical application origin before calling the invitation RPC. Non-loopback authentication and invitation links require a configured HTTPS `APP_URL`; invalid or missing configuration produces zero invitation RPC calls. Local loopback ports continue to use their active request origin.
- TUS resume now adopts an old object path only when a successful `HEAD` targets the exact persisted candidate URL. A stale candidate followed by a retry on a newly created upload therefore keeps the new path, and malformed persisted metadata falls back to a fresh upload instead of aborting the form.
- The focused XLSX, organization, authentication, and upload-resume union passed 75/75. App and collaboration typechecks/builds, scoped formatting, and `git diff --check` passed. The default Drawing Workspace Node union reported 1,415 pass, 11 skip, and two expected evidence-only failures caused by the retained P7 performance artifact's historical commit binding; no product or widened-command-contract test failed.
- A fresh disposable production-shaped Supabase/Postgres/Storage/Realtime/Edge run passed all 17/17 Chromium scenarios. It includes a deterministic 7,344,038-byte PDF interrupted after the first 6,291,456 bytes, a new browser-context resume against the exact stored TUS URL, immutable Storage/DB SHA equality, source attachment, reload/relogin, role denial, collaboration, offline replay, BOQ/material lineage, and legacy routes.
- Independent read-only review approved the XLSX, invitation-origin, and TUS changes with no remaining P0/P1 finding. The route-to-helper assertion remains a P2 test-depth improvement, and invitation-provider failure diagnostics remain an observability follow-up; neither changes the verified current action path.
- Production-environment candidate `dpl_3hP4QbgcZLHTBSnLYgvYZPYn1cnm` reached `READY` at `https://lukas-qto-platform-boi3sokvz-lukas-projects-a540c097.vercel.app`. Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; `/workspace?entry=dashboard` returned HTTP 302 to the exact candidate-local encoded login return. Candidate error, fatal, and HTTP 500 scans were empty.
- The exact candidate artifact was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_3hP4QbgcZLHTBSnLYgvYZPYn1cnm` in `READY` state and repeated the six HTTP 200 responses plus the exact HTTP 302 login contract. Post-promotion error, fatal, and HTTP 500 scans were empty. Immediate rollback target: `dpl_AECv7DTyfx8yTt1xJwQGc2Wq4xgj`; older verified fallback: `dpl_9sZVTYqzjVcZPgSkqwKiLtjwa6UG`.
- No Supabase schema/Edge artifact or Railway collaboration artifact changed in this correction, so those services were not redeployed. Credentialed hosted invitation delivery, hosted price-book mutation, approved-customer DXF fixture replay, and the full three-user production workflow remain explicitly unexecuted; the broader Universal Drawing Workspace goal remains active.

## Completed-upload recovery and browser-journal release (2026-09-04)

- The source-upload browser journal is now versioned and protected by actor/project-scoped Web Locks plus revision compare-and-swap writes. Completion and verification state cannot regress, another tab cannot clear a newer upload, and browsers without Web Locks fail closed instead of silently permitting conflicting writes.
- TUS creation sends no file bytes, while resumable transfer retains 6 MiB PATCH chunks. The journal survives verifier and finalizer failures, preserves the authoritative upload path and verification ID, accepts an expired verifier refresh only through a newer journal revision, and clears only after the idempotent finalizer succeeds.
- Finalization now reuses the already authenticated request client, user, and refreshed response headers. The resource authenticates before its strict 2 KiB JSON parser, and a successful journal transition is copied into live React state so same-page retry skips both TUS and redundant verification before reload recovery.
- RED-first coverage reproduced the stale live-state defect as three verifier calls with only one finalizer interruption. After the correction, the focused upload/route/entry/browser-contract union passed 166/166. App typecheck and production build, collaboration typecheck/build, scoped Prettier, and `git diff --check` passed.
- A fresh disposable production-shaped Supabase/Postgres/Storage/Realtime/Edge run passed all 17/17 Chromium scenarios in 1.7 minutes. The upload case interrupts a greater-than-6-MiB transfer, resumes in a new browser context, interrupts verification, then interrupts finalization twice; the second same-page retry performs no TUS request and no redundant verifier request, and a later browser-context restart finalizes exactly one immutable source with matching bytes and SHA-256.
- Independent release review reported zero Critical and zero Important findings and approved deployment. Two Minor test-depth improvements remain: exercise monotonic guards at the exact current revision and assert the exact shared actor/project Web Lock name.
- Production-environment candidate `dpl_2ny9AHXfxfVVZWKqm3P5QTe5mfMQ` reached `READY` at `https://lukas-qto-platform-2sylfwq04-lukas-projects-a540c097.vercel.app`. Before promotion, `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned HTTP 200; `/workspace?entry=dashboard` returned HTTP 302 to the exact candidate-local encoded login return. An anonymous malformed-JSON finalization POST was redirected to login before body parsing. Candidate error, fatal, and HTTP 500 scans were empty.
- After exact-artifact promotion, `https://lukas-qto-platform.vercel.app` resolved to the same `READY` deployment and repeated the six HTTP 200 responses, exact HTTP 302 login return, and authentication-before-parse result. Post-promotion error, fatal, and HTTP 500 scans remained empty. Immediate rollback target: `dpl_3hP4QbgcZLHTBSnLYgvYZPYn1cnm`; older verified fallback: `dpl_AECv7DTyfx8yTt1xJwQGc2Wq4xgj`.
- No Supabase schema/Edge artifact or Railway collaboration artifact changed in this increment, so those services were not redeployed. Scheduled deletion of unregistered source-upload orphans remains deliberately deferred until a service-only cleanup worker and cleanup-claim protocol can prevent races with valid finalization. Hosted credentialed upload mutation and the complete three-user production workflow remain explicitly unexecuted; this release does not mark the broader Universal Drawing Workspace roadmap complete.

## Frozen drawing-revision share release (2026-09-04)

- Admins can create and revoke seven-day bearer links for one exact frozen drawing revision. Only the token SHA-256 is retained; the public resolver is service-only, the two evidence tables deny direct anonymous/authenticated access, and create/list/revoke RPCs recheck verified-session Admin authority. Shared pages are read-only, snapshot-backed, and carry `private, no-store`, `no-referrer`, and `noindex, nofollow` response headers.
- Supabase project `naubrijesaqnnbfaehpy` applied migration `20260904064308 drawing_revision_share_authority` from local source SHA-256 `0a5bf6e519469e8ca4127e403f0cf9faf9f31f6bfb2113b34cf504eb9020122e`. Hosted inspection confirmed both tables, all four RPCs, RLS, authenticated create/list/revoke execution, service-only resolver execution, and denied anonymous/authenticated table reads. No raw share token was inserted during deployment verification.
- The final disposable production-shaped gate passed 18/18 Chromium scenarios after the exact navigation-cancellation assertion was constrained to one obsolete React Router `view=3d` data request. It covered real authentication, project and blank/PDF/DXF/IFC entry, immutable source SHA evidence, editing and relogin, role denials, collaboration and offline replay, revision approval, frozen multi-canvas sharing and revoke-to-404, BOQ/material lineage, exports, performance, and legacy routes. App typecheck, production build, collaboration build, real PostgreSQL gates, and `git diff --check` passed.
- Independent read-only application and database reviews reported no P0/P1 finding after the frozen snapshot, anonymous-auth, full role/RPC denial, exact heading, and revoke-race assertions were strengthened.
- Production-environment candidate `dpl_2avTVkKy6PcpEERyF2uVy2JfB1zy` reached `READY` at `https://lukas-qto-platform-menqaemmq-lukas-projects-a540c097.vercel.app`. Deployment protection required Vercel SSO on the candidate URL, so public application smokes were performed immediately after exact-artifact promotion with the preceding deployment `dpl_2ny9AHXfxfVVZWKqm3P5QTe5mfMQ` retained as the rollback target.
- After promotion, `https://lukas-qto-platform.vercel.app` resolved to `dpl_2avTVkKy6PcpEERyF2uVy2JfB1zy` in `READY` state. `/` returned 200, an anonymous canonical workspace deep link reached the magic-link login screen with its exact `next` value, malformed and unknown canonical share tokens returned the bounded Korean 404 surface, and the share responses carried the required cache, referrer, robots, frame, and content-type protections. Headless Chromium found no unexpected console, page, hydration, or 5xx error on the public home, share-404, and authenticated-entry redirect paths. Deployment-scoped error and HTTP 500 log scans were empty.
- Supabase advisor findings for the new objects are the intentional service-boundary shape: RLS-enabled tables have no client policies because their grants are revoked, while the authenticated security-definer RPCs perform verified-session and Admin checks internally. Fresh-index unused and composite-FK coverage notices are informational follow-up work, not correctness failures.
- A credentialed hosted Admin create/open/revoke replay against real customer data was deliberately not manufactured during release. Its full authority and browser flow is proven against the residue-free disposable production-shaped stack; the broader Universal Drawing Workspace roadmap remains active.

## Journal-bound upload resume and M2 browser reliability release (2026-09-04)

- TUS recovery now considers a persisted candidate only when `metadata.objectName` is a primitive string exactly equal to the already validated actor/project browser-journal path. Without that trusted path, with only another path, or with malformed numeric metadata, the client creates a fresh non-upsert object path. Existing origin, project namespace, parallel-session, exact-HEAD, stale fallback, completion journal, and immutable-source safeguards remain in force.
- The local functional-browser authority now supplies a fixed non-secret loopback-only server role, overwrites inherited server-role input, strips any inherited `VITE_SUPABASE_SERVICE_ROLE_KEY`, and uses a dedicated non-client Vite cache. `@radix-ui/react-dropdown-menu` and `gltf-validator` are pre-optimized so a cold first navigation does not rewrite the dependency graph and abort the entry dynamic import.
- Fresh controller verification passed the combined focused Node tests 75/75, standalone M2 Chromium 5/5, application and collaboration typechecks, application and collaboration builds, targeted Prettier, and full `git diff --check`. A fresh disposable Supabase/PostgreSQL/Storage/Realtime/Edge run passed all 18/18 authenticated Chromium scenarios in 2.0 minutes. Independent final review reported Critical 0, Important 0, spec PASS, quality PASS, and release GO.
- Production-environment candidate `dpl_2voVeyaAZRKu92TzYTE3JP99zsUo` reached `READY` at `https://lukas-qto-platform-ltxvs0ak0-lukas-projects-a540c097.vercel.app`. Before promotion, `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned HTTP 200; `/workspace?entry=dashboard` and a canonical split-view drawing deep link returned HTTP 302 with exact candidate-local encoded return paths; a malformed share token returned the bounded HTTP 404. Candidate error, fatal, and HTTP 500 log queries were empty.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to the same `READY` deployment and repeated the six HTTP 200 responses, exact two HTTP 302 return paths, and malformed-share HTTP 404. Headless Chromium rendered the home and magic-link surfaces without unexpected-error text or page exceptions; the deliberately requested missing share resource produced its expected browser 404 console line, with no same-origin request failure or 5xx. Post-promotion deployment-scoped error, fatal, and HTTP 500 queries remained empty.
- Immediate rollback target: `dpl_2avTVkKy6PcpEERyF2uVy2JfB1zy`. No Supabase schema, Edge Function, Storage policy, or Railway collaboration artifact changed in this increment, so those services were not redeployed. Hosted credentialed upload mutation, approved customer fixtures, the complete three-user production workflow, and the broader M2-M5 roadmap remain explicitly unexecuted/incomplete.

## Approved drawing-export receipt hardening release (2026-09-04)

- The canonical disposable approval flow now serially exports PDF, PNG, and SVG and binds each real downloaded byte sequence to the exact actor, project, workspace, approved revision/version, operation checkpoint/SHA, and approved snapshot SHA. Source bytes are re-read before and after the three exports. Korean artifact names use an ASCII fallback plus RFC 5987 UTF-8 disposition instead of raising a Fetch header error.
- Strict request parsing rejects missing, empty, duplicate, whitespace, leading-zero, exponent, extra, and malformed export authority fields before RPC execution. A new drawing receipt also fails closed if no matching approved snapshot exists; exact prior retries still return before current live-revision checks.
- Supabase project `naubrijesaqnnbfaehpy` applied forward migration `20260904102443 drawing_export_approved_snapshot_required` from local source SHA-256 `965b212960dbb86964bd5567fe8d53b1608cde0a221ecc802a9f267669afa346`. Hosted inspection confirmed `SECURITY DEFINER`, empty `search_path`, `postgres` ownership, authenticated execute ACL, approved-snapshot and retry guards, and zero historical drawing receipts with a null approved snapshot.
- Final controller verification passed focused export/retention authority `30/31` with zero failures and one environment-gated skip, application and collaboration typechecks/builds, scoped formatting, full diff checks, and the final post-format disposable production-shaped Chromium suite `18/18` in 1.9 minutes. Independent re-review reported Critical 0, Important 0, Minor 1, Ready YES.
- Production-environment candidate `dpl_AECPU3AieRdmoZhQLgphYBBgPBVf` reached `READY` at `https://lukas-qto-platform-fwj6hrg8k-lukas-projects-a540c097.vercel.app`. Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; the workspace dashboard and canonical split-view deep link returned HTTP 302 with exact candidate-local login returns; malformed sharing returned HTTP 404. Error, fatal, and HTTP 500 scans were empty.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to the same `READY` deployment and repeated those route contracts. Headless Chromium found no page exception, request failure, 5xx, or unexpected-error surface; the deliberately missing share resource produced only its expected 404 console entry. Post-promotion error/fatal/500 scans were empty.
- Immediate web rollback target: `dpl_2voVeyaAZRKu92TzYTE3JP99zsUo`. The forward database change is backward-compatible with the preceding web artifact; rollback therefore does not require destructive schema reversal. No Edge Function, Storage policy, or Railway collaboration artifact changed. Credentialed hosted export mutation, customer fixtures, M3 latency authority, managed restore, and the complete three-user production workflow remain explicitly unexecuted/incomplete.

## Post-deployment M3 verification-only follow-up (2026-09-04)

- The M3 follow-up changes only the canonical browser authority, percentile helper export/validation, and evidence documents; it changes no production application, collaboration service, database schema, Edge Function, or dependency. The already promoted product artifact therefore remains the exact current runtime, and no redundant deployment was created.
- A fresh inspection resolved `https://lukas-qto-platform.vercel.app` to production deployment `dpl_AECPU3AieRdmoZhQLgphYBBgPBVf` in `READY` state. `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned HTTP 200; `/workspace?entry=dashboard` returned HTTP 302 to `/login?next=%2Fworkspace%3Fentry%3Ddashboard`; none rendered the unexpected-error surface.
- Deployment-scoped `error`, `fatal`, and HTTP 500 queries for the latest hour returned no log records.
- The controller's final disposable production-shaped authority passed all 18 Chromium scenarios after app typecheck/build, collaboration build, and real local Supabase/PostgreSQL/Storage/Realtime gates. Its canonical three-context collaboration artifact contains 30 unfiltered warm samples, nearest-rank 29, p95 `104.04058299999451 ms` against `500 ms`, a completed observer flush, zero Owner/Editor/Viewer violations, and Viewer read-only.
- Credentialed hosted multi-user latency replay remains explicitly `UNEXECUTED`; this verification-only follow-up does not mark the broader M2–M5 roadmap complete.

## Server-issued share authority and transaction-origin lineage release (2026-09-04)

- Drawing-share creation no longer accepts a browser-selected bearer hash. The authenticated server action verifies the actor, derives a deterministic domain-separated HMAC bearer for the exact request and frozen revision, and calls an eight-argument service-only RPC. That RPC independently verifies the service role and JWT, the non-anonymous actor, the active project entitlement, and live Admin capability before and after locking. The historical seven-argument authenticated create signature is removed; list, revoke, and public resolution retain their scoped authority.
- Supabase project `naubrijesaqnnbfaehpy` applied only migration `20260904122447 drawing_share_server_issued_authority`, whose source SHA-256 is `11b98f091eaff4e5765decf4d1e7356559742837116d4d6344a1a94c29632363`. Because the repository contains historical local/remote migration-name divergence, deployment used a disposable Supabase work directory populated from the exact remote migration history; its dry run listed only `20260904122447` before the push. Post-apply inspection confirmed the migration row, absent old signature, present new signature, denied `anon`/`authenticated` execute, and allowed `service_role` execute.
- A residue-free production API canary called the new RPC with a service-role JWT and deliberately nonexistent actor/project identifiers. PostgREST reached the function and returned the expected bounded `PDS03 Drawing share authority denied` response; no customer row or share was created.
- Material control now accepts one exact project-scoped `transactionId`, resolves its authoritative material plan before paging, keeps that transaction in the returned dependency closure, and preserves it through lineage pagination. Malformed, duplicate, missing, foreign-project, conflicting, and disappeared transaction identities fail closed. This release did not actually start its browser journey from an unversioned transaction-origin URL or click the BOQ-row self-filter; the resulting orphan-line defect and the missing exact transaction → BOQ row → Drawing browser proof are corrected in the later 2026-09-05 increment.
- Fresh focused verification passed drawing-share tests 39/39 and transaction-lineage tests 93/93. Application typecheck, scoped Prettier, and `git diff --check` passed. The first fresh full browser run recorded one isolated performance miss (`pan` p95 17.5 ms against 16.7 ms, with a 40.8 ms outlier); an unchanged full rerun passed 18/18 in 2.2 minutes. Its canonical 10,000-object artifact recorded first usable 1436.677 ms, zoom p95 10.2 ms, pan p95 10.3 ms, and selection p95 10.2 ms. No threshold or target was widened.
- Production-environment candidate `dpl_65jPHVYEqmkYHs2XWBUmgBMQ23rK` reached `READY` at `https://lukas-qto-platform-b2k9fxmir-lukas-projects-a540c097.vercel.app`. Through Vercel's protected-deployment bypass, `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned HTTP 200; the dashboard and canonical Drawing deep link returned HTTP 302 with exact encoded login returns; malformed sharing returned bounded HTTP 404 with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`. Candidate fatal and HTTP 500 scans were empty; the error scan contained only the deliberately requested malformed-share 404.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_65jPHVYEqmkYHs2XWBUmgBMQ23rK` in `READY` state and repeated all six HTTP 200, both exact HTTP 302, and the protected HTTP 404 contracts without an unexpected-error surface or sensitive server name in the response. Post-promotion fatal and HTTP 500 scans remained empty; error entries were limited to the two intentional candidate/production malformed-share probes.

## 2026-09-04 carbon-factor-origin lineage deployment

- Exact carbon-factor-origin lineage was added without a schema, RPC, Edge Function, Storage, collaboration-service, or dependency change. The selected factor resolves within one project, unions baseline-plan and direct-transaction references, preserves exact 200-row cursor pagination, and navigates factor → approved BOQ row → canonical Drawing object.
- Fresh release authority passed 98/98 focused server/route tests, TypeScript, scoped Prettier, and `git diff --check`. A new disposable full Supabase stack then rebuilt the application and collaboration service, applied the complete schema, and passed all 18 authenticated Chromium scenarios in 2.2 minutes. Independent re-review returned Critical 0, Important 0, Minor 0, Ready YES.
- Production-environment candidate `dpl_3v94Tum1CoicA3oUctZPnEEncWrM` reached `READY` at `https://lukas-qto-platform-dzlxcjijg-lukas-projects-a540c097.vercel.app`. Candidate checks returned six HTTP 200 responses, exact internal-return HTTP 302 responses for dashboard, canonical split Drawing, and carbon-factor material paths, and a bounded malformed-share HTTP 404 with the required cache/referrer/robots/frame/content-type protections. Fatal and HTTP 500 scans were empty; error logs contained only deliberate malformed-share probes.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_3v94Tum1CoicA3oUctZPnEEncWrM` in `READY` state and repeated the route, header, title, and no-unexpected-error checks. Post-promotion fatal and HTTP 500 scans remained empty. Immediate schema-compatible web rollback reference: `dpl_65jPHVYEqmkYHs2XWBUmgBMQ23rK`.
- The preceding web deployment is `dpl_AECPU3AieRdmoZhQLgphYBBgPBVf`. It remains available as a web artifact but is not a complete rollback for this release because it calls the removed seven-argument RPC and therefore cannot create new share links against the forward database contract. Do not restore the insecure RPC; recover share creation by promoting this compatible artifact again or deploying a compatible forward fix. Existing share open/list/revoke behavior remains compatible.
- Credentialed mutation against customer data, dedicated `DRAWING_SHARE_TOKEN_SECRET` rotation, clipboard-click coverage, hosted three-user workflow, and the broader M2–M5 roadmap remain explicitly incomplete. This deployment records the current verified increment only and does not mark the Universal Drawing Workspace goal complete.

## Canonical P7 receipt verification-only follow-up (2026-09-04)

- The hosted release harness now opens the exact query-free canonical workspace route for all three ordered identities and emits only a V2 receipt. The validator rejects legacy/V1 paths and binds the PDF, PNG, and SVG audit rows to one serialized approved revision version, snapshot SHA, and operation checkpoint; case-variant duplicate request UUIDs also fail closed.
- Fresh controller verification passed P7 contracts `29/29`, canonical/legacy route contracts `2/2`, application typecheck, scoped formatting, and `git diff --check`. A fresh disposable production-shaped Supabase/PostgreSQL/Storage/Realtime/Edge run rebuilt the application and passed all `18/18` authenticated Chromium scenarios in 2.2 minutes. Independent final review reported Critical `0`, Important `0`, Minor `0`, Ready `YES`.
- This correction changes only test and release-runner files, so it does not create a different web, schema, Edge, Storage, or collaboration artifact. Per the no-redundant-deployment ruling, production remains `dpl_3v94Tum1CoicA3oUctZPnEEncWrM` in `READY` state rather than manufacturing a no-op deployment.
- A fresh public check returned six HTTP 200 resources, exact HTTP 302 login returns for the dashboard and canonical split-view workspace, and the protected malformed-share HTTP 404. The last-15-minute deployment scan contained zero fatal and zero HTTP 500 entries; its single error was the deliberate malformed-share probe. The home, login, and bounded 404 bodies contained no `Oops!` or `unexpected error` surface.
- The complete credentialed hosted P7 three-user replay remains `UNEXECUTED` because the required real `P7_E2E_*` authorities are absent. No identity or customer project was manufactured, and this follow-up does not mark the broader roadmap complete.

## Explicit current-progress redeployment (2026-09-05)

- At the user's explicit request, the current working-file set was uploaded as a new production-environment candidate after a fresh verification run. The Vercel dry-run file manifest SHA-256 was `347b6493953c6cec055e2686e803c08198bfe94abf0a44180ff530bc8129226e`.
- Fresh local evidence passed the canonical P7 release contracts `29/29`, the collaboration TypeScript build, an application production build under the fixed loopback-only Supabase build authority, `git diff --check`, and the complete disposable Supabase/PostgreSQL/Storage/Realtime/Edge Chromium authority `18/18` in 2.1 minutes. The first environment-free build attempt stopped during prerender because `SUPABASE_URL` was absent; no source assertion or typecheck failed, and the same build passed once the documented isolated build authority was supplied.
- Production-environment candidate `dpl_qEiNDhRF5bEyJb6JKqLmeN2WJYZ4` reached `READY` at `https://lukas-qto-platform-jwtrln9rl-lukas-projects-a540c097.vercel.app`. Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; `/workspace?entry=dashboard` and the canonical split-view Drawing path returned HTTP 302 with exact candidate-local encoded login returns; malformed sharing returned bounded HTTP 404 with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff` protections. Candidate fatal and HTTP 500 scans were empty; error entries were only deliberate malformed-share probes.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_qEiNDhRF5bEyJb6JKqLmeN2WJYZ4` in `READY` state and repeated the six HTTP 200, two exact HTTP 302, protected HTTP 404, and no-unexpected-error body checks. Post-promotion fatal and HTTP 500 scans remained empty; error entries were limited to intentional malformed-share probes.
- The new and immediately preceding deployments share Lambda digest `8c83fc727e5fcaff1a6d0e13508ef70b9466346d9bea3822e7c95445b2f89109`, confirming that the intervening canonical P7 harness correction did not alter the application runtime. Immediate rollback reference: `dpl_3v94Tum1CoicA3oUctZPnEEncWrM`.
- No Supabase migration, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker was changed or redeployed in this explicit web redeployment. Hosted credentialed P7 replay and the broader M2-M5 roadmap remain incomplete; the parent goal remains active.

## Canvas-region issue anchoring release (2026-09-05)

- The authenticated Drawing Workspace now creates issue anchors by dragging a region directly on the canvas instead of typing world-coordinate fields. The draft keeps one stable anchor UUID through submission, normalizes reverse drags, converts screen coordinates through the active viewport, and exposes the selected bounds in a rounded summary while submitting the exact world values. Escape, pointer ownership, selection-boundary changes, permission changes, blur, calibration, and multi-touch transitions clear or isolate the draft as applicable.
- Comment capability now uses the exact `admin`, `editor`, `commenter`, and `reviewer` allowlist in both the rendered workspace and authenticated action boundary. `approver` and `viewer` cannot open the region-anchor form or submit the mutation. The existing public drawing share remains view-only and unchanged.
- Fresh controller verification passed the focused Drawing Workspace suites `112/112`, application typecheck and Vercel production build, collaboration build, scoped Prettier, and `git diff --check`. A fresh disposable production-shaped Supabase/PostgreSQL/Storage/Realtime/Edge run passed all `18/18` Chromium scenarios in 2.2 minutes, including real role denials, Escape cancellation, drag-to-form submission, exact persisted UUID/bindings, and positive normalized bounds. Independent implementation review and browser-gate review both returned no Critical or Important finding and approved deployment.
- The Vercel dry-run uploaded 936 files with manifest SHA-256 `d6346f839bedf6acf5a55f42522d94cdc5e74cb703edbba000debb2bb4a3d033`. Production-environment candidate `dpl_5WZiiCd5QpPELue6N1kar2Nt6bgw` reached `READY` at `https://lukas-qto-platform-add4har4v-lukas-projects-a540c097.vercel.app`; its Lambda digest is `a2ad0367f117584238459b4f9a0cd412ada905e3e543a42cd70fba483e7c6f71`, distinct from the preceding runtime digest.
- Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; `/workspace?entry=dashboard` and the canonical split-view Drawing path returned exact HTTP 302 login return paths. `/share/not-a-valid-token/drawing` returned the bounded HTTP 404 with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`; bodies contained no `Oops!` or unexpected-error surface. Fatal and HTTP 500 scans were empty, and error entries were limited to the deliberate malformed-drawing-share probes.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_5WZiiCd5QpPELue6N1kar2Nt6bgw` in `READY` state and repeated the six HTTP 200, two exact HTTP 302, protected HTTP 404, security-header, and error-shell checks. Post-promotion fatal, HTTP 500, and unexpected-error log counts were zero.
- Immediate rollback target: `dpl_qEiNDhRF5bEyJb6JKqLmeN2WJYZ4`. No Supabase migration, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker changed in this increment, so those services were not redeployed. Browser coverage for blur, identity/deactivation transitions, calibration priority, and out-of-host captured-pointer movement remains a non-blocking follow-up; the broader M2-M5 roadmap remains active.

## Workspace-created issues and persistent region navigation release (2026-09-05)

- The authenticated Drawing Workspace now creates an issue without leaving the canvas, including when the room has no existing issue. A successful local creation is selected exactly once after canonical data arrives; later realtime arrivals do not overwrite a valid manual choice. Admin, Editor, Commenter, and Reviewer share the exact create authority, while Approver and Viewer remain mutation-denied.
- Saved canvas-region anchors now remain visible as viewport-projected, pointer-transparent outlines. Their compact native-button labels reopen the collaboration dock, select the bound issue, support keyboard activation, preserve drawing-object selection, and retain tablet dock exclusivity. Labels yield input to drawing tools, region picking, active PDF calibration, and Space-pan.
- TDD first reproduced the no-op state identity defect (`122/123`) and the Space ownership defect in the real browser (`13` pass, `1` expected failure, `5` not run). Final controller verification passed focused Node tests `123/123`, application TypeScript, collaboration build, scoped formatting/diff checks, and the complete disposable Supabase/PostgreSQL/Storage/Realtime/Edge Chromium authority `19/19` in 2.2 minutes. The browser authority covers a true zero-issue start, six authenticated role boundaries, persistence, exact pan/zoom alignment, inactive input modes, keyboard navigation, object-selection preservation, and compact layout.
- Independent final review initially blocked release on calibration/Space-pan ownership and behavior-test gaps. One bounded final fix wave addressed every Critical/Important/Minor/YAGNI item; scoped re-review returned no new Critical or Important finding and `Ready: YES`.
- A local `vercel build --prod` attempt reached application compilation but stopped at prerender because Vercel intentionally writes `[SENSITIVE]` placeholders for production secrets that cannot be pulled locally. No source or type failure occurred. The production-environment candidate was therefore built remotely with the actual configured production environment; all 2,726 client modules and 222 server modules compiled, prerender completed, and the deployment reached `READY`.
- Candidate `dpl_3WwpPBMBtYoWsk1NWXZsPaPuvosj` was verified at `https://lukas-qto-platform-45ekglwvk-lukas-projects-a540c097.vercel.app`. Its Lambda digest is `bac36c12db3926e61d265abd88cf5f6763d3e658784707cf11de036c52a915f7`. Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; dashboard, canonical workspace, and legacy drawing-workspace paths returned exact candidate-local HTTP 302 login returns.
- Candidate and production malformed-share probes returned bounded HTTP 404 with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`; neither body contained the generic unexpected-error shell. Candidate fatal and HTTP 500 scans were empty, and error entries were limited to the deliberate malformed-share probe.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_3WwpPBMBtYoWsk1NWXZsPaPuvosj` in `READY` state and repeated all six HTTP 200, three exact HTTP 302, protected HTTP 404, security-header, and error-shell checks. Post-promotion fatal and HTTP 500 scans remained empty; its two error entries are the intentional candidate and production malformed-share probes.
- Immediate rollback target: `dpl_5WZiiCd5QpPELue6N1kar2Nt6bgw`. No dependency, Supabase migration/RPC/RLS change, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker changed in this increment, so only the web application was deployed. Hosted customer-data mutation and the broader M2-M5 roadmap remain outside this release and the parent goal remains active.
- A post-promotion production dry run over the unchanged web deployment inputs enumerated exactly `936` canonical manifest entries (`167,915` JSON bytes) with SHA-256 `2a57979ac2e13d71ec9614d3eb7e2b250a768c5258d64a7ade6ee5639915e25f`. This records the working-file snapshot identity independently of the intentionally unchanged Git HEAD; only evidence/SDD documents outside the `platform` deployment root changed after promotion.

## Revision-anchor relink and current-progress deployment (2026-09-05)

- PDF and IFC revision-review candidates now use a bounded 50-row keyset page with exact out-of-page candidate authority. An in-progress candidate remains visible while paging, the previous anchor stays as inactive lineage, and retry after a lost response replays the exact committed replacement instead of issuing a second mutation.
- The canonical workspace supports pointer-drawn PDF replacement regions and a keyboard-only percentage-coordinate path. IFC replacement uses the selected GlobalId/camera explicitly. Canonical and legacy screens share one exact relink role policy; Viewer, Approver, and unknown roles cannot see or invoke relink controls.
- Elevated Supabase authority is now loaded only inside the server-side share action/public-share loader that needs it. Ordinary route-module startup no longer requires the service-role key, and the built client assets contain neither the service-role variable/value nor the admin-client module. The typed relink error also avoids Node 26's unsupported constructor parameter-property syntax.
- Fresh controller verification passed the affected server/UI union `135/135`, the real database contract/runtime union `183/183`, application typecheck and production build (`2,726` client modules, `222` SSR modules), collaboration typecheck/build, scoped Prettier, and `git diff --check`. The final P5 browser gate passed `15/15`; its IFC focus scenario also passed `10/10` isolated repeats. The complete disposable Supabase/PostgreSQL/Storage/Realtime/Edge M1 gate passed `19/19` in 2.2 minutes after the production changes.
- The broader drawing union reported `1,486` pass, `11` skip, and the same two evidence-only P7 failures: that older performance receipt remains intentionally bound to source commit `6bf871cc6d5985529fa82f8e673dc65749c33ae6` rather than current base `9f5f56d93db325ff935772252f9d4fb64d69f98c`. Fresh typecheck, builds, focused database/UI coverage, and both browser gates contain no product failure.
- Independent release and UX re-reviews reported Critical `0`, Important `0`, and `Ready: YES`. The UX review retained two minor test-depth follow-ups: explicit browser paging during an active legacy relink and IFC relink across a remount; current helper/action coverage and the real legacy atomic browser flow cover the underlying authority and lineage.
- The final Vercel dry report enumerated `940` files (`17,017,575` source bytes). Its exact pretty-JSON report was `167,551` bytes with SHA-256 `95202e0b80d5bff975cf317501c58d9c76ff5e40d461b6edbb2f0a14e28a1eb0`.
- Production-environment candidate `dpl_3rgTo4NT1rKLyA8tCMEXjs9KpCwu` reached `READY` at `https://lukas-qto-platform-flcziefn9-lukas-projects-a540c097.vercel.app`; its Lambda digest is `dad5e520361c9464ce8f12aa9cf0604b40eceae54e6d3fe65239da7f6862fd31`. Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; dashboard, canonical split workspace, and legacy workspace routes returned exact candidate-local HTTP 302 login returns.
- Candidate and production malformed-share probes returned bounded HTTP 404 with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`; no checked body contained the generic unexpected-error shell. Fatal and HTTP 500 scans were empty, and error entries were limited to the deliberate malformed-share probes.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_3rgTo4NT1rKLyA8tCMEXjs9KpCwu` in `READY` state and repeated all six HTTP 200, three exact HTTP 302, and protected HTTP 404 contracts. Immediate web rollback target: `dpl_3WwpPBMBtYoWsk1NWXZsPaPuvosj`.
- No Supabase migration/RPC/RLS artifact, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker changed in this increment, so only the Vercel web application was deployed. Hosted customer-data mutation and the broader M2-M5 roadmap remain outside this release; the parent goal remains active.

## Truthful material-progress lineage deployment (2026-09-05)

- The selected Drawing object now distinguishes the downstream records that actually exist for its exact BOQ line: material handoff, purchase order, goods receipt, site activity (`installation`, `return_to_supplier`, or `waste_disposal`), and carbon evidence. Carbon evidence is present only when the linked material plan has a baseline factor or a linked material transaction has a carbon factor. The compact top lineage strip was narrowed from the over-broad `자재·현장` claim to `자재 인계`.
- The server projection remains bound to the authenticated project, exact BOQ version and line, and exact material-plan IDs. Filter groups are limited to 100 IDs, the transaction union has a shared 10,000-row ceiling, malformed or cross-scope results fail closed, and the existing `hasMaterialLineage` value remains exactly equal to the handoff status. No schema, RPC, dependency, or client state library was added.
- Focused server, SSR, shell, and browser-contract coverage passed `165/165`. It includes 101-plan chunking, capped PostgREST pagination, `return_to_supplier`, query/shape/project fail-closed cases, and rejection of the 10,001st transaction. Application and collaboration TypeScript checks, production builds, scoped Prettier, and `git diff --check` passed.
- The final disposable Supabase/PostgreSQL/Storage/Realtime/Edge authority rebuilt the application and passed all `19/19` authenticated Chromium scenarios in 2.2 minutes. The M5 flow proves both Owner and Viewer see only `자재 인계` immediately after handoff, then see all five exact stages after purchase, receipt, site, and carbon records are committed and the workspace is loaded again. Independent re-review reported no Critical or Important finding and approved deployment.
- The broader Drawing unit union recorded `1,487` pass and `11` environment-bound skips. Its only three failures were unrelated release-evidence conditions: one performance threshold ran concurrently with the production build and passed alone in `96.4 ms`, while two checks correctly rejected the already documented P7 artifact bound to source commit `6bf871cc6d5985529fa82f8e673dc65749c33ae6` instead of the current base `9f5f56d93db325ff935772252f9d4fb64d69f98c`. The old receipt was not rewritten or represented as evidence for this deployment.
- Production-environment candidate `dpl_8UMz5iXFDNcio51yDLr69GbGk7ry` reached `READY` at `https://lukas-qto-platform-b2om66hsm-lukas-projects-a540c097.vercel.app`; its Lambda digest is `dd2f014c2aec884e40bf3d08614997d750c0b5e908f6bc67e8e4ea557cedb841`. Candidate checks returned HTTP 200 for `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md`; dashboard, canonical split workspace, and legacy workspace routes returned exact candidate-local HTTP 302 login returns.
- Candidate and production malformed-share probes returned the bounded HTTP 404 contract with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`; checked bodies contained neither `Oops!` nor the generic unexpected-error surface. Candidate and post-promotion fatal and HTTP 500 scans were empty. Error entries were limited to the deliberate malformed-share probes.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to `dpl_8UMz5iXFDNcio51yDLr69GbGk7ry` in `READY` state and repeated the six HTTP 200, three exact HTTP 302, and protected HTTP 404 contracts. Immediate web rollback target: `dpl_3rgTo4NT1rKLyA8tCMEXjs9KpCwu`.
- No Supabase migration/RPC/RLS artifact, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker changed in this increment, so only the Vercel web application was deployed. Hosted customer-data mutation, the customer-redistributable DXF fixture, the credentialed P7 three-user replay, managed restore evidence, and the remaining M5/productization roadmap remain incomplete; the parent goal remains active.

## OSS DXF interoperability and P7 evidence hardening release (2026-09-05)

- The bounded server DXF adapter now accepts the exact AutoCAD unused `$EXTMIN=(1e20,1e20,1e20)` plus `$EXTMAX=(-1e20,-1e20,-1e20)` pair without treating it as drawing geometry. Every partial, altered, case-folded, approximate, or unrelated value and all entity coordinates remain under the original coordinate-bomb defenses; no parser limit or public interface widened.
- The repository contains the exact 102001-byte MIT-licensed `gdsestimating/dxf-parser` `extendeddata.dxf` fixture from commit `0df7a37a4207a1f925b8d0bfffc270ff121446b4`, SHA-256 `9b39289e3435fb187eb0e671fb8a07b8728cdf69a0275836f67ca005732f781d`, with complete licence and pinned provenance validation. It is explicitly OSS interoperability evidence, not customer data or customer acceptance.
- The fixture imports eight deterministic lines, eight normalized-style reports, one bounded unsupported `VIEWPORT` skip, zero blockers, and the existing two-operation graph. The DXF plus licence suites passed 57/57; typecheck, production build, scoped formatting, and `git diff --check` passed. A fresh disposable authenticated M1–M4 run passed 19/19 Chromium scenarios in 2.2 minutes. Both independent reviews reported 0 Critical, 0 Important, and 0 Minor findings.
- P7 restore evidence now requires ordered drill timestamps and distinguishes provider-correlated comparison from unavailable direct selected-backup-to-target binding. Forged PASS states and exit-code contradictions fail closed, and honest legacy `UNEXECUTED` evidence remains compatible. Direct provider binding, the managed restore drill, approved-customer DXF acceptance, and credentialed hosted multi-user replay remain `UNEXECUTED`.
- Production-environment candidate `dpl_J7Ye8J27uZg5AigNeERs8q7nkMJx` reached `READY` at `https://lukas-qto-platform-h68d2up1b-lukas-projects-a540c097.vercel.app`; its Lambda digest is `a92bca0a45585d5d87966553402aea95823ac4cf03ae5684c136c48e83da28c3`. Before promotion, six public routes returned HTTP 200 and dashboard, canonical-new-workspace, and legacy-workspace routes returned exact candidate-local HTTP 302 login returns. Checked public bodies contained no generic unexpected-error surface, and candidate-scoped error, fatal, and HTTP 500 queries were empty.
- The exact candidate was promoted. `https://lukas-qto-platform.vercel.app` resolves to the same deployment in `READY` production state and repeated the six HTTP 200, three exact HTTP 302, body, and zero-log checks. Immediate rollback target: `dpl_8UMz5iXFDNcio51yDLr69GbGk7ry`.
- No Supabase schema, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker changed in this release slice, so only the Vercel web application was deployed. The broader Universal Drawing Workspace roadmap remains active.

## Earlier current-tree P7 performance evidence refresh (2026-09-05)

- After promotion, the complete Drawing Workspace Node union initially reported `1,498` pass, `11` environment-bound skip, and `2` fail out of `1,511`. Both failures were evidence-authority checks: the retained P7 performance receipt was intentionally bound to source commit `6bf871cc6d5985529fa82f8e673dc65749c33ae6`, while the current relevant source is based on `9f5f56d93db325ff935772252f9d4fb64d69f98c`. No production behavior assertion failed, and neither validator nor threshold was relaxed.
- The real production-build Chromium runner was executed to replace that stale receipt with then-current raw evidence. It passed `3/3` Playwright scenarios and emitted schema V4 evidence with run ID `b7a31f6b-cd19-4ada-bebd-f3511fa356a4`, source-tree SHA-256 `ce9df5518bd043b13c73bf192e51696e189ddc4d13c60400bd5f4acc5fd1dc8a`, raw capture SHA-256 `e78f9dc509be24a8f0885e545839beebddaec422adffaee80b2cf594c236589c`, and exact `10,000`-object authority. That run wrote `.superpowers/sdd/2026-08-28-drawing-workspace-p7/task-4-performance-evidence.json` and `.superpowers/sdd/2026-08-28-drawing-workspace-p7/task-4-performance-playwright-capture.json`; the same paths were subsequently superseded by the latest current-tree receipt recorded below.
- The cold cache-miss first-usable boundary was `2355.0999999046326 ms` and the warm first-usable boundary was `1631.2999999523163 ms`, both below the `2500 ms` target. Warm p95 was `0.20000004768371582 ms` for zoom, `0.20000004768371582 ms` for pan, and `9 ms` for selection, each below the `16.7 ms` frame target. The workload contained all `10,000` authoritative wall/opening/space/area/grid/arc objects plus PDF and IFC source visibility.
- A focused receipt inspection then passed `13/13`. A fresh rerun of `npm run test:drawing-workspace` passed the full `1,511`-test union with `1,500` pass, `0` fail, and `11` explicitly environment-bound skips.
- This was post-deployment local evidence for the source state at that point, not a new production deployment or a hosted-runtime performance claim. It recorded local gate `MET` and production-runtime gate `UNEXECUTED`; no web, database, Edge, Storage, collaboration, or derivative artifact changed in that refresh.

## Disposable P3 authority and current-progress deployment (2026-09-05)

- An explicit runner-owned P3 profile now composes isolated Supabase/PostgreSQL/Storage/Auth, the production React Router build, and the collaboration service without weakening the separate hosted-production credential guard. Default M1 execution removes inherited P3-only variables; direct base-config invocation cannot claim disposable authority. The deterministic Revit fixture ZIP contains only the repository's exact `.addin` manifest and does not claim an absent DLL.
- The final disposable P3 run passed Gates 01–08 (`8/8`). It proved three-user awareness and selection, concurrent edits, version conflict handling, 100-operation offline replay with zero loss, Viewer/Reviewer/Approver/non-member write denial, object/region comments and mentions, Reviewer-to-Approver separation, approved-parent freeze with a distinct same-document child revision, audited SVG/PDF export, Revit release routes, and immutable PDF/IFC source hashes. The outer runner completed isolated service, database, Storage, Auth, and temporary-root disposal.
- Fresh release verification passed: P7 real production-build Chromium performance `3/3`; Drawing Workspace Node union `1,524` pass, `0` fail, `11` environment-bound skip; application and collaboration typechecks; application and collaboration builds; `git diff --check`; and the complete production-shaped M1 Chromium gate `19/19`. Independent final review reported Critical `0`, Important `0`, Minor `0`.
- The release-time exact 10,000-object performance receipt was schema V4, run ID `71825fe5-d727-416c-83d7-226df8dd5d69`, source-tree SHA-256 `014bfd9a9e9086aa1ca0d18aff8c4d94fc23565334da24ac3eba9e06d9acdc7f`, and local gate `MET`. Warm first usable was `1623.5 ms`; warm p95 was `0.10 ms` zoom, `0.20 ms` pan, and `9.80 ms` selection. Its separately derived cold cache-miss sample was `5404.3 ms` and truthfully recorded `NOT MET` while the machine reported only `209715200` free bytes.
- A same-source diagnostic rerun retained the exact threshold and passed all `3/3` Playwright scenarios. Receipt `c95a2c97-5813-4227-b89a-3b14a3e1de17` recorded cold cache-miss first usable `2167.5 ms`, warm first usable `1639.7999999523163 ms`, warm p95 `0.20000004768371582 ms` zoom, `0.20000004768371582 ms` pan, and `9.600000143051147 ms` selection; the machine reported `2462777344` free bytes. A later current-source run described below passed with substantially less reported free memory, so memory pressure alone is not established as the cause of the isolated `5404.3 ms` observation. The miss remains documented as unresolved variability and no threshold was weakened. The production-runtime performance gate remains `UNEXECUTED`.
- Production-environment candidate `dpl_J3VEdL9G5nnX9bhK5sMfthqvWRTH` reached `READY` at `https://lukas-qto-platform-9y5tuyy0p-lukas-projects-a540c097.vercel.app`; Lambda digest `4209d9216809918f3f19e34ae103291d43bf162ecca99d29a93d052e815b0042`. Six public routes returned HTTP 200. Dashboard, new-workspace, and legacy drawing-workspace routes returned exact candidate-local HTTP 302 login return paths. The malformed drawing share returned the bounded HTTP 404 contract with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`; checked bodies contained neither `Oops!` nor the generic unexpected-error surface. Fatal and HTTP 500 scans were empty; error entries were only the deliberate malformed-share probe.
- The exact candidate was promoted without rebuilding. `https://lukas-qto-platform.vercel.app` resolves to `dpl_J3VEdL9G5nnX9bhK5sMfthqvWRTH` in `READY` state and repeated all six HTTP 200, three exact HTTP 302, bounded HTTP 404, body, and log checks. Immediate web rollback target: `dpl_J7Ye8J27uZg5AigNeERs8q7nkMJx`.
- This release deploys the verified current web tree. It does not convert local disposable evidence into hosted authority: externally hosted P3 TLS/multi-replica proof, the managed backup/restore drill, customer-controlled DXF/IFC acceptance, and the broader product roadmap remain `UNEXECUTED` or active as applicable. The parent goal is not marked complete.

## M5 transaction-origin exact-tuple correction deployment (2026-09-05)

- A transaction-, carbon-factor-, or material-plan-origin material page can exist without a collection-level `version`. Its BOQ-row self-link previously emitted only `boqLineId`, so the loader correctly rejected the orphan tuple with HTTP 400. The link now takes the authoritative `boqVersionId + boqLineId` from the row itself; the loader/server orphan-line guards remain unchanged.
- TDD and release verification passed: focused M5 `224/224`; full Drawing Workspace Node union `1,525` pass, `0` fail, `11` explicitly environment-bound skips; disposable authenticated Chromium `19/19` in 2.3 minutes; application typecheck and production build; collaboration typecheck and build; scoped formatting and `git diff --check`. Independent review reported Critical `0`, Important `0`, Minor `0`, Ready to deploy `Yes`.
- The current production-build P7 runner passed `3/3` with run ID `114b8dac-2d03-43d4-a2e1-027074b352cf` and source-tree SHA-256 `e9ccfb7be47e248027c43197ab29c4a58c0f79664dc088d3801569b64f4dc65b`. Cold first usable was `2228.9000000953674 ms`, warm first usable was `1615.3999998569489 ms`, and warm p95 was `0.10000014305114746 ms` zoom, `0.20000004768371582 ms` pan, and `9.700000047683716 ms` selection. Local thresholds are `MET`; production-runtime performance remains `UNEXECUTED`.
- Production-environment candidate `dpl_DXrtemKPhhYktJdethjnmL458ZVd` reached `READY` at `https://lukas-qto-platform-pxqxtzadh-lukas-projects-a540c097.vercel.app`; its React Router Node 24 Lambda digest is `6d3b225d17391ffd32620e9ad699c3620a1e1b2bc29bd85a39c4618099c1ee84`. Before promotion, `/`, `/auth/magic-link`, `/robots.txt`, `/sitemap.xml`, `/examples/`, and `/examples/IFC_FIXTURE_NOTICE.md` returned HTTP 200. Dashboard, new-workspace, canonical split-workspace, and unversioned exact-transaction material paths returned HTTP 302 to exact candidate-local encoded login returns.
- Candidate and production malformed-share probes returned the bounded HTTP 404 contract with `private, no-store`, `no-referrer`, `noindex, nofollow`, `DENY`, and `nosniff`. All checked bodies contained neither `Oops!` nor the generic unexpected-error surface. Candidate and post-promotion fatal and HTTP 500 scans were empty; all six error entries were the intentional malformed-share probes used for status, body, and header checks.
- The exact candidate was promoted without rebuilding. `https://lukas-qto-platform.vercel.app` resolves to `dpl_DXrtemKPhhYktJdethjnmL458ZVd` in `READY` production state and repeated all six HTTP 200, four exact HTTP 302, bounded HTTP 404, security-header, body, and log contracts. Immediate schema-compatible web rollback target: `dpl_J3VEdL9G5nnX9bhK5sMfthqvWRTH`.
- This increment changed only the Vercel web application. It made no Supabase schema/RLS, Edge Function, Storage policy, Railway collaboration service, or IFC derivative worker change. Credentialed hosted M5/P7 flows, customer-approved fixture acceptance, authenticated Site-role field validation, managed backup/restore, and hosted production-runtime performance remain `UNEXECUTED`; the broader roadmap remains active.

## Initial-render correction and diagnostic release (2026-09-05)

- The intervening production deployment `dpl_3e5EpeHhFWBj9M7VvZvBMuiyg99q` included the dashboard `Asia/Seoul` hydration correction and DXF attestation recovery. Hosted migrations `20260905014005` (foundation) and `20260905014006` (enforcement) were applied in order, with migration records, issuer/table privileges, and enabled enforcement trigger inspected. Full hosted DXF positive/negative behavior canaries remain unexecuted; migration inspection alone is not that proof.
- This increment makes the estimate-binding form's initial markup deterministic. Its request UUID is generated synchronously at submission and retained on the mounted form for retries. The existing server authorization, revision fencing, and binding contract remain intact. Browser evidence now attaches before initial authentication and relogin navigation so early errors cannot escape those evidence collectors.
- RED/GREEN and affected Node checks passed `185/185`; app/collaboration typechecks and production builds passed. The disposable production-shaped Auth/Postgres/Storage/Edge/collaboration browser gate passed `19/19`, then passed `19/19` again (2.5 minutes) after the final relogin listener correction. The final listener correction changed tests only; the tested app source equals the candidate app source. Scoped review found no remaining blocking issue.
- Candidate `dpl_AEBqA5WDBmNwtzDEEYEE6ExydE6k` reached `READY` at `https://lukas-qto-platform-pm3tpe2gk-lukas-projects-a540c097.vercel.app`. Candidate `/` returned 200; dashboard, reported PDF upload, and canonical workspace paths returned exact 302 login returns. The candidate error-log scan was empty. The same artifact was promoted and the main production URL was verified to resolve to this exact deployment in `Ready` state.
- After promotion, the authenticated dashboard, previously reported PDF upload screen, and existing Drawing Workspace rendered correctly; the workspace reached saved/connected state. Timestamp-scoped browser error logs were empty and the deployment's 20-minute HTTP 500 scan was empty. No production file upload or user-data mutation was performed. Schema-compatible immediate web rollback target: `dpl_3e5EpeHhFWBj9M7VvZvBMuiyg99q`.
- Current diagnosis and prioritized remaining work are recorded in `2026-09-05-current-diagnosis-and-next-steps.md`. This increment adds no dependency or database/service change and does not mark the broader goal complete.
