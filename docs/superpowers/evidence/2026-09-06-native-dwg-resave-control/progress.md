# SDD ledger — plan: docs/superpowers/plans/2026-09-06-native-dwg-resave-control.md

Spec: `docs/superpowers/specs/2026-09-06-native-dwg-resave-control-design.md`. The full Universal Workspace goal remains active.

## Workspace and workflow

Authoritative linked worktree `/Users/h/Documents/GoAgent/.worktrees/universal-workspace-m1`, branch `codex/universal-workspace-m1`, HEAD `9f5f56d93db325ff935772252f9d4fb64d69f98c`. Existing source is dirty; use exact pre-task copies for reviews, not HEAD-to-HEAD diffs. Existing libraries are installed; no installation required. Original preview4173/PID80284 is preserved. Prior goal turn changed source and produced final172Node+27browser+6repeat evidence: classified progress, not a wait or no-progress turn.

Ruling: keep this plan's recovery ledger, briefs, reports and review evidence in this public evidence directory, with exact baselines in an owned temporary directory, rather than invoking scripts that access private `.superpowers/sdd` artifacts — preserves the established private-artifact boundary and no-commit constraint — cost if wrong: additional explicit artifact packaging instead of the skill's helper scripts. Do not delete this evidence on completion because no commit history is being created.

Ruling: execute the approved goal without new approval/execution-choice prompts — follows the user's explicit instruction to stop asking and implement — cost if a reversible design choice is wrong: visible local rework; no external mutation authority is inferred.

## Preflight compatibility scan

| Tasks | Producer/consumer or internal agreement | Result |
| --- | --- | --- |
|1→2 | Exact attestation fields and nullable no-op consumed by service admission | Agrees; actor/request ID remain separate queue identity; no early no-op return |
|1→3 | Claim attestation rebuilt from fresh payload using the same production builder | Agrees; parser alone is not geometry authority |
|2→3 | Exact claim token/attempt, control actions and failure/cancel acknowledgements | Agrees; no completed state exists until artifact publication |
|1 | Literal LINE geometry versus existing cm fixture; schema export and shared fixture file | Corrected plan example to native2A,(2,2)→(3,4), with exact changed snapshot rehash |
|2 | Seven RPCs, strict actor/session/service boundaries, viewer request/status and requester/admin cancel | Agrees with explicit new resave policy; source-free and draft-import policies unchanged |
|3 | Independent control/cleanup versus execution cancellation; internal prepared value versus public output | Agrees; CLI/UI scheduling remains disconnected until publication |

## Task status

- Task1: complete — `/root/resave_attestation_review` gpt-6-astra/high approved spec and quality; only minor normalization-test finding fixed with an alphabetic UUID. `/root/resave_attestation_fix_review` gpt-5.6-sol/high confirmed addressed/no new breakage. Controller read125/125 focused GREEN, empty no-emit TypeScript log, and fresh72/72 covering the test-only fix. Exact baseline files remain in `/tmp/1hk-resave-control-m6Q9WS`; report and exact-delta package are public. Production attestation interface is frozen for Task2/3.
- Task2: complete — `/root/resave_control_review`, gpt-6-astra/high, independently accepted spec and quality with no Critical/Important findings. Implementer `/root/resave_control_jobs` produced CLI migration20260906110304, server adapters, actual DB fixture and narrow wrapper integration. Controller-requested two-valid-project race and present-but-falsy RPC error refusal are covered; final129/129, realPG1/1 clean-chain/retention and no-emit TypeScript0 logs/hash identities checked. Review package/report are public in this directory; production interface frozen for Task3.
- Task3: complete — implementer `/root/resave_execution_bridge`, gpt-6-astra/high, froze five code/test files with275 focused passes, actual native1/1 and two typechecks0. Independent `/root/resave_bridge_final_review`, gpt-6-astra/high, approved spec and quality without actionable findings. Exact hashes and pre-task sandbox baselines verified. See task-3-report.md and task-3-review.md; no publication/UI or full R4/R5 completion claim.

Task3 implementer-owned evidence directory: `/tmp/1hk-resave-control-m6Q9WS/task3-IgWX4x`. Earlier37/37 cleanup and46/46 initial-worker runs are historical milestones. Final frozen Task3 report/package, accepted task gate, whole-unit review and final controller logs supersede these interim statuses. Do not wait for another freeze or redispatch Task3.

All three tasks and whole-unit integration gate are complete. Final review by `/root/resave_control_unit_review`, gpt-6-astra/high, accepted backend architecture/integration with no Critical/Important findings. Its one test-only Minor was fixed in one wave by `/root/resave_control_review_fix`, gpt-5.6-sol/high; scoped `/root/resave_control_fix_review`, gpt-5.6-sol/high, confirmed ADDRESSED/no new breakage. Implementer and controller covering82/82 passed (overlapping prior tests), exact final test hash/copied-runtime identity recorded. No parked/open findings or task commits. Source-free/native production and SQL remained frozen during this correction. The overall goal stays active; continue artifact publication/download/Auth/browser, using next-publication-seams.md as preparation rather than repeating this plan.

Ruling: Task3 actual execution-bridge probe will be a retained explicit-opt-in test using the public synthetic source and cached immutable native image, rather than an old private build DLL — preserves the private-artifact boundary and tests the new bridge instead of merely rerunning the accepted sandbox tests — cost if wrong: one focused integration test to maintain. Task3 file list updated before its dispatch; no implementation overlap with Task2.

Actual-process prerequisites checked read-only: Docker executable `/opt/homebrew/bin/docker`; socket `unix:///Users/h/.colima/default/docker.sock`; cached image `sha256:c921c67ddb37d2a57969d04c7a4983f6d847e0b89901403e3b015ac25bf566ae` carries both expected reader/resaver protocol labels. Public synthetic source `docs/superpowers/evidence/2026-09-06-native-dwg-resave-protocol/controller/actual/source/synthetic-input.dwg` SHA256 `bcc54d3c768444c9ade41a23e4ef2b9f5b5668d9972522ee4a4adbc98c670434`. This check is prerequisite evidence, not new execution/cancellation proof.

Controller actual Task1 integration session36869 exited0: cached reader → production attestation → cached resaver → reader; literal LINE4A(10,11)→(120,21), other projected entities/layers unchanged, original10987byte source SHA unchanged. Output11019bytes, SHAa0d662353ddbb0496acd1d9c1f30c0ce7493701845da762ab9cd20c69de8473a. Script `/tmp/1hk-resave-control-m6Q9WS/attestation-native-probe.mjs`, log task1-actual-attestation-native.log. This is real native execution with a synthetic approval payload, not DB/Auth approval, cancellation or delivery qualification.

Ruling: permit narrowly typed cleanup confirmation on resaver rejection in Task3, preserving generic error text/reader behavior and all lifecycle operations — read-only integration audit and controller source inspection found current abort and failed cleanup both collapse to the same generic error, so rejected await cannot justify a cancel acknowledgement — cost if wrong: a small internal error-contract extension and focused regressions. Unknown/unconfirmed cleanup returns uncertainty and no settlement; it never fabricates cancellation success. Design and Task3 plan amended before implementation. Read-only audit by `/root/freeze_identity_task_review` did not change code or execute tests.

Ruling: bind service failure/cancel acknowledgement responses to exact `{jobId,attemptNumber,leaseToken,status}` — their body was implicit in the initial spec, and Task3 must distinguish a different attempt's settlement response — cost if wrong: four fields rather than two on private worker transport. Task2 implementer confirmed and updates SQL/fixture; fail status retry_wait|failed, cancel acknowledgement cancelled. Public status stays unchanged.

Task3 brief extracted after those preflight rulings. Exact existing sandbox source/test copies saved under `/tmp/1hk-resave-control-m6Q9WS/drawing-native-dwg-sandbox.server.ts` and drawing-native-dwg-resave-sandbox.test.mjs, before any Task3 code edits. New worker files will compare against absent pre-task files. Docker label inventory after controller native probe showed zero remaining owned reader/resaver containers.

Task2 advisors ran inside the migrated wrapper database (not empty maintenance postgres). Controller parsed retained raw JSON:274 findings,0ERROR/12WARN/262INFO. Eleven existing price/BOQ policy initplan WARN and one existing files DELETE-policy WARN; no resave WARN/ERROR. Resave INFO is an unused tiny-fixture FK index plus intentional forced-RLS/no-application-policy findings. Do not describe the whole database as warning-free or add application policies just to silence these. The missing new composite FK index was fixed before the final advisors run.

Controller final-build: after Task3 freeze, mechanically synced app/tests/migrations/native-worker source and matching package/build configuration to owned `/Users/h/1hk-r2-restore-uvc74K/platform`, without deletion or dependency installation. Checksum dry-run confirmed app source equality. Build/typegen/tsc exited0, log controller-owned-build.log under the task temp root. SENTRY_AUTH_TOKEN/ORG/PROJECT/DSN and VERCEL explicitly blank; only robots/sitemap prerendered. Existing build warnings are recorded in task-3-review.md. Original4173/PID80284 and index SHAab8778babeb2aa04f070a609cbd1240760b3158cb5aa1e6ced3da34bb9a9f84c remain unchanged. Fresh controller focused, actual native and realPG final checks are running during whole-unit review.

## Owned verification runtime

Supabase CLI2.114.0 help was inspected. PostgreSQL17.6 uses cached image `sha256:99b1729aeb0bac314445024fc149fbd39306170b61dd50800ccf180327ab3459` in owned container `6a694ad8dceadb81344c551be3788a72735ce68d1097e9994dc08c74d6280076`, name `1hk-resave-control-pg-20260906-m6q9ws`, loopback host port32779, tmpfs data. The first empty setup container was removed because its default listen address was container-loopback only; no task data had been created. The replacement explicitly listens inside the container and exposes only127.0.0.1 on the host. Local-only throwaway credentials are not customer credentials.

The owned PostgreSQL fixture's prerequisite collaboration role was created and granted to postgres with INHERIT FALSE/SET TRUE using explicit loopback `supabase db query`; both commands exited0. The CLI's generic “remote database” wording refers here to its `--db-url` transport, whose endpoint was the verified local container. Its first TLS attempt and first multi-command prepared statement failed before changes; explicit local sslmode=disable and one statement per invocation succeeded.

Existing copied-runtime M1 real database baseline started as session44750, log `/tmp/1hk-resave-control-m6Q9WS/database-baseline.log`. It creates/drops its own isolated database and has independent worker connections; this is not actual Supabase Auth/JWKS/Storage/browser acceptance. Do not confuse it with the later new resave tests.

Baseline session44750 exited1 before new queue code: the existing rolled-back corrupt-evidence probe could not SET session_replication_role (42501 rather than expected PNR01). Read-only pg_roles/has_parameter_privilege showed owned image postgres is non-superuser without that parameter grant; a minimal BEGIN/SET LOCAL/ROLLBACK reproduced it. The owned container's supabase_admin granted only SET on session_replication_role to its fixture postgres; the exact minimal probe then succeeded. No production/application role or expected denial was changed. Fresh configured baseline log: `/tmp/1hk-resave-control-m6Q9WS/database-baseline-configured.log`.

Configured baseline session13077 exited0:1/1 real PostgreSQL wrapper passed in6045ms including three approved-resave-source acceptance probes. Pre-existing identifier-truncation NOTICE output is retained in the raw log; this is not pristine output and is not caused by Task1 or the new queue. It remained available through Task2 and the controller final database check.

Controller final verification completed: focused275pass/0fail/1opt-in-skip, actualnative1/1 and realPG1/1, all exit0. Report controller-verification.md and final-source-identity.md record exact code/runtime/log identities. Final reviewer read this evidence. Only a test assertion is now being strengthened; no SQL/runtime production fix was requested.

Owned runtime cleanup: after successful final database run and integration review, verified zero m1_* databases and zero other client sessions, then revalidated exact container ID/name/task label and removed only6a694ad8dceadb81344c551be3788a72735ce68d1097e9994dc08c74d6280076. Disposable tmpfs data is gone; reproducible fixtures/logs/source remain. Native reader/resaver label inventory is empty. Original4173 preview remains untouched.

Next-unit read-only preparation by /root/resave_publication_seams is retained in next-publication-seams.md: existing no-overwrite/readback/close/publication and final-authorized download reuse points, imported caps/prefix/retention changes, and control across Storage settlement. It is not a binding design or implemented publication feature.
