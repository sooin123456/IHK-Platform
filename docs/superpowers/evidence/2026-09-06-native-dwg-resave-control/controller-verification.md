# Final controller verification

Date2026-09-06; authoritative universal-workspace-m1 worktree, HEAD9f5f56d93db325ff935772252f9d4fb64d69f98c. All commands used existing dependencies and local disposable/public synthetic evidence. Final whole-unit and scoped test-fix reviews are accepted with no open findings; this is a bounded backend gate, not a release claim.

## Executed on frozen code

Raw logs under /tmp/1hk-resave-control-m6Q9WS:

| Check | Log | Result |
| --- | --- | --- |
| All13 focused native attestation/source/compiler/protocol/jobs/worker/sandbox regression files listed in Task3 report | controller-focused-final.log | exit0;275pass,0fail,1explicit-opt-in skip;38.94s |
| Opt-in actual execution bridge using public synthetic source/cached native image | controller-actual-final.log | exit0;1/1;2.97s |
| Real PostgreSQL clean migration chain, new resave authority/replay/capacity/lease/cancel/grants/retention plus existing M1 contracts | controller-database-final.log | exit0;1/1;9.08s |
| Owned-copy npm run build, including React Router typegen and TypeScript | controller-owned-build.log | exit0; client/server/static resources emitted |

These are277 passing tests across the three final commands, not the sum of repeated historical executions. The opt-in skip in the first command is the same test run separately in the second. Both application and native-worker direct no-emit TypeScript checks separately passed on these frozen files in Task3; their empty logs/hashes are in task-3-report.md. No browser/Auth/Storage acceptance was rerun in this backend unit.

Environment: Node26.5.0 with NODE_OPTIONS=--no-experimental-webstorage. PostgreSQL command used M1_REAL_POSTGRES_REQUIRED=1 and the exact verified loopback32779 disposable URL documented in task-2-runtime-notes.md; no linked/remote Supabase project. Actual native command used NATIVE_DWG_RESAVE_WORKER_SANDBOX=1 and a fresh evidence directory controller-actual-final. Build ran only in /Users/h/1hk-r2-restore-uvc74K/platform with SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT, SENTRY_DSN and VERCEL explicitly empty. No environment/dependency installation or external build publication.

## Native boundaries and identities

Actual native source/read/write/readback and cancellation process cleanup are real. Approval/claim/service replies in the native probe are finite in-memory fixtures; actual database races are a separate test, not a combined Auth/Storage/browser result. Controller probe summary: controller-actual-final/summary.json.

- Immutable image sha256:c921c67ddb37d2a57969d04c7a4983f6d847e0b89901403e3b015ac25bf566ae.
- Original10987byte AC1024 source SHA256bcc54d3c768444c9ade41a23e4ef2b9f5b5668d9972522ee4a4adbc98c670434, unchanged before/after.
- Exact request SHA256c0284f1afc70b7c8641c9010b1f61131038202ea3490730ad7e7d9f226f83a2a; authority SHA256fbaea666300bcce97b78b55d7184d58fd7837be2c859eeff50fa4de0dc21de17.
- Output11019bytes SHA256a9ef2ff0397178b67cf0698353aaa746694497237c1ed4793cd3262ac48341e2; native report SHA256f372fb3170d0a6127a8832be22f2a3a896215b32f015406e1456f60187e17a2c. Native binary outputs can vary across writes; semantic readback and exact per-run integrity, not deterministic whole-DWG byte identity, are proved.
- Exact running cancellation container ff00710b872f310313b77351bb7e3792c26eb0654455df7bd56210058bb06e90 and its private CLI configuration were absent before acknowledgement; execution aborted while control/cleanup remained alive.
- Publication calls0; experimental-unqualified; persistenceAuthority:not-issued.

## Preserved state and warnings

All15 task files hash-match the owned build copy; see final-source-identity.md. Original preview4173/PID80284 remains listening and its build was not replaced. Index SHA256ab8778babeb2aa04f070a609cbd1240760b3158cb5aa1e6ced3da34bb9a9f84c remains unchanged. No stage/commit/push/deploy or customer DB/Storage mutation.

Build retains existing large-chunk/React Router future-flag/mixed BOQ import/unsigned theme-cookie warnings. Actual migrated-database advisors previously recorded0ERROR/12WARN/262INFO;12WARN concern existing price/BOQ policies and a files DELETE policy, not the new resave tables. No warning-free production claim. Retention cleanup for owned wrapper databases passed. After integration review, verified no remaining m1_* databases or other client sessions, revalidated exact ID/name/task-label ownership and removed only the disposable PostgreSQL container6a694ad8dceadb81344c551be3788a72735ce68d1097e9994dc08c74d6280076. Test source/logs remain; only disposable tmpfs data was discarded. No owned native reader/resaver containers remain.

## Final test-only review correction

Whole-unit reviewer accepted production architecture/integration without Critical/Important findings. Its sole Minor was a vacuous numeric-only uppercase UUID test in the jobs adapter. The final fix uses alphabetic project/request UUIDs and literal normalized source/admission RPC arguments, preserving abort-ignoring coverage. Final-review-fix-report.md records82/82 covering tests with0fail/0skip and final hash; controller independently reran the same command with82/82 in controller-fix-covering.log. These overlap earlier275 tests and are not added to the unique count. Production code/migration/build inputs are unchanged; no production rebuild or DB/native rerun is needed for a stronger test oracle. Scoped re-review accepted the exact final-review-fix-delta.patch without new breakage.

Final preservation check: exact-source whitespace check exit0; original4173/PID80284 unchanged and preview URL HTTP200; index hash unchanged. Evidence retained because there is no task commit history. No deployment or browser UI update occurred in this unit.

## Required next acceptance

Publish immutable attempt-scoped content-addressed DWG/edit_request/authority/report with exact upload/readback/close and fenced completion; receipt-only final-authorized downloads; real authenticated imported-DWG edit/approval/relogin/download/cancel/retry; exact approved DWG/PDF/BOQ delivery package; independent recipient CAD/corpus qualification. These are not complete, and the full Universal Workspace goal stays active.
