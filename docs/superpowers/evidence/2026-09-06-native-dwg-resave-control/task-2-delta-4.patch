diff --git a/platform/tests/fixtures/drawing-native-dwg-resave-jobs-database.mjs b/platform/tests/fixtures/drawing-native-dwg-resave-jobs-database.mjs
new file mode 100644
index 0000000..ec5c97e
--- /dev/null
+++ b/platform/tests/fixtures/drawing-native-dwg-resave-jobs-database.mjs
@@ -0,0 +1,771 @@
+import assert from "node:assert/strict";
+import { createHash, randomUUID } from "node:crypto";
+import { execFile } from "node:child_process";
+import { promisify } from "node:util";
+import { fileURLToPath } from "node:url";
+import { createServer } from "vite";
+
+const imageId = "sha256:" + "c".repeat(64);
+const session = (sql, role, actor, callback, jwtRole = role) =>
+  sql.begin(async (tx) => {
+    assert.ok(
+      [
+        "authenticated",
+        "service_role",
+        "anon",
+        "lukas_drawing_collaboration",
+      ].includes(role),
+    );
+    await tx.unsafe(`set local role "${role}"`);
+    await tx`select set_config('request.jwt.claims',${JSON.stringify({ role: jwtRole, sub: actor, is_anonymous: false })},true)`;
+    await tx`select set_config('statement_timeout','10000',true)`;
+    return callback(tx);
+  });
+const denied = (promise, code) =>
+  assert.rejects(promise, (e) => {
+    assert.equal(e.code, code, e.message);
+    return true;
+  });
+
+// Missing replay/cancellation fencing would duplicate jobs or restart cancelled work.
+export async function proveNativeDwgResaveJobAuthority({
+  owner,
+  workerA,
+  workerB,
+  ids,
+  imported,
+  otherImported,
+}) {
+  const editor = ids.users.editor;
+  const source = (scope, actor = editor) =>
+    session(
+      owner,
+      "authenticated",
+      actor,
+      (tx) =>
+        tx`select public.lukas_qto_drawing_native_dwg_resave_source(${tx.json(scope)}) value`,
+    ).then(([r]) => r.value);
+  const scopeFor = async (revisionId) => {
+    const [r] =
+      await owner`select r.project_id,r.document_id,r.version,s.sha256,c.id canvas_id from public.lukas_drawing_revisions r join public.lukas_drawing_snapshots s on s.revision_id=r.id and s.revision_version=r.version join public.lukas_drawing_canvases c on c.revision_id=r.id where r.id=${revisionId}::uuid`;
+    return {
+      projectId: r.project_id,
+      documentId: r.document_id,
+      revisionId,
+      revisionVersion: Number(r.version),
+      canvasId: r.canvas_id,
+      snapshotSha256: r.sha256,
+    };
+  };
+  const unchangedScope = await scopeFor(imported.scope.revisionId);
+  const [clone] = await session(
+    owner,
+    "authenticated",
+    editor,
+    (tx) =>
+      tx`select public.lukas_drawing_create_from_template(${imported.scope.revisionId}::uuid,'Resave changed LINE',null::uuid,${randomUUID()}::uuid) value`,
+  );
+  const [snapshot] =
+    await owner`select private.lukas_drawing_p2_canonical_snapshot(${clone.value.revisionId}::uuid,true) value`;
+  const line = snapshot.value.objects.find(
+    (o) => o.geometry.type === "line" || o.type === "line",
+  );
+  assert.ok(line, "trusted clone contains a LINE");
+  const geometry = structuredClone(line.geometry);
+  geometry.end.x += 10;
+  await session(
+    owner,
+    "authenticated",
+    editor,
+    (tx) =>
+      tx`select public.lukas_drawing_apply_operation(${clone.value.revisionId}::uuid,${randomUUID()}::uuid,'update_objects',${tx.json({ [line.id]: line.version })},${tx.json({ type: "update_objects", updates: [{ objectId: line.id, patch: { geometry } }] })},${tx.json({ type: "update_objects", updates: [{ objectId: line.id, patch: { geometry: line.geometry } }] })})`,
+  );
+  const [review] = await session(
+    owner,
+    "authenticated",
+    editor,
+    (tx) =>
+      tx`select public.lukas_drawing_request_review(${clone.value.revisionId}::uuid) value`,
+  );
+  const [frozen] =
+    await owner`select sha256,revision_version from public.lukas_drawing_snapshots where id=${review.value.snapshotId}::uuid`;
+  for (const [who, decision] of [
+    [ids.users.reviewer, "reviewed"],
+    [ids.users.approver, "approved"],
+  ])
+    await session(
+      owner,
+      "authenticated",
+      who,
+      (tx) =>
+        tx`select public.lukas_drawing_record_revision_decision(${clone.value.revisionId}::uuid,${frozen.revision_version}::bigint,${frozen.sha256},${decision},'Resave control proof')`,
+    );
+  const editedScope = await scopeFor(clone.value.revisionId);
+  const vite = await createServer({
+    appType: "custom",
+    configFile: false,
+    logLevel: "silent",
+    resolve: {
+      alias: { "~": fileURLToPath(new URL("../../app", import.meta.url)) },
+    },
+    server: { middlewareMode: true },
+  });
+  try {
+    const { buildNativeDrawingDwgResaveAttestation: build } =
+      await vite.ssrLoadModule(
+        "/app/lukas/lib/drawing-native-dwg-resave-attestation.server.ts",
+      );
+    const attestation = await build(
+      editedScope,
+      await source(editedScope),
+      imageId,
+    );
+    const noOpAttestation = await build(
+      unchangedScope,
+      await source(unchangedScope),
+      imageId,
+    );
+    assert.equal(attestation.request.handles.length, 1);
+    assert.equal(noOpAttestation.request, null);
+    const admit = (
+      actor,
+      scope,
+      requestId,
+      a,
+      sql = owner,
+      role = "service_role",
+      jwtRole = role,
+    ) =>
+      session(
+        sql,
+        role,
+        null,
+        (tx) =>
+          tx`select public.lukas_drawing_admit_native_dwg_resave(${actor}::uuid,${tx.json(scope)},${requestId}::uuid,${tx.json(a)}) value`,
+        jwtRole,
+      ).then(([r]) => r.value);
+    const claim = (sql = owner, image = imageId) =>
+      session(
+        sql,
+        "service_role",
+        null,
+        (tx) =>
+          tx`select public.lukas_drawing_claim_native_dwg_resave(${image},300) value`,
+      ).then(([r]) => r.value);
+    const cancel = (actor, scope, jobId, sql = owner) =>
+      session(
+        sql,
+        "authenticated",
+        actor,
+        (tx) =>
+          tx`select public.lukas_drawing_cancel_native_dwg_resave(${tx.json(scope)},${jobId}::uuid) value`,
+      ).then(([r]) => r.value);
+    const status = (actor, scope, jobId) =>
+      session(
+        owner,
+        "authenticated",
+        actor,
+        (tx) =>
+          tx`select public.lukas_drawing_native_dwg_resave_status(${tx.json(scope)},${jobId}::uuid) value`,
+      ).then(([r]) => r.value);
+    const control = (c) =>
+      session(
+        owner,
+        "service_role",
+        null,
+        (tx) =>
+          tx`select public.lukas_drawing_native_dwg_resave_control(${c.jobId}::uuid,${c.attemptNumber},${c.leaseToken}::uuid) value`,
+      ).then(([r]) => r.value);
+    const fail = (c, code, retryable, sql = owner) =>
+      session(
+        sql,
+        "service_role",
+        null,
+        (tx) =>
+          tx`select public.lukas_drawing_fail_native_dwg_resave(${c.jobId}::uuid,${c.attemptNumber},${c.leaseToken}::uuid,${code},${retryable}) value`,
+      ).then(([r]) => r.value);
+    const acknowledgeCancel = (c) =>
+      session(
+        owner,
+        "service_role",
+        null,
+        (tx) =>
+          tx`select public.lukas_drawing_ack_native_dwg_resave_cancel(${c.jobId}::uuid,${c.attemptNumber},${c.leaseToken}::uuid) value`,
+      ).then(([r]) => r.value);
+    const requestId = randomUUID();
+    const first = await admit(editor, editedScope, requestId, attestation);
+    assert.deepEqual(
+      await admit(editor, editedScope, requestId, attestation),
+      first,
+    );
+    const concurrentId = randomUUID();
+    const [sameA, sameB] = await Promise.all([
+      admit(editor, editedScope, concurrentId, attestation, workerA),
+      admit(editor, editedScope, concurrentId, attestation, workerB),
+    ]);
+    assert.deepEqual(
+      sameA,
+      sameB,
+      "concurrent admission creates one immutable identity",
+    );
+    await cancel(editor, editedScope, sameA.jobId);
+    await denied(
+      admit(editor, unchangedScope, requestId, noOpAttestation),
+      "PNR12",
+    );
+    const [a, b] = await Promise.all([claim(workerA), claim(workerB)]);
+    assert.equal([a, b].filter(Boolean).length, 1);
+    const c = a ?? b;
+    assert.deepEqual(c.attestation, attestation);
+    assert.equal(c.source.fileId, imported.scope.sourceFileId);
+    assert.equal((await control(c)).action, "continue");
+    await denied(cancel(ids.users.viewer, editedScope, first.jobId), "PNR11");
+    await denied(cancel(editor, unchangedScope, first.jobId), "PNR11");
+    assert.deepEqual(
+      Object.keys(
+        await status(ids.users.viewer, editedScope, first.jobId),
+      ).sort(),
+      [
+        "attemptCount",
+        "failureCode",
+        "hasChanges",
+        "jobId",
+        "requestId",
+        "status",
+      ],
+    );
+    const [[ownerPid], [aPid], [bPid]] = await Promise.all([
+      owner`select pg_backend_pid() pid`,
+      workerA`select pg_backend_pid() pid`,
+      workerB`select pg_backend_pid() pid`,
+    ]);
+    const blockedBy = async (observer, waiter, blocker) => {
+      const deadline = Date.now() + 5000;
+      let blocked = false;
+      do {
+        const [r] =
+          await observer`select ${blocker}::integer=any(pg_blocking_pids(${waiter}::integer)) blocked`;
+        blocked = r.blocked;
+      } while (!blocked && Date.now() < deadline);
+      if (!blocked)
+        console.log(
+          "Task2 lock barrier diagnostics",
+          await observer`select pid,pg_blocking_pids(pid) blockers,wait_event_type,wait_event from pg_stat_activity where pid in (${ownerPid.pid},${aPid.pid},${bPid.pid})`,
+        );
+      assert.equal(
+        blocked,
+        true,
+        "independent session reached expected lock barrier",
+      );
+    };
+    let cancelPending, failPending;
+    await owner.begin(async (tx) => {
+      await tx`select id from public.lukas_qto_projects where id=${imported.projectId}::uuid for update`;
+      cancelPending = cancel(editor, editedScope, first.jobId, workerA);
+      await blockedBy(tx, aPid.pid, ownerPid.pid);
+      failPending = fail(c, "worker_interrupted", true, workerB).then(
+        (value) => ({ value }),
+        (error) => ({ error }),
+      );
+      // The first waiter owns the tuple lock while waiting for owner's xid.
+      await blockedBy(tx, bPid.pid, aPid.pid);
+    });
+    await cancelPending;
+    assert.equal(
+      (await failPending).error?.code,
+      "PNR13",
+      "cancel first fences a concurrently blocked failure",
+    );
+    assert.equal((await control(c)).action, "cancel");
+    await denied(fail(c, "worker_interrupted", true), "PNR13");
+    const cancelledReceipt = {
+      jobId: c.jobId,
+      attemptNumber: c.attemptNumber,
+      leaseToken: c.leaseToken,
+      status: "cancelled",
+    };
+    assert.deepEqual(await acknowledgeCancel(c), cancelledReceipt);
+    assert.deepEqual(await acknowledgeCancel(c), cancelledReceipt);
+    assert.equal(
+      (await status(editor, editedScope, first.jobId)).status,
+      "cancelled",
+    );
+    assert.equal(await claim(workerA), null);
+    assert.ok(
+      otherImported,
+      "cross-project replay proof requires a second real import",
+    );
+    const otherScope = await scopeFor(otherImported.scope.revisionId);
+    assert.notEqual(otherScope.projectId, editedScope.projectId);
+    const otherAttestation = await build(
+      otherScope,
+      await source(otherScope),
+      imageId,
+    );
+    assert.equal(
+      otherAttestation.request,
+      null,
+      "second approved import remains unchanged",
+    );
+    const crossProjectRequestId = randomUUID();
+    const crossInputs = [
+      { scope: editedScope, attestation },
+      { scope: otherScope, attestation: otherAttestation },
+    ];
+    const settle = (promise) =>
+      promise.then(
+        (value) => ({ value }),
+        (error) => ({ error }),
+      );
+    let crossA, crossB;
+    await owner.begin(async (tx) => {
+      await tx`select id from public.lukas_qto_projects where id=any(${crossInputs.map((input) => input.scope.projectId)}::uuid[]) order by id for update`;
+      crossA = settle(
+        admit(
+          editor,
+          crossInputs[0].scope,
+          crossProjectRequestId,
+          crossInputs[0].attestation,
+          workerA,
+        ),
+      );
+      await blockedBy(tx, aPid.pid, ownerPid.pid);
+      crossB = settle(
+        admit(
+          editor,
+          crossInputs[1].scope,
+          crossProjectRequestId,
+          crossInputs[1].attestation,
+          workerB,
+        ),
+      );
+      await blockedBy(tx, bPid.pid, ownerPid.pid);
+    });
+    const crossOutcomes = await Promise.all([crossA, crossB]);
+    assert.equal(
+      crossOutcomes.filter((outcome) => outcome.value).length,
+      1,
+      "one cross-project admission fulfills",
+    );
+    assert.equal(
+      crossOutcomes.filter((outcome) => outcome.error?.code === "PNR12").length,
+      1,
+      "other valid project conflicts with global actor/request identity",
+    );
+    const winnerIndex = crossOutcomes.findIndex((outcome) => outcome.value);
+    const winner = crossOutcomes[winnerIndex].value;
+    const winningInput = crossInputs[winnerIndex];
+    const losingInput = crossInputs[1 - winnerIndex];
+    const readCrossRows = () =>
+      owner`select * from public.lukas_drawing_native_dwg_resave_jobs where requested_by=${editor}::uuid and request_id=${crossProjectRequestId}::uuid`;
+    const crossRows = await readCrossRows();
+    assert.equal(crossRows.length, 1);
+    assert.equal(crossRows[0].id, winner.jobId);
+    assert.equal(crossRows[0].project_id, winningInput.scope.projectId);
+    assert.deepEqual(crossRows[0].scope, winningInput.scope);
+    assert.deepEqual(crossRows[0].attestation, winningInput.attestation);
+    for (const sql of [workerB, workerA]) {
+      assert.deepEqual(
+        await admit(
+          editor,
+          winningInput.scope,
+          crossProjectRequestId,
+          winningInput.attestation,
+          sql,
+        ),
+        winner,
+      );
+      await denied(
+        admit(
+          editor,
+          losingInput.scope,
+          crossProjectRequestId,
+          losingInput.attestation,
+          sql,
+        ),
+        "PNR12",
+      );
+    }
+    assert.deepEqual(
+      await readCrossRows(),
+      crossRows,
+      "later replay/conflict preserves the exact single stored row",
+    );
+    if (winner.hasChanges)
+      await cancel(editor, winningInput.scope, winner.jobId);
+    console.log(
+      "Task2 cross-project replay: two real approved imported scopes, independent project-lock barriers, one fulfillment/one PNR12, one immutable row, stable replay/conflict",
+    );
+    const noOpId = randomUUID();
+    const noOp = await admit(
+      ids.users.viewer,
+      unchangedScope,
+      noOpId,
+      noOpAttestation,
+    );
+    assert.equal(noOp.hasChanges, false);
+    assert.equal(
+      (await status(editor, unchangedScope, noOp.jobId)).status,
+      "no_changes",
+    );
+    assert.deepEqual(
+      await admit(ids.users.viewer, unchangedScope, noOpId, noOpAttestation),
+      noOp,
+    );
+    await denied(
+      admit(
+        editor,
+        editedScope,
+        randomUUID(),
+        attestation,
+        owner,
+        "authenticated",
+      ),
+      "42501",
+    );
+    await denied(
+      admit(
+        editor,
+        editedScope,
+        randomUUID(),
+        attestation,
+        owner,
+        "service_role",
+        "authenticated",
+      ),
+      "PNR11",
+    );
+    const viewerJob = await admit(
+      ids.users.viewer,
+      editedScope,
+      randomUUID(),
+      attestation,
+    );
+    await denied(cancel(editor, editedScope, viewerJob.jobId), "PNR11");
+    await cancel(ids.users.owner, editedScope, viewerJob.jobId);
+    const active = [];
+    for (let i = 0; i < 5; i++)
+      active.push(await admit(editor, editedScope, randomUUID(), attestation));
+    await denied(
+      admit(editor, editedScope, randomUUID(), attestation),
+      "PNR15",
+    );
+    assert.deepEqual(
+      await admit(editor, editedScope, active[0].requestId, attestation),
+      active[0],
+    );
+    const capacityNoOp = await admit(
+      editor,
+      unchangedScope,
+      randomUUID(),
+      noOpAttestation,
+    );
+    assert.equal(
+      capacityNoOp.hasChanges,
+      false,
+      "no-op bypasses active capacity only after identity checks",
+    );
+    for (const job of active) await cancel(editor, editedScope, job.jobId);
+    const blockedJob = await admit(
+      editor,
+      editedScope,
+      randomUUID(),
+      attestation,
+    );
+    await owner.begin(async (tx) => {
+      await tx`select id from public.lukas_qto_projects where id=${imported.projectId}::uuid for update`;
+      assert.equal(
+        await claim(workerA),
+        null,
+        "claim skips an independently locked project",
+      );
+    });
+    await owner.begin(async (tx) => {
+      await tx`select id from public.lukas_drawing_native_dwg_resave_jobs where id=${blockedJob.jobId}::uuid for update`;
+      assert.equal(
+        await claim(workerA),
+        null,
+        "claim skips an independently locked job",
+      );
+    });
+    const blockedClaim = await claim();
+    assert.equal(blockedClaim.jobId, blockedJob.jobId);
+    await denied(
+      control({ ...blockedClaim, leaseToken: randomUUID() }),
+      "PNR13",
+    );
+    await owner`update public.lukas_drawing_native_dwg_resave_jobs set lease_expires_at=clock_timestamp()-interval '1 second' where id=${blockedJob.jobId}::uuid`;
+    assert.equal((await control(blockedClaim)).reason, "lease_expired");
+    await denied(fail(blockedClaim, "worker_interrupted", true), "PNR13");
+    const replacement = await claim();
+    assert.equal(replacement.attemptNumber, 2);
+    assert.notEqual(replacement.leaseToken, blockedClaim.leaseToken);
+    await denied(control(blockedClaim), "PNR13");
+    await cancel(editor, editedScope, blockedJob.jobId);
+    await denied(acknowledgeCancel(blockedClaim), "PNR13");
+    await acknowledgeCancel(replacement);
+    const expiredCancelJob = await admit(
+      editor,
+      editedScope,
+      randomUUID(),
+      attestation,
+    );
+    const expiredCancel = await claim();
+    await cancel(editor, editedScope, expiredCancelJob.jobId);
+    await owner`update public.lukas_drawing_native_dwg_resave_jobs set lease_expires_at=clock_timestamp()-interval '1 second' where id=${expiredCancelJob.jobId}::uuid`;
+    assert.equal(
+      (await control(expiredCancel)).action,
+      "cancel",
+      "cancellation precedes expiry for matching attempt",
+    );
+    assert.equal(
+      await claim(),
+      null,
+      "expired cancellation is finalized without another attempt",
+    );
+    assert.equal(
+      (await status(editor, editedScope, expiredCancelJob.jobId)).status,
+      "cancelled",
+    );
+    await acknowledgeCancel(expiredCancel);
+    const retryJob = await admit(
+      editor,
+      editedScope,
+      randomUUID(),
+      attestation,
+    );
+    for (let attempt = 1; attempt <= 3; attempt++) {
+      const current = await claim();
+      assert.equal(current.jobId, retryJob.jobId);
+      assert.equal(current.attemptNumber, attempt);
+      assert.deepEqual(await fail(current, "resaver_failed", true), {
+        jobId: current.jobId,
+        attemptNumber: attempt,
+        leaseToken: current.leaseToken,
+        status: attempt === 3 ? "failed" : "retry_wait",
+      });
+      if (attempt < 3) {
+        const [delay] =
+          await owner`select extract(epoch from (next_attempt_at-clock_timestamp()))::float8 seconds from public.lukas_drawing_native_dwg_resave_jobs where id=${retryJob.jobId}::uuid`;
+        assert.ok(
+          delay.seconds > attempt * 30 - 5 && delay.seconds <= attempt * 30,
+        );
+        assert.equal(await claim(), null);
+        await owner`update public.lukas_drawing_native_dwg_resave_jobs set next_attempt_at=clock_timestamp()-interval '1 second' where id=${retryJob.jobId}::uuid`;
+      }
+    }
+    assert.equal(await claim(), null);
+    for (const code of [
+      "source_mismatch",
+      "output_invalid",
+      "authority_revoked",
+    ]) {
+      await admit(editor, editedScope, randomUUID(), attestation);
+      const current = await claim();
+      assert.equal((await fail(current, code, true)).status, "failed");
+    }
+    const revokedJob = await admit(
+      editor,
+      editedScope,
+      randomUUID(),
+      attestation,
+    );
+    const revokedClaim = await claim();
+    const revokedQueued = await admit(
+      editor,
+      editedScope,
+      randomUUID(),
+      attestation,
+    );
+    const rollback = Symbol("rollback owned corruption");
+    // Corrupt evidence only inside a rolled-back transaction. The private resolver
+    // must reject it even when the previous scope and source digest still match.
+    for (const mutate of [
+      (tx) =>
+        tx`update public.lukas_drawing_native_dwg_import_jobs set source=jsonb_set(source,'{path}','"changed/private.dwg"') where id=${imported.jobId}::uuid`,
+      (tx) =>
+        tx`update public.lukas_drawing_snapshots set canonical_json=jsonb_set(canonical_json,'{operationSequence}','99999') where revision_id=${editedScope.revisionId}::uuid`,
+    ]) {
+      try {
+        await owner.begin(async (tx) => {
+          await tx`set local session_replication_role=replica`;
+          await mutate(tx);
+          await tx`set local session_replication_role=origin`;
+          await tx`set local role service_role`;
+          await tx`select set_config('request.jwt.claims','{"role":"service_role"}',true)`;
+          const [r] =
+            await tx`select public.lukas_drawing_native_dwg_resave_control(${revokedClaim.jobId}::uuid,${revokedClaim.attemptNumber},${revokedClaim.leaseToken}::uuid) value`;
+          assert.equal(r.value.reason, "authority_revoked");
+          throw rollback;
+        });
+      } catch (e) {
+        if (e !== rollback) throw e;
+      }
+    }
+    await owner`delete from public.lukas_qto_project_members where project_id=${imported.projectId}::uuid and user_id=${editor}::uuid`;
+    assert.equal((await control(revokedClaim)).reason, "authority_revoked");
+    assert.equal(
+      await claim(),
+      null,
+      "queued work with revoked authority cannot claim",
+    );
+    const [revokedQueuedState] =
+      await owner`select status,failure_code from public.lukas_drawing_native_dwg_resave_jobs where id=${revokedQueued.jobId}::uuid`;
+    assert.deepEqual(revokedQueuedState, {
+      status: "failed",
+      failure_code: "authority_revoked",
+    });
+    await denied(cancel(editor, editedScope, revokedJob.jobId), "PNR11");
+    assert.equal(
+      (await fail(revokedClaim, "worker_interrupted", true)).status,
+      "failed",
+    );
+    await owner`insert into public.lukas_qto_project_members(project_id,user_id,role) values(${imported.projectId}::uuid,${editor}::uuid,'estimator')`;
+    const invalidAttestations = [];
+    for (const mutate of [
+      (a) => (a.authority.sha256 = "0".repeat(64)),
+      (a) => a.request.byteSize++,
+      (a) => (a.request.handles = ["00"]),
+      (a) => (a.extra = true),
+      (a) => (a.request.extra = true),
+    ]) {
+      const changed = structuredClone(attestation);
+      mutate(changed);
+      invalidAttestations.push(changed);
+    }
+    const wrongEnvelope = structuredClone(attestation);
+    const envelope = JSON.parse(wrongEnvelope.authority.text);
+    envelope.analysis.receipt.jobId = randomUUID();
+    wrongEnvelope.authority.text = JSON.stringify(envelope);
+    wrongEnvelope.authority.byteSize = Buffer.byteLength(
+      wrongEnvelope.authority.text,
+    );
+    wrongEnvelope.authority.sha256 = createHash("sha256")
+      .update(wrongEnvelope.authority.text)
+      .digest("hex");
+    invalidAttestations.push(wrongEnvelope);
+    for (const invalid of invalidAttestations)
+      await denied(admit(editor, editedScope, randomUUID(), invalid), "PNR11");
+    for (const role of [
+      "anon",
+      "authenticated",
+      "service_role",
+      "lukas_drawing_collaboration",
+    ]) {
+      for (const [signature, allowed] of [
+        ["private.lukas_drawing_native_dwg_resave_context(uuid,jsonb)", false],
+        ["private.lukas_drawing_native_dwg_resave_locked_job(uuid)", false],
+        [
+          "public.lukas_drawing_admit_native_dwg_resave(uuid,jsonb,uuid,jsonb)",
+          role === "service_role",
+        ],
+        [
+          "public.lukas_drawing_claim_native_dwg_resave(text,integer)",
+          role === "service_role",
+        ],
+        [
+          "public.lukas_drawing_native_dwg_resave_control(uuid,integer,uuid)",
+          role === "service_role",
+        ],
+        [
+          "public.lukas_drawing_fail_native_dwg_resave(uuid,integer,uuid,text,boolean)",
+          role === "service_role",
+        ],
+        [
+          "public.lukas_drawing_ack_native_dwg_resave_cancel(uuid,integer,uuid)",
+          role === "service_role",
+        ],
+        [
+          "public.lukas_drawing_native_dwg_resave_status(jsonb,uuid)",
+          role === "authenticated",
+        ],
+        [
+          "public.lukas_drawing_cancel_native_dwg_resave(jsonb,uuid)",
+          role === "authenticated",
+        ],
+      ]) {
+        const [grant] =
+          await owner`select has_function_privilege(${role},${signature},'EXECUTE') allowed`;
+        assert.equal(grant.allowed, allowed, `${role} ${signature}`);
+      }
+    }
+    for (const suffix of ["jobs", "attempts"]) {
+      const table = `public.lukas_drawing_native_dwg_resave_${suffix}`;
+      for (const role of [
+        "anon",
+        "authenticated",
+        "service_role",
+        "lukas_drawing_collaboration",
+      ]) {
+        const [grants] =
+          await owner`select has_table_privilege(${role},${table},'SELECT,INSERT,UPDATE,DELETE') access`;
+        assert.equal(grants.access, false);
+      }
+      const [rls] =
+        await owner`select relrowsecurity,relforcerowsecurity from pg_class where oid=${table}::regclass`;
+      assert.deepEqual(rls, {
+        relrowsecurity: true,
+        relforcerowsecurity: true,
+      });
+      await denied(
+        owner.unsafe(`delete from ${table} where project_id=$1::uuid`, [
+          imported.projectId,
+        ]),
+        "PNR11",
+      );
+      await denied(
+        owner.begin(async (tx) => {
+          await tx`select set_config('app.lukas_retention_purge_project',${imported.projectId},true)`;
+          await tx.unsafe(`delete from ${table} where project_id=$1::uuid`, [
+            imported.projectId,
+          ]);
+        }),
+        "PNR11",
+      );
+    }
+    await denied(
+      owner`update public.lukas_drawing_native_dwg_resave_jobs set status='queued' where id=${first.jobId}::uuid`,
+      "PNR11",
+    );
+    await denied(
+      owner`update public.lukas_drawing_native_dwg_resave_attempts set outcome='failed' where job_id=${first.jobId}::uuid`,
+      "PNR11",
+    );
+    await denied(
+      owner`update public.lukas_drawing_native_dwg_resave_jobs set requested_by=${ids.users.owner}::uuid where id=${first.jobId}::uuid`,
+      "PNR11",
+    );
+    if (process.env.M1_RESAVE_ADVISORS === "1") {
+      const [db] = await owner`select current_database() name`;
+      const url = new URL(process.env.M1_REAL_POSTGRES_DATABASE_URL);
+      assert.equal(url.hostname, "127.0.0.1");
+      assert.equal(url.port, "32779");
+      assert.match(db.name, /^m1_\d+_[0-9a-f]+$/);
+      url.pathname = `/${db.name}`;
+      const { stdout, stderr } = await promisify(execFile)(
+        "supabase",
+        [
+          "db",
+          "advisors",
+          "--db-url",
+          url.toString(),
+          "--type",
+          "all",
+          "--level",
+          "info",
+          "--fail-on",
+          "none",
+          "--output-format",
+          "json",
+        ],
+        { timeout: 60000, maxBuffer: 16 * 1024 * 1024 },
+      );
+      console.log("Task2 migrated database advisors", db.name, stdout, stderr);
+    }
+    console.log(
+      "Task2 real database: replay/no-op/capacity, role+JWT+RLS/grants, locked project/job SKIP LOCKED, pg_blocking_pids ordered cancel/failure race, expiry/replacement/three attempts, rolled-back changed-source/snapshot evidence, live revocation and immutable terminal retention guards passed",
+    );
+  } finally {
+    await vite.close();
+  }
+}
