# Task 2 — DONE

Implemented canonical initialization and durable first admission in the existing storage/server boundary. No unresolved implementation concerns found in self-review. Root's separate Task 3 supplies the actual PostgreSQL adapter + WebSocket integration gate; this task's actual socket test uses the production server and storage facade over an in-memory database double with real Yjs bytes, computed SHA, and generation/CAS bookkeeping.

## Exact owned files

- `platform/collaboration/src/storage.ts`: exact initialization input/interfaces, user/service PostgreSQL calls and full returned state mapping, existing transient retry, winner-only adoption without CAS merge, candidate/winner validation before seeding tokens, user initialization seeds both authorities. Existing reads now validate before seeding a usable token as well.
- `platform/collaboration/src/server.ts`: one bounded canonical loader shared by browser, detached freeze/authority and detached receipt paths; absent support fails closed; only P3S04 rebuilds the candidate, up to three attempts. Browser reconciles detached state, reloads and compares normalized Yjs encodings, retries concurrent movement up to three passes, refuses unpersisted transient fences with `DRAWING_RECONCILIATION_RETRY`/`retryable=true`, and persists active corrections only through guarded service storage followed by reload. Receipt detached cleanup is in `finally`; malformed `Y.applyUpdate` validation is now inside cleanup scope.
- `platform/tests/drawing-collaboration-service.test.mjs`: only affected first-admission/receipt fixtures made durable. Retains the original pending-first-boot test and its assertions; adds initialization count to restart receipt coverage. New fixture rows compute hashes from actual bytes.
- `platform/tests/drawing-collaboration-initialization.test.mjs`: 15 focused cases, including actual Hocuspocus/WebSocket untouched initialization and cached replay after server restart, with zero ordinary stores required; Viewer/approved user initialization; unchanged Viewer/protected forgery rejection; deferred winner admission; losing origin discarded; stale candidate bounds; invalid winner/token rejection; active correction persistence; foreign lease and local preparation refusal; missing initializer refusal; service authority/receipt origin; cleanup after receipt conflicts/store failure and malformed bytes; transient initialization retry; bounded concurrent movement.

`drawing-review-freeze.test.mjs` and `drawing-native-collaboration.test.mjs` remain byte-identical to the supplied Task 2 baselines. No SQL, accepted M1 test, dependencies, preview build, staging, commits, deployment, or remote writes were performed. HEAD remains `9f5f56d93db325ff935772252f9d4fb64d69f98c`.

## RED / GREEN evidence

All commands below ran from `platform` with `NODE_OPTIONS=--no-experimental-webstorage`.

1. Before production edits: `node --experimental-strip-types --test tests/drawing-collaboration-initialization.test.mjs` — initial eight tests failed, zero passed. Recorded in `task-2-red.log`. Evidence includes the missing initializer API, payload already containing four collections instead of remaining empty, and missing initial persistence. The shell redirected output and tailed the log; the recorded test summary is the RED evidence.
2. First implementation run: same command — six passed, two failed. Preserved in **`task-2-green-initial.log` (misnamed intermediate failure log, NOT GREEN evidence)**. Both remaining failures were fixture corrections: the foreign lease field must be `leaseExpiresAtMs`; the signed operation needs its `revisionId`. Corrected fixtures, reran eight cases, all passed. No production workaround or relaxed validator was introduced for these fixture errors.
3. Added focused socket, concurrency, unavailable-support, cleanup, and transport-retry coverage, reaching 15/15 passing. Intermediate focused output was returned directly; final stable log includes every one of these tests.
4. Affected legacy selection: `node --experimental-strip-types --test --test-name-pattern='real HocuspocusProvider|production-shaped first boot|outcome endpoint|signed outcome|polling persists|actual Hocuspocus|frame' tests/drawing-collaboration-service.test.mjs` — 11/11 passed, no skips/failures. `task-2-legacy-focused.log`.
5. One final full named regression: `node --experimental-strip-types --test tests/drawing-collaboration-service.test.mjs tests/drawing-review-freeze.test.mjs tests/drawing-native-collaboration.test.mjs tests/drawing-collaboration-protocol.test.mjs tests/drawing-workspace-collaboration.test.mjs tests/drawing-collaboration-policy.test.mjs tests/drawing-collaboration-e2e-contract.test.mjs tests/drawing-collaboration-initialization.test.mjs` — **155/155 passed, zero skipped, zero failed**, exit 0. `task-2-full-green.log`.
6. `npx tsc -p collaboration/tsconfig.json --noEmit` — exit 0, no diagnostics. `task-2-tsc.log` (empty because successful).
7. `curl --silent --show-error --output /dev/null --write-out '%{http_code}\n' http://localhost:4173/` — HTTP 200. No build command run.

## Self-review and boundaries

Read the complete exact diff against the supplied baseline for all three modified existing files; reviewed the new test file. Removed three unrelated formatter changes in pre-existing service tests. `git diff --check` passed for owned production/service files. Existing protected-state validation and ordinary CAS merge behavior are unchanged; initial candidates never take that merge route. Viewer initialization uses the verified user authority without an editor guard or service fallback. Detached service loaders intentionally adopt durable state before freeze/receipt mutation, while first browser payload remains empty until durable normalized equality.

No commit created. Deployment ordering remains migration before server rollout. Existing live-room transient projections, legacy divergent IndexedDB cache recovery, and the broader R2/R4/R5 goal remain outside this completed unit.

## Fix round 1 — wire retry reason and refused payload cleanup — DONE

Root/reviewer found that Hocuspocus serializes only `error.reason`, losing the local retry classification. This round adds `reason: "drawing-reconciling"` to the existing reconciliation error. The actual socket RED also exposed the upstream unregistered-payload cleanup gap: Hocuspocus calls `unloadDocument` after failed load, but that routine returns without destroying a document absent from its room map. Root explicitly included this same-boundary production fix. The server's `onLoadDocument` now destroys the never-admitted payload on failure and rethrows the original error. Dependencies and authorization/validation guards remain unchanged.

Only `platform/collaboration/src/server.ts` and `platform/tests/drawing-collaboration-initialization.test.mjs` changed in this fix, compared with `baseline-task2-fix1`. Two tests added: actual WebSocket receives exact `drawing-reconciling`, leaves both refused payload and client empty, verifies refused payload destruction and no registered room, clears the foreign lease, explicitly calls `provider.sendToken()`, observes the production admission message and same canonical server bytes, then explicitly calls `provider.startSync()` and verifies the same client bytes. Initialization stays at one and ordinary stores stay at zero. A separate actual Hocuspocus `createDocument` refusal asserts destruction for unavailable initialization. Both tests retain a fallback destroy in `finally` only after assertions so RED cannot leave upstream Awareness intervals running.

Explicit retry detail: `sendToken()` sends authentication only. The public provider `startSync()` call is a separate required synchronization step after the initial refused queue was discarded. This test proves explicit token retry plus explicit sync. **Automatic application UI retry is neither implemented nor proven.** A pending R2 follow-up must consume `drawing-reconciling` appropriately; no client application files changed.

Commands from `platform`, all with `NODE_OPTIONS=--no-experimental-webstorage`:

- Bounded RED before production fix: `node --experimental-strip-types --test --test-name-pattern='real WebSocket reports drawing-reconciling|failed Hocuspocus initial admission' tests/drawing-collaboration-initialization.test.mjs` — exit 1, zero passed/two failed. `task-2-fix1-bounded-red.log` shows `permission-denied` instead of `drawing-reconciling` and `isDestroyed: false` instead of true.
- Preserved exploratory logs `task-2-fix1-red.log` and `task-2-fix1-wire-red.log` caught the wire assertion but remained alive because the failed payload was unregistered; only the two owned Node test children were terminated. The final test observes real load payloads through a Hocuspocus extension (changing `configuration.onLoadDocument` after construction does not replace the installed hook). The bounded RED above exits normally and is the acceptance RED evidence.
- Intermediate `task-2-fix1-focused-green.log` is **not GREEN evidence**: cleanup passed, wire retry timed out while incorrectly awaiting sync from `sendToken()` alone. Inspection of provider `onOpen`/`sendToken`/`startSync` and root's explicit ruling corrected the test to distinguish admission from synchronization, without application changes.
- Same focused two-test command after fix: exit 0, **2/2 passed**, zero skipped/failed. Stable `task-2-fix1-focused-final-green.log`.
- Final full named command: `node --experimental-strip-types --test tests/drawing-collaboration-service.test.mjs tests/drawing-review-freeze.test.mjs tests/drawing-native-collaboration.test.mjs tests/drawing-collaboration-protocol.test.mjs tests/drawing-workspace-collaboration.test.mjs tests/drawing-collaboration-policy.test.mjs tests/drawing-collaboration-e2e-contract.test.mjs tests/drawing-collaboration-initialization.test.mjs` — exit 0, **157/157 passed**, zero skipped/failed. Stable `task-2-fix1-full-green.log`. Expected Hocuspocus refused-load diagnostic messages remain in the logs.
- `npx tsc -p collaboration/tsconfig.json --noEmit` — exit 0, zero diagnostics. Stable `task-2-fix1-tsc.log`.

Read and reviewed the entire two-file delta against `baseline-task2-fix1`; `git diff --check` passed. No remaining identified fix-round concern. No other production/test scope changes, commits, build, deployment, dependency updates or remote writes. Root owns the final PostgreSQL integration rerun against this code.
