# SDD ledger — plan: docs/superpowers/plans/2026-09-06-native-dwg-resave-publication.md

## User priority change — 2026-09-07

Latest explicit user request is UI-first: organize the target screen composition one screen at a time, then implement functionality. This supersedes autonomous continuation of this backend/native unit for now. No additional native runner or implementation dispatch is authorized until the new screen-first direction is settled. Existing work and goal remain preserved; this is not goal completion or a technical blocked condition. Controller sent current Task5 agent a stop-new-work/owned-runtime-cleanup/report-only instruction. First actual Task5 run failed before app mutation on a test oracle: native baseline nonModelSpaceEntities is3 rather than hardcoded0. Native actual acceptance remains unpassed; source correction/cleanup details await final agent checkpoint. Resume from that checkpoint, not Tasks1–4.

## Identity and authority

Worktree /Users/h/Documents/GoAgent/.worktrees/universal-workspace-m1, branch codex/universal-workspace-m1, HEAD 9f5f56d93db325ff935772252f9d4fb64d69f98c. Index SHA256 ab8778babeb2aa04f070a609cbd1240760b3158cb5aa1e6ced3da34bb9a9f84c. Exact pre-task snapshots are the review authority; unrelated dirty changes remain user-owned.

Overall unbudgeted goal remains active. Previous control unit is complete and independently accepted; fresh275focused+1actualPG+1actualnative and owned-copy build passed. Do not redispatch it. Current unit advances imported-DWG publication; R5 exact-revision packages and independent recipient CAD qualification remain subsequent work.

No commit/stage/merge/push/deploy, remote/customer mutation, paid/license service, dependency addition, private .superpowers/sdd access or original4173 rebuild/restart. Public artifacts replace skill-private ledger scripts. Owned build copy /Users/h/1hk-r2-restore-uvc74K/platform; owned temp /tmp/1hk-resave-publication-uU7wEx. Prior disposable PG removed; its32779 URL is not live.

## Preflight and controller self-review

| Tasks | Producer vs consumer or internal check | Result |
| --- | --- | --- |
| 1→2 | strict ordered metadata/receipt/descriptor schemas vs SQL exact fields/path and user RPC parsing | Same four kinds/caps/attempts; DB independently derives paths. |
| 1→3 | build snapshots and stage/receipt validation vs publication/readback | Full attestation rebuild retained; unknown POST cannot close. |
| 1→4 | imported Storage profile and receipt descriptors vs CLI/download | Same profile; download must actually bound stalled reads. |
| 1→5 | synthetic finite unit output vs real native acceptance | Distinct evidence, no finite-native claim. |
| 2→3 | stage/close/publish/fail/ack and open-state fencing vs worker order | Same exact historical close; no retry/terminal while open. |
| 2→4 | latest authenticated status/receipt/descriptor vs HTTP/UI | No actor/path/lease in public response; exact scope and latest null. |
| 2→5 | canonical import/approval fixture and retention vs real acceptance | Full-chain local DB and fresh Auth required; not fabricated snapshots. |
| 3→4 | runNativeDrawingDwgResaveToStorage options/outcomes vs CLI | Native prepared is only publisher input; no-op never claimed. |
| 3→5 | uncertainty and poll behavior vs actual worker story | Full actual success plus explicitly finite race seams. |
| 4→5 | route/UI/CLI config vs browser/runtime | Single DWG control, same actor/scope request namespace, owned copy only. |
| 1 internal | tests vs core/transport file list | Existing Storage tests located at drawing-native-dwg-worker.test.mjs; corrected plan. |
| 2 internal | migration/adapters vs dependencies | User RPC adapter imports core+jobs; no core→adapter cycle. Service RPC calls owned by Task3; no forward helper dependency. |
| 3 internal | close/uncertain tests vs native helper extraction | Existing native lifecycle unchanged; publisher independently owns polls. |
| 4 internal | route tests vs fixed handler names | Exact new handler signatures recorded; existing source-free exporter filename confirmed. |
| 5 internal | fixture/native claims vs accepted source | Public10987byte SHA fixed; different13387byte ingestion fixture is not substituted. |

Ruling: separate authenticated artifact RPC adapters from existing job parser — avoids a runtime import cycle while reusing claim validation — costs one focused adapter file; if wrong, consolidate after interface review.

Ruling: retain uncertainty after a refused/unconfirmed stage-close sequence rather than infer not_started — replay may already have opened a session — costs possible operational reconciliation after an RPC outage.

Ruling: standard bounded server upload remains the initial transport and the local50MiB bucket stays unchanged — existing dependency handles synthetic acceptance — costs no claim of200MiB performance; large-file transport qualification remains separate.

Ruling: use public exact pre-task baseline/brief/report/diff artifacts and do not commit — preserves user's standing privacy/worktree instructions — costs manual review packaging rather than skill scripts.

## Task state

- Task 1: complete (no commits; exact public baseline delta; independent task review plus fix1 re-review clean). Both Important findings addressed, none parked/deferred. Final controller90/90 and owned-copy build/typecheck pass; exact logs controller-task-1-tests.log and controller-task-1-build.log. Existing build warnings remain: large chunks, ReactRouter future flags, mixed BOQ import and unsigned theme cookie. No browser/native/DB publication-completion claim.
- Task 2: complete (no commits; exact nine-file public baseline delta). Independent /root/resave_publication_database_review: spec compliant, task quality Approved, no Critical/Important, one nonblocking existing-validation-noise Minor tracked below. Review task-2-review.md resolves cross-task/ownership verification. Controller fresh23/23 focused and1/1 actual full-chain DB passed; owned-copy full build/typecheck exit0 at final app hashes. Advisors0ERROR/12existingQTO WARN/266INFO. Post-review HEAD/index unchanged and original4173 HTTP200; accepted Task1 core/control/native worker hashes unchanged.
- Task 3: complete (no commits; initial task review + clean scoped fix1 re-review). Fix round1/5:1Important addressed,0open, no new breakage; deferred coverage Minor retained for final review. Exact publisher0983a6…612e3/test9f0b53…04791; native helper workercdcbc6…606b. Controller fresh65/65 and full owned-copy build/typecheck exit0; initial unchanged-context116/116 evidence separately recorded. task-3-fix1-review.md resolves cross-task limitations.
- Task 4: complete (no commits; exact8baseline+10new-file delta; independent Spec compliant/Task quality Approved). /root/r2_restore_acceptance found noCritical/Important; sole package-modeMinor withdrawn after exact HEAD/index100755 evidence proved baseline-copy metadata artifact. No fix round. Controller fresh37/37/0skip plus full owned typegen/tsc/build0 and all18hashesverified; implementer221/221+formatGREEN. task-4-review.md resolves each CannotVerify item, retains real Task5/recipient/performance gates. Original preview/build untouched.
- Task 5: implementing actual acceptance in /root/resave_publication_handoff_fix_review (existing sol/high capacity slot; no concurrent implementation). Exact four source files frozen/synced to owned copy; spec final4ff13304…b259e after pre-run cancellation-order correction. Main fresh harness27/27/0skip and full owned typegen/tsc/build0; logs controller-task-5-initial-harness.log/build.log. Actual marked Auth/Storage/browser/resave run authorized to begin; no actual pass yet. Fresh public-source source-free regression writer remains /private/tmp/1hk-resave-acceptance-writer-YiAOMM SHAd25ca572…bd6e. Original4173 and separate owned PG remain off limits to this runner.

Tasks1–4 artifact/transport, durable DB authority, controlled publication and product/CLI wiring are accepted; Task5actualacceptance remains. Design/plan alone are not completion evidence.

## Controller baseline verification

- Existing protocol + source-free worker: NODE_OPTIONS=--no-experimental-webstorage node --test tests/drawing-native-dwg-resave-protocol.test.mjs tests/drawing-native-dwg-worker.test.mjs →29pass/0fail/0skip.
- Owned PG17.6 container5acd32bf651a5f784c4737f8215dfb7c402b3207f83f50518f2fd0042b7e1f90, name1hk-resave-publication-pg-20260906-uu7wex, labelcom.1hk.task=resave-publication-uu7wex. Cached image99b1729a…3459, loopback32780, tmpfs/var/lib/postgresql/data confirmed.
- Initial baseline failed42501 on existing collaboration-role grant: owned bootstrap membership lacked ADMIN. Read exact failing migration20260901110735 and pg_auth_members; supabase_admin granted only this owned role membership ADMIN while retaining NOINHERIT and SET. No production schema/test changed. Existing corruption-fixture SET session_replication_role grant retained for owned postgres.
- Re-run M1_REAL_POSTGRES_REQUIRED=1 node --test tests/drawing-workspace-m1-real-database.test.mjs with owned loopback URL →1pass/0fail/0skip,9553ms. Includes prior real imported-resave/control barriers. Existing migration NOTICE messages (identifier truncation/absent-policy guards) and expected negative-auth log remain; not pristine-output claim. PGOPTIONS did not suppress postgres.js notices.
- Installed CLI /opt/homebrew/bin/supabase2.114.0 help, migration new help, db advisors help read. No remote/linked CLI calls.
- Owned build copy realpaths for node_modules/build/.react-router are all inside /Users/h/1hk-r2-restore-uvc74K/platform and no .env files exist; safe to use after exact source sync. Original4173 HTTP200 and index/HEAD unchanged after baseline.

## Task2 handoff preparation

Public task-2-brief.md and exact three pre-task snapshots are saved. Implementation is not dispatched until Task1 passes its independent review. Latest relevant retention definitions are in20260905185231_drawing_native_dwg_export_jobs.sql: private.lukas_qto_project_retention_storage_files, private.lukas_qto_project_retention_dependencies, public.lukas_qto_purge_project, public.lukas_qto_finalize_project_purge. Import20260905234036 adds no newer retention override. Sourcefree managed prefix is native-dwg; imported resave must reserve native-dwg-resave independently.

Ruling: extract the existing actual approved clone/edit setup into one test-only helper shared by old/new DB proofs — avoids duplicating approval setup while preserving prior assertions — costs a narrow fixture refactor requiring its existing real DB regression.

Ruling: remove obsolete32779 literal only from advisors opt-in validation, retain explicit loopback/nonempty port and generated m1_ database checks plus controller-owned runtime verification — current owned port is32780 — costs trusting the explicitly supplied local disposable URL rather than a stale numeric port.

Ruling: treat any exception after transport invocation and all unknown backend/proxy responses as uncertain; initially only fully consumed401/InvalidJWT is a confirmed non-duplicate HTTP rejection — official Storage error docs identify it as authentication refusal, unlike500/503/timeouts — costs more conservative manual reconciliation for other non-writing errors until their producer semantics are proved. Both Important review findings enter fix round1; no generic5xx or synchronous throw is evidence of no-write.

Task1: fix round1/5 (2 addressed,0 open; no commits). Re-review task-1-fix1-review.md accepted. Controller verified all five source bytes equal owned build copy after final sync. Current hashes: core1cbe7ec8…; transporta517c869…; fixture7eba51a7…; artifact testsf7db7939…; Storage tests1d0d7cdc…. Full hashes in implementer report/fix appendix. Entire90-test covering run21997ms,0fail/0skip; npm run build includes typegen+tsc and exits0. No original preview/build/deployment change.

Ruling: when Task4 adds the actual browser consumer, extract imported public DTO schemas into one browser-safe contract module and re-export from existing server APIs — current server files cannot be imported by React and duplicate handwritten status/receipt checks would drift — costs a narrowly tested schema relocation; source-free control stays unchanged. Task4 helper names use existing drawingNativeDwg prefix. No extraction is performed during Task2.

Ruling: permit a narrow export of the existing bounded actor-verification helper alongside the authenticated job RPC helper for Task2 artifact adapters — reuse the existing30s auth/deadline implementation instead of duplicating it — costs one internal export whose signature/behavior must be covered by the adapter review; no authority semantics or browser exposure changes.

Task3 public brief and exact pre-task native worker baseline prepared, worker SHA25695c99102ed97c287594bd22a0662bb2d37f27ca28f2a40bc6aa8f9a5f6302be8. Not dispatched; Task2 must pass its independent review first. Native-only API and existing cleanup semantics remain the regression authority.

Ruling: narrowly correct the existing source-free real retention fixture's expiry seed from clock_timestamp()-1s to now()-1s — accepted purge semantics compare transaction now(), so a transaction lasting over1s can stop before the intended protected-dependency assertion — costs one additional test-fixture review file, not any production retention change. Agent reported actual retention_not_expired versus expected protected_dependencies at tests/fixtures/drawing-native-dwg-jobs-database.mjs:855; controller independently read seed841 and both accepted/new purge comparisons. Numeric transaction age was not logged and is not claimed. Exact pre-fix source is baseline/task-2-drawing-native-dwg-jobs-database.mjs. This removes the mismatched clock basis instead of relying on retries-until-green.

Task2 additional jobs negative-test baseline recovered from owned pre-task copy, SHA256a323adea06176d96ca3f9028b3fedff951650b329e601c075c468f3c30c3fe5c; controller confirmed byte equality after reversing only the two implementer-reported test edits. No HEAD-based reconstruction.

Ruling: set sslmode=disable only in the opt-in advisors URL after explicit loopback/nonempty-port/generated m1_ database validation — CLI2.114 defaults to TLS while the owned disposable PG has no SSL — costs test-only plain transport on127.0.0.1, no production client or remote connection change. Initial CLI LegacyDbConnectError said server does not support SSL; its generic Connecting to remote database label does not describe the validated loopback target. No advisory pass is claimed before a successful rerun.

Controller Task2 verification: exact final application jobs SHA1c11d4cd…4fdc8 and artifact-jobs cdf5df3b…2cea9 synchronized to owned copy, full build/typegen/tsc exit0 (controller-task-2-build.log). Fresh focused23pass/0fail/0skip1782ms (controller-task-2-tests.log) and full-chain real DB1pass/0fail/0skip10839ms/exit0 (controller-task-2-database.log). Docker label/runtime confirmed and docker port reported127.0.0.1:32780. Existing PostgreSQL notices and approved-source diagnostic logs remain; no pristine-output claim. Initial tool result bookkeeping hit an undefined serialization error after focused process completed; full focused stdout was already saved and shows23/23, no test interruption. No original preview/index/HEAD mutation; actual native/Storage/browser publication remains Tasks3–5.

Task2: minor (deferred): existing large chunks, ReactRouter future flags, mixed BOQ import, unsigned theme-cookie build warnings and12existingQTO advisor warnings. Nonblocking Task2 review finding; final whole-unit review must triage it. No new resave ERROR/WARN/unindexed-FK advisor result, no Critical/Important parked and no fix loop required.

Ruling: Task4 will make only the two imported job-error constructors strip-only-compatible using equivalent explicit fields/assignments, and add the matching direct-Node package script — controller's actual Node26.5 module import failed ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX at resave jobs constructor, and the alternate transform-types flag is unavailable — costs one narrow source-free syntax-only review file plus existing error regressions, no dependency or behavior change. Actual subprocess CLI test is required; Vite SSR alone would miss this startup failure. No code changed during preflight.

Ruling: Task4 worker loop waits its configured abortable interval after every worker turn, including an immediate unknown claim/refusal — existing import CLI waits only on idle, which could spin on immediate resave control failures — costs up to the default1second between queued jobs, avoiding an additional backoff/framework. Worker-once config/dependency/result and shutdown-during-claim semantics are now explicit in the plan.

Controller initial Task3 evidence: same three final implementation hashes as package synchronized to owned copy; fresh full build/typecheck exit0 and covering116/116/0skip22123ms exit0. Logs controller-task-3-initial-build.log and controller-task-3-initial-tests.log; existing build warnings remain. Independent reviewer /root/resave_publication_worker_review is still running and has identified a final shutdown/publish handoff race; initial tests are not Task3 acceptance. Await complete finding list before one scoped fix dispatch.

Task3: minor (deferred): pending-poll await during an unfinished read/POST, readback refusal and first-refusal competition need stronger controlled-promise regression coverage. Reviewer sees existing production logic as correct; final whole-unit review must triage this explicit coverage gap. This Minor does not expand Task3 fix loop.

Controller fresh Task3 fix1: exact two changed files synced to owned copy; full build/typecheck exit0 and covering65/65/0skip21983ms exit0. Logs controller-task-3-fix1-build.log and controller-task-3-fix1-tests.log. Existing build warnings remain, tests show no new warning. Scoped re-review /root/resave_publication_handoff_fix_review still pending; no Task4 dispatch yet.

Task5 preparation: controller fully read existing owned-runner authority/cleanup implementation and verification/browser skills. agent-browser executable/connector unavailable; existing Playwright browser/screenshot fallback preserves no-new-dependency rule. No new app server started. All19native public source/lock/notice bytes in owned tool copy match authoritative source; nonincremental .NET build and fresh publish exit0. Initial writer hash check used /tmp alias and was correctly refused by accepted physical-path guard; realpath confirmed /private/tmp then hash succeeded. This is an owned config correction, not production guard relaxation. Exact3Task5baselines were written/rehashed. Separate read-only native seam map confirmed actual LINE baseline[0,0,0]→[100,0,0], unitfactor1/noYflip; Task5 literal endpoint edit preserves start and targets[120,21,0]. Current viewer read/download authority is retained; deny viewer geometry/approval/directStorage, not approved route downloads. Brief clarifies real collaboration review freeze handshake.

Task4 implementation update (not final acceptance): initial resource/download5tests, public DTO regression20tests, actual strip-only CLI idle/SIGTERM and config/shutdown-claim tests reported GREEN. React tests still being completed, broad regression/report and independent review pending. Controller corrected one stale brief symbol suffix Rpc5s to actual callNativeDrawingDwgResaveAttemptRpc (existing5s helper), no production API change.

Ruling: reuse a completed independent read-only reviewer when the collaboration harness refuses a fresh agent at its four-thread limit — no close/remove/new-model option exists on followup_task — costs residual old context, mitigated by an explicit new isolated brief/report/diff-only review scope. Do not reuse the implementer as its own reviewer or create a user-visible task to bypass the limit. Fresh attempts must not become repeated failed dispatches.

Controller Task4 owned build includes new route typegen and tsc and passed0; prior authoritative noEmit missing-generated-type messages are resolved in the owned build. Existing large chunks/ReactRouter flags/mixed BOQ import/unsigned-theme warnings remain, recorded as existing validation noise. Task5 pre-sync copied accepted publication migration+fourDBharness/fixture deltas into owned copy; all5hashes equal source. A large JSON read truncated during the last copy, before patching that file; controller used its exact two-hunk diff then independently verified SHA8263da20…0810. No authoritative file changed during this copy.

Read-only futureR5map completed by /root/resave_product_wiring afterTask4 acceptance, saved next-r5-read-only-map.md. Four native templates/24symbols already present. Missing cross-output exact-approved DWG/PDF/BOQ package authority identified; current browser-submitted PDF audit and PGRST202 legacy fallback are not strict rendered-content proof. Do not redispatch the map or implement R5 before Task5/final unit review. Actual3users/recipientCAD qualification remains explicitly separate.

Controller Task5 pre-run sync detected spec hash drift before copying; implementer clarified final self-review moved finite cancellation after completed-job relogin so latest-status assertion is meaningful, added explicit role/storage guards and formatted. Final spec4ff13304aed488df39d30397a92f44ad1c1afd3b79b1efc1a0f7ca2e689b259e and other3reported hashes independently matched owned source. No intermediate spec was copied or run. Main owned build resolved expected generated Task4 types; existing warning set unchanged. No authoritative build output/index/HEAD mutation.
