# Task5 brief — actual imported DWG publication acceptance

Read only this brief, sibling task-5-preflight.md and the accepted Task4 report after controller dispatch. Task4 must have passed its independent review before this implementation starts. Overall goal remains active; this unit does not qualify professional DWG delivery or finish R5.

## Authority and ownership

- Root /Users/h/Documents/GoAgent/.worktrees/universal-workspace-m1; app platform. Existing linked branch codex/universal-workspace-m1, HEAD9f5f56d93db325ff935772252f9d4fb64d69f98c. Exact pre-task baseline copies are in task-5-preflight.md. Verify them first, preserve all unrelated dirty work.
- No stage/commit/merge/push/deploy, paid/license contract, remote/customer mutations, new dependencies/lock changes. No private .superpowers/sdd artifacts/scripts or historical private binaries. Public evidence and fresh public-source builds only.
- Original4173 preview and authoritative platform/build/.react-router are off limits. Actual builds/servers run only from owned /Users/h/1hk-r2-restore-uvc74K/platform with physically owned dependencies/build output, no .env files. Controller synchronizes accepted source there before your run. Set NODE_OPTIONS=--no-experimental-webstorage.
- You own only Task5 runner profile/tests/e2e/deployment guidance/public evidence. Do not alter accepted SQL, publisher, compiler, Storage, UI or server routes to make the story pass. Stop at the first real broken application boundary and report a concrete failing request/expected behavior/owned evidence to controller; preserve runtime evidence for a scoped fix. Do not skip a mandatory assertion or forge a response.
- No subagents or independent review dispatch; controller handles exact-delta review. Use TDD/Ponytail and existing verification/browser skills. Public baselines/brief/report replace private skill scripts and no-commit standing instruction overrides generic commit guidance. No repeated user approval questions.

## Files

- Modify platform/scripts/run-drawing-workspace-m1-e2e.mjs: exact new --profile=dwg-resave selector and only required owned runtime wiring. Preserve m1/p3/dwg-source, marker/status/loopback, process/cleanup/build/real-DB gates. Do not create an alternative unguarded runner.
- Modify platform/tests/drawing-workspace-m1-release-harness.test.mjs: RED/GREEN tests for exact new profile/target and existing profile/env authority preservation.
- Create platform/e2e/drawing-native-dwg-resave.spec.ts. If the story needs a focused helper, a single platform/e2e/utils/drawing-native-dwg-resave-evidence.ts is allowed; avoid copying the entire large source-free helper. Reuse existing fixtures/auth/cleanup/import/worker APIs directly.
- Modify platform/DEPLOYMENT.md by appending a focused imported-resave worker section; preserve existing unrelated guidance.
- Public report/evidence under docs/superpowers/evidence/2026-09-06-native-dwg-resave-publication/. No generated credentials/cookies/service keys, raw SQL/native paths/errors in saved browser evidence. Sanitize any traces before linking public evidence; do not embed secrets in screenshots/logs.
- Any additional existing file needs controller notice and exact baseline before editing. No package.json script addition is required; invoke node scripts/run-drawing-workspace-m1-e2e.mjs --profile=dwg-resave with existing Node.

## Required story: real success boundaries

The one-sentence story is: a real authenticated owner uploads the exact public DWG, imports its verified entities into a blank draft, edits one native LINE, completes separate review/approval, requests the exact approved resave in the workspace dialog, and receives four hash-verified downloads that remain available after relogin while the original stays unchanged.

1. Before any test mutation, verify marked disposable Supabase authority, immutable cached reader/resaver image and exact source10987bytes/AC1024/SHA256bcc54d3c768444c9ade41a23e4ef2b9f5b5668d9972522ee4a4adbc98c670434. The different13387byte source-ingestion fixture is not interchangeable. No arbitrary/invented claim or approved snapshot.
2. Use actual browser file upload through TUS→verification function→finalization, then read immutable file metadata/Storage bytes to verify original identity. Existing source-ingestion selectors/pattern are in preflight. Do not merely seed a lukas_qto_files row.
3. Open a real blank workspace and the production DWG import controls. Request analysis with existing HTTP action, run real native import worker using marked PostgREST/Storage + cached reader, then click the production canonical import/apply control. Wait for actual saved checkpoint/outbox/collaboration synchronization. Prove the server persisted5supported entity sources and the report's unsupported coverage; do not fake a prepared plan.
4. Edit only native LINE4A endpoint from[100,0,0] to[120,21,0], retaining start[0,0,0], using production drawing operations, preferably actual inspector/canvas UI where it exposes this exact geometry. If exact endpoint is not editable by existing UI, use authenticated production operation API with canonical version checks and label that seam honestly; do not seed edited snapshots or mutate approved rows. Preserve other entities/layers. Use real editor→reviewer→approver review/approval flow and derive exact six-field scope from approved snapshot, not a hand-written hash. With collaboration enabled, review must use workspace UI/action for the freeze handshake, not direct non-collaboration request_review RPC.
5. Open actual workspace export dialog and choose DWG. Assert exactly the imported-resave control appears (no source-free create control), it is ready only after saved approved identity is available, and opening it does not POST. Click resave; prove202 and strict intent+six-field scope+requestId, no browser actor/image/edit/lease/path. Capture real queued status.
6. Use Task4 actual runNativeDrawingDwgResaveWorkerOnce(config,dependencies) against real service client, real source transport and imported Storage profile, with no overridden resave function on the main success path. Source/native execution must be the actual pinned sandbox. Task4 subprocess tests separately establish executable startup/shutdown; additionally invoking its CLI as a child on this real queue is welcome if it preserves deterministic ownership and safe cleanup. Do not auto-run two consumers that make receipt/claim ownership indeterminate.
7. Assert strict completed {job,receipt}, four ordered kinds dwg/edit_request/authority/report, qualification experimental-unqualified, persistenceAuthority not-issued. Download all four through authenticated browser resource links, verify response headers/private,no-store/no-referrer/nosniff, exact receipt byte sizes/hashes and fixed kinds. Never use direct service Storage as the user's download proof.
8. Decode returned resaved DWG using actual isolated reader, assert literal LINE endpoint, unchanged other supported entities/layers, preserved original unsupported identity/coverage. Native report is supported-fields-only/independentCad not-performed; do not overstate full unsupported payload preservation. Whole binary can vary by timestamp across separate native runs; compare each run with its own receipt and semantic readback.
9. Logout/relogin as same user, reopen actual dialog and restore latest completed status/four downloads without auto-POST/new job. Re-download/hash at least DWG through new authenticated session; original Storage bytes and source metadata SHA remain exactly unchanged. Check page errors at each key boundary and save screenshots of meaningful imported-ready and completed states.

## Required separate failure/permission stories

- Cancellation uses real admission/cancel/status authority and a deterministic controlled external boundary when needed. In-flight native/Storage must settle before cancellation is acknowledged; cancelled task exposes no completed artifact receipt. Any controlled promise/injected failed transport is explicitly finite, never presented as the success-path native proof.
- Retry covers an intentionally lost/failed status or admission reply with real server state, same local request ID/no duplicate job, then real worker/native/Storage completion. Explicit new request after terminal cancelled/failed is user-triggered and distinct. No arbitrary sleep-based SQL ordering.
- Viewer must not mutate geometry/approval, outsider must not see scoped status/receipt/download, and fresh revoked membership must deny a formerly authorized download. Accepted SQL allows current Viewer to read approved receipt/download: do not change that into a blanket Viewer export denial. Other collaborators cannot cancel someone else's task unless accepted admin capability allows it; UI only offers own request.
- Direct authenticated/anon managed Storage prefix read/upload/update/delete remains denied, even though approved download route is allowed. Verify exact resource authority and no descriptor/storage locator/lease/service-key leakage to browser; UI downloads supply kind/job/scope only.
- Finite downloader late-revocation/stalled-body behavior is already covered in Task4. Use actual role removal before fresh request for integrated revocation, or a clearly labelled exact transport barrier if necessary; do not introduce a production test backdoor.
- Keep source-free M1/browser and R2 blank/template create/save/restore/permission regression gates. Existing full M1 scenario requires fresh public-source .NET writer described in preflight; no old private DLL. Do not silently skip native source-free test due missing config.

## Runtime/testing/report

- Start from runner profile unit RED (missing selector/target), implement narrowly and run GREEN plus existing harness tests. Main actual run uses owned copy and marked runtime only. Before actual app actions, perform browser screenshot/UI availability check using installed Playwright (agent-browser fallback is recorded in preflight). No browser dependency install.
- Existing runner starts an entirely new isolated Supabase stack and automatically cleans only its marked resources after each command. It does not use the controller standalone PG. Match exact environment to runner status; do not print secret status output. Keep runtime label/project/path/ports without keys in evidence. Stop at first broken boundary instead of running the same failing check repeatedly.
- Existing runner local Storage bucket50MiB is sufficient for synthetic file; it is not200MiB performance evidence. Do not change its global limit. No pulls of mutable native image, no mounted source/output directories to native container.
- Run owned-copy app/build/typecheck, scoped new+old tests, real DB and actual full browser/native story. Record command exit/pass/fail/skip counts honestly; a test that did not run is unverified, not passed. Controller independently verifies final exact application delta and reviews the full unit later.
- Deployment guidance must list Task4 exact required env SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NATIVE_DWG_RESAVER_IMAGE_ID, NATIVE_DWG_DOCKER_PATH, NATIVE_DWG_DOCKER_HOST; optional NATIVE_DWG_RESAVE_LEASE_SECONDS default900/range180–900 and NATIVE_DWG_RESAVE_POLL_MILLISECONDS default1000/range100–60000; start:native-dwg-resave-worker. Web app also needs the same trusted image for new admission; deploying only web cannot drain queue. Never include concrete service keys.
- Explain uncertain POST/upload sessions require operational reconciliation, not expiry-based automatic close/retry or forced publication. Distinguish publication_uncertain/settlement_uncertain and actual receipt checking. Do not supply an unsafe force-close one-liner. Current results are experimental; professional DWG/font/Xref/layout/recipient qualification and R5 exact-approved packages stay open. This task performs no deployment.
- Write task-5-report.md with exact changed files/hashes, RED/GREEN, actual pipeline evidence/screenshots/hash links, cleanup outcomes and remaining limits. Return concise DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED with report path. On a real app failure, report exact narrow boundary to controller without modifying accepted app code.

Controller final whole-unit review must triage both ledgered Minors: existing unrelated build/advisor warnings and stronger pending-poll/readback/first-refusal controlled-promise coverage. They are not silently closed by Task5 success. No overall goal completion claim until R2/R4/R5 acceptance truly finishes.
