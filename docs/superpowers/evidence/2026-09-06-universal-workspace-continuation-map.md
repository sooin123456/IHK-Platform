# Universal Workspace — active goal continuation map

This is a code/evidence map, not a launch claim. The full goal remains active. Current implementation checkout: `/Users/h/Documents/GoAgent/.worktrees/universal-workspace-m1`, branch `codex/universal-workspace-m1`, HEAD `9f5f56d93db325ff935772252f9d4fb64d69f98c`, with pre-existing uncommitted changes.

## Verified scope versus required end state

| Requirement | Existing implementation/evidence | Remaining acceptance |
| --- | --- | --- |
| R1 simple navigation | One active space/menu and membership-scoped server loading; latest copied authenticated23/23 passes; bounded shell/fixture/keyboard correction now built27/27 plus6repeat and172Node passes | Real-user usability; broader performance/accessibility qualification |
| R2 upload-free start | Strict retry-stable personal blank/template/file entry; canonical first-state persistence/durable admission and automatic retry accepted; lifecycle-generation UI fix, actual logout/full work+binding restoration and independently expected offline geometry now pass in fresh authenticated23/23 | Live-room transition durability beyond current workflow assertions and legacy divergent-cache recovery; exact actor correlation for remaining authorization diagnostics |
| R3 task modes | 작성 / 검토 / 수량·금액 presentations over one editor and shared state | Real-user workflow from authoring to approval/export |
| R4 native DWG | Immutable ingestion/canonical anchors, five-type selected compiler, approved-source bridge and isolated native protocol; exact resave attestation + immutable jobs/live control/cancellation-aware native execution now independently accepted | Immutable artifacts/publication, user request/status/cancel/download UI; actual imported-resave Auth/Storage/browser path; independent CAD/corpus qualification |
| R5 templates and handoff | Four editable native templates and 24 geometry symbols; immutable catalog, SQL clone and native source-free export; copied existing authenticated acceptance passed | Content rights confirmation; one exact-approved-revision DWG/PDF/BOQ package; permitted dependency/compatibility/change documentation; recipient acceptance |

The [approved-source evidence](2026-09-06-native-dwg-approved-resave-source.md) records exact code/source hashes and separate source-bridge versus copied-runtime verification. The first2 runtime attempts failed before tests because this Docker daemon could not bind `/tmp` files; the byte-identical `/Users` copy passed. Live port4173/PID80284 remained unchanged and HTTP200; do not rebuild its checkout casually.

The passing browser suite is not a clean-log qualification. The subsequent [reconnect diagnosis](2026-09-06-collaboration-reconnect-diagnosis.md) identifies a bootstrap-persistence/whole-Y.Doc replay defect that can reject honest reconnects. Preserve the protected-state guard; fix bootstrap CRDT provenance or channel design with an honest-replay regression and forged-update counterexamples. Exact bytes/actors for each of the nine closures remain absent. The deeper helper trace also corrects the initial report: scenario23 DOES temporarily revoke the Viewer, close its page, then restore membership, strongly explaining the awareness P3A01 (actor-correlated log proof still absent). One editor .data abort remains unclassified. Do not silently allowlist it; add the missing editor request-failure assertion and phase-aware evidence. This is a required follow-up fix alongside remaining DWG integration, not a completed correction.

The earlier production initializer/guard diagnostic reproduced the honest-replay rejection in memory: two logically equal independent bootstraps rejected a363-byte old-state replay, while restoring original CRDT identities accepted it and still rejected forgery. The subsequent [canonical initialization unit](2026-09-06-collaboration-canonical-initialization.md) now implements and verifies atomic insert-or-return-winner across all three loaders. Losing candidates never use ordinary conflict-merge storage. Authorized Viewer serialization, including approved absent state, is permitted without ordinary write authority; absent service initialization remains draft-only. Existing rows and leases are preserved.

The [R2 audit](2026-09-06-native-dwg-resave-protocol/r2-end-state-audit.md) originally found fresh-context authentication but no actual logout, and an offline geometry oracle derived from the already-mutated database. The latest [lifecycle and restore unit](2026-09-06-workspace-lifecycle-restore.md) resolves those coverage gaps in the existing scenarios: actual logout/denied access/same-user confirmation/organization selection/Recent Drawing restore, complete fixed W/F/D properties and saved binding/result identity, plus independently calculated offline geometry/versions/forward/ack on both clients before and after online reload. Offline shell navigation/reload while disconnected remains explicitly outside the M1 contract; do not expand that requirement. The earlier [bootstrap contract preparation](2026-09-06-native-dwg-resave-protocol/collaboration-bootstrap-contract.md) is historical context, superseded for implemented scope by the canonical initialization spec/evidence.

The [freeze identity unit](2026-09-06-collaboration-freeze-identity.md) is implemented and independently accepted: one scalar-aware helper across seven projections, exact byte/vector/event stability, focused29/29 and final140/140+legacy17/17 with collaboration tsc0. No new dependencies, SQL, deployment, or preview rebuild in that unit. Do not redispatch this no-op assignment fix. It did **not by itself** complete canonical initialization or real transition durability. The [follow-up contract findings](2026-09-06-collaboration-provenance-followup.md) identified all three absent-state loaders and the additional initial-admission counterexample: beforeSync type0/read-only bypass could deliver locally projected lease identities before writer closure. Latest read_freeze remains `20260905131704_drawing_released_freeze_recovery.sql`. The accepted canonical unit now preserves approved read-only active/null initialization, allows insert-only origin during lease-only preparation without changing the lease, and refuses nondurable initial projections. Ordinary store lease fencing and all protected-state/approval guards remain in place. These initial-state contracts are resolved; live-room transition durability is still separate.

The canonical initialization unit also fixes actual wire retry classification (`drawing-reconciling`) and destroys refused Hocuspocus payloads. Its real WebSocket test proves explicit `sendToken()` followed by `startSync()` after lease release. Final verification for that unit was157collaboration+17legacy+1actualPostgreSQL=175 distinct tests, tsc0 and local security advisorsresults[]. Its actual database/socket fixture uses fixed token identity mapping, not real Auth/JWKS/IndexedDB. Do not redispatch that unit's Tasks1–3.

The subsequent [application retry unit](2026-09-06-collaboration-application-retry.md) consumes that reason automatically, keeps the caller's Y.Doc and pending edits, gates connected status on authenticated sync, distinguishes access refusal, and cancels disposed retries. Actual production-server lease release recovers the exact winner without manual token/sync calls.166regression+17legacy=183 distinct tests and both application/collaboration typechecks pass; independent review accepted. PostgreSQL/Auth/IndexedDB/full browser were not rerun in that client-only unit. Do not redispatch it.

The newer [lifecycle/restore unit](2026-09-06-workspace-lifecycle-restore.md) scopes UI state to a monotonic lifecycle generation, including A→B→A and obsolete callback refusal, with deferred-provider browser RED/GREEN and the existing local-edit regression passing. Independent review accepted;159 runtime/shell/realtime/structure/collaboration checks pass. Final copied M1 runner has23/23 authenticated browser passes,5 real-database gate passes with2 configuration sentinels skipped, and a fresh locked native writer publication. This reruns actual Auth/IndexedDB/browser and source-free DWG receipts/downloads. The final log has no protected-state rewrite diagnostic; one awareness P3A01 remains during the explicit native Viewer-revocation scenario without exact server actor correlation. Do not claim legacy-cache or arbitrary live-transition durability solely from these workflow assertions, and do not clear user drafts.

The old preview failures are now corrected by the [shell/fixture/keyboard unit](2026-09-06-workspace-shell-polish.md): actual243-vs-239-pixel scale-control overflow, clipped split IFC height, portable guarded current-PDF resource, awareness fixture routing and stale test contracts. Repeated mounted testing additionally proved a real Radix/global-shortcut leak (opening its menu moved a selected wall700→701); defaultPrevented and menu ownership guards now preserve exact selection/geometry. Final matching-source build/typecheck passes,172Node tests pass, and built Chromium shell18+targetedPDF/IFC8+P6preview1=27pass, with both menu viewports repeated3times=6pass. Independent review accepted. Auth/PG/M1 were not rerun in this unit; synthetic checkpoint/ACK reload semantics are not general persistence acceptance. Historical P5 release/performance evidence has not been regenerated against the new synthetic PDF. Do not redispatch the bounded shell corrections. Original live4173/PID80284 still serves its existing build, not the latest source; no deployment occurred.

## Next R4 dependency chain

1. **Approved source bridge (complete unit; do not redispatch):** scope-only authenticated read RPC → exact approved snapshot → immutable analyzed report and verified upload → clone-safe handle/layer mapping → existing strict selected-edit compiler. Spec: `../specs/2026-09-06-native-dwg-approved-resave-source-design.md`. Existing source/free compiler unchanged. Original IDs are exact; only differing revisions normalize clone IDs.
2. **Isolated native resave protocol (complete unit; do not redispatch):** exact `resave-native-stdio` framed input/output and immutable image profile; strict C# selected writer/inventory/readback, preserved default VIEWPORT, shared owned-container lifecycle and2GiB resaver/1GiB reader.41native selftests,186host/adapter regressions, currentactualresaver3/3+reader5/5 andtypecheck pass; both task gates and finalreview accepted. Public report has source/request/output hashes and explicit experimental/no-authority status. See the [unit evidence](2026-09-06-native-dwg-resave-protocol.md); do not reimplement the command or duplicate the sandbox lifecycle.
3. **Immutable resave jobs/control (complete unit; do not redispatch):** separate imported-resave namespace, scope-only request+request ID, exact server attestation, live authority/source recheck and three-attempt leases, cancellation-aware native execution with explicit cleanup proof. [Unit evidence](2026-09-06-native-dwg-resave-control.md), [binding design](../specs/2026-09-06-native-dwg-resave-control-design.md), [completed plan](../plans/2026-09-06-native-dwg-resave-control.md), [ledger](2026-09-06-native-dwg-resave-control/progress.md). All three task gates and whole-unit review accepted; final test-only UUID correction also independently accepted. Controller275focused+1realPG+1actualnative pass, final covering82/82 overlap rather than add to totals, application/native-worker typechecks and owned-copy build0. Actual native cancellation/cleanup uses finite service control, separate from actualPG races; no Auth/Storage/browser/publication claim. Existing12 unrelated DB advisor warnings and build warnings remain documented. No scheduling/UI or completed receipt is wired. Original4173/PID80284 remains unchanged/HTTP200; disposable control-unit PostgreSQL container removed. Next unit starts with artifact publication, not reimplementation of request/control/native execution.
4. **Artifact and browser handoff (next):** `{dwg, edit_request, authority, report}` content-addressed attempt artifacts, no-overwrite uploads, exact readback, closed upload before publication, receipt-only hash-verified downloads with final authorization recheck. Real authenticated upload/import/edit/approve/request/worker/relogin/download test, plus cancellation and retry. [Read-only reuse map](2026-09-06-native-dwg-resave-control/next-publication-seams.md) records existing source-free code/RPC reuse, imported caps/prefix/retention differences and the need to continue control across Storage settlement. It is preparation, not a binding artifact spec or implemented publication.
5. **Delivery qualification:** independent recipient CAD open/save/visual/plot checks; supported layout/fonts/Xrefs/types; unknown/proxy and nonzero polyline vertex-ID preservation strategy; representative licensed corpus and declared excluded cases. The current ACadSharp writer rejects known lossy cases and is not qualified for general production DWG delivery.

Avoid re-auditing the complete repository for each unit: the current approved-source spec and the following exact existing files are the continuation points.

The bounded [resave job follow-up map](2026-09-06-native-dwg-resave-job-followup.md) identifies exact source-authority/locator and actual cancellation-signal seams. It is read-only preparation, not a binding design or implemented job. Use it when writing the next control-plane spec; keep source-free eligibility and public browser-safe authority payloads unchanged.

## Reuse points

- `platform/app/lukas/lib/drawing-native-dwg-selected-edits.server.ts`: existing five-type strict compiler; `request:null` on no-op; experimental/no persistence authority.
- `platform/supabase/migrations/20260906011309_drawing_native_dwg_canonical_import.sql`: `private.lukas_drawing_dwg_source_report_matches` and clone-preserved DWG source payloads.
- `platform/app/lukas/lib/drawing-authority-snapshot.server.ts`: canonical authority hydration.
- `platform/app/lukas/lib/drawing-native-dwg-import-worker.server.ts`: immutable source checks, bounded phase execution and uncertain publication outcome.
- `platform/app/lukas/lib/drawing-native-dwg-sandbox.server.ts`: fixed image/profile, process confinement and cleanup patterns.
- `platform/app/lukas/lib/drawing-native-dwg-resave-protocol.server.ts`: strict experimental resave frame/report validation; consumes exact compiled request bytes and immutable original source, not authorization or persistence authority.
- `platform/app/lukas/lib/drawing-native-dwg-resave-worker.server.ts`: cancellation-aware claimed attempt with exact attestation rebuild, separate bounded control, cleanup-evidence settlement gates and internal prepared output; publication is the next integration unit.
- `platform/native-dwg-worker/src/import-supabase.ts`: bounded verified source Storage download.
- `platform/app/lukas/lib/drawing-native-dwg-worker.server.ts`: staged immutable artifact/readback/publication lifecycle patterns; existing source-free contract stays unchanged.
- `platform/app/lukas/lib/drawing-native-dwg-download.server.ts`: final download authorization and hash verification.
- `platform/tests/fixtures/drawing-native-dwg-canonical-import-database.mjs`: actual import attestation, canonical operations, approval and clone fixture.
- `platform/scripts/run-drawing-workspace-m1-e2e.mjs`: disposable real Auth/PostgREST/Storage/browser story runner; builds output, so use an owned copy when preview is live.

## Decisions requiring external evidence, not invention

Commercial prices, customer acquisition, billing and plan entitlements remain hypotheses in the approved business document. They are not implemented from assumed revenue. Native engine commercial contracts, template licensing assertions, real customers and recipient CAD acceptance cannot be fabricated by passing synthetic tests. These items stay visible without blocking safe local implementation of the remaining technical workflow.
