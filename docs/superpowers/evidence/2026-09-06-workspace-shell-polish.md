# Workspace shell, preview source and keyboard correction

Bounded implementation and verification on 2026-09-06. This supersedes the unresolved shell status in [the earlier diagnosis](2026-09-06-workspace-shell-followup.md), not the remaining Universal Workspace goal. Imported DWG job/delivery integration and remaining R2/R5 acceptance are still required.

## Actual production changes

- The scale input and its flex label can shrink to the available width. The original strict assertion observed `scrollWidth=243` against `clientWidth=239`; the fix uses `min-w-0` and `w-full`, not clipping or an increased tolerance.
- The split IFC column allocates the remaining shell height to its compact viewer. Its model shrinks within that space while the element list and property section remain reachable by scrolling. Noncompact viewer sizing is preserved. Final direct browser inspection at 1280×720 measured the model at top413, bottom703, height290; there were no alert elements or browser error logs in that fresh inspection.
- Workspace shortcuts respect `event.defaultPrevented`, and menu roots/descendants own their keys. A deterministic mounted RED showed that simply opening the Radix architectural menu with ArrowDown changed a selected wall's two Y coordinates from700 to701. The final browser regression preserves its exact selected ID and all four coordinates through trigger ArrowDown, menu ArrowDown and Delete, then verifies Escape focus restoration. Normal canvas shortcut positive controls remain.
- The explicit `awarenessTest=1` preview selects its intended harness. `p5IfcTest=1` remains an explicit opt-in when that harness also needs the IFC fixture; ordinary preview still opens in2D.
- The development-only current PDF route now uses a tracked public synthetic fixture instead of a private generated-artifact dependency. Production and non-loopback requests still return404. Shell verification no longer intercepts this resource with predecessor bytes.

No dependency, database schema, persistence protocol, permission, approval or native engine implementation changed in this unit. Existing libraries/components were reused in accordance with ponytail's minimal-change guidance. Lifecycle-generation changes from the preceding unit were preserved.

## Portable PDF fixture

`platform/tests/fixtures/p5-current-revision.pdf` is self-authored synthetic technical geometry, generated by `platform/scripts/generate-p5-current-pdf.mjs` using the already installed pdf-lib. It is8289bytes with SHA-256 `298cdc57f86b73f96ad6c743e20d9fb76e08d9f8dc7b827b6558ff068f95c8db`. Its three pages match the tracked predecessor's A0/A0/A1 page sizes, but are not customer content or calibrated construction drawings. Fixed metadata and standard Helvetica make regeneration byte-deterministic; three generator runs matched. All three rendered pages were visually checked; the current page was also inspected in the mounted PDF/IFC split view.

Live preview descriptors, current P5/P6 tests and the P5 evidence generator now agree on those exact bytes. Tests compare the actual route response, public file and descriptors; check PDF metadata/page sizes, content type/no-store, differing predecessor identity and the production/non-loopback guards. Historical P4/P5 release evidence was deliberately not rewritten: it is historical and does not qualify the new PDF's performance. Rerun the release measurements before making a new release-gate claim.

Verification limitation: an initial test invoked the old authoritative PDF loader and thereby indirectly read its existing private dependency before failing on the missing new fixture. Those returned bytes were not inspected or reused. The corrected isolated RED used a disposable source root without that dependency and failed with ENOENT. The replacement fixture was independently authored. Do not invoke the old loader or treat that initial attempt as a clean isolated RED.

## Test-contract alignment, distinct from fixes

Existing tests were aligned with the already implemented mode/dock behavior: explicit split selection, authoring versus review tab order, Object-default inspector and explicit Result selection, disabled-to-enabled tools during recovery, the installed Radix trigger-derived menu name, and a single lazy-export click followed by its mounted dialog. Permission, immutable source/link checks and error assertions remain.

The former arbitrary canvas height greater than600px conflicted with the existing199px header/source controls at a720px viewport. Its replacement checks exact top/bottom/height agreement with the remaining shell within1px, width greater than900px and bottom no greater than720px. IFC property scrolling and return to a fully visible model are additionally verified. Tablet toolbar horizontal scrolling is intentional; document overflow remains strictly zero and the final tool must scroll/focus into view with its44px touch target.

The desktop and tablet architectural scenarios now use independent browser contexts. This alone did not fix the problem: the isolated repeat still failed1/6. The earlier carryover-only diagnosis was incomplete. The deterministic700→701 RED established the global key leak and justified the two production guards. No quarantine state or user draft is silently cleared, and no focus retry loop hides the bug.

## Final verification on matching source

All runtime code was copied into the owned `/Users/h/1hk-r2-restore-uvc74K/platform` checkout. [The13-file source manifest](2026-09-06-workspace-shell-polish/source-manifest.json) verifies authoritative/runtime byte equality and records exact pre-task hashes. [The task-only patch](2026-09-06-workspace-shell-polish/exact-task.patch) compares preserved pre-task copies rather than the repository's much larger dirty HEAD diff. The PDF is covered by its manifest hash, not a text patch.

| Check | Final result | Evidence |
| --- | --- | --- |
| Commands, geometry, shell and local preview Node tests |172passed,0failed/skipped | [reproducible final run](2026-09-06-workspace-shell-polish/node-reproducible-final.log) |
| Application type generation, TypeScript and production build |exit0 | [final build](2026-09-06-workspace-shell-polish/build-final.log) |
| Built Chromium shell18 + targeted PDF/IFC8 + P6 preview1 |27passed,0failed | [final built run](2026-09-06-workspace-shell-polish/shell-pdf-ifc-built-keyboard-final.log) |
| Built architectural menu, desktop/tablet,3 repetitions each |6passed,0failed | [final repeated run](2026-09-06-workspace-shell-polish/menu-key-leak-green-repeat.log) |
| Actual selected-wall key leak before fix |expected failure,700→701 | [mounted RED](2026-09-06-workspace-shell-polish/menu-key-leak-red.log) |

The final Node command, run from the owned platform copy:

```sh
NODE_OPTIONS=--no-experimental-webstorage node --test tests/drawing-workspace-commands.test.mjs tests/drawing-workspace-geometry.test.mjs tests/drawing-workspace-shell.test.mjs tests/local-drawing-workspace-preview.test.mjs
```

The built browser run uses a disposable config extending the existing Playwright config, with baseURL `http://127.0.0.1:5198`, webServer disabled, retries0 and workers1. The owned built server uses `react-router-serve ./build/server/index.js`, `HOST=127.0.0.1`, `PORT=5198`, development mode to permit the guarded fixture route, no Sentry deployment credentials, and dummy loopback Supabase configuration. The final command is:

```sh
NODE_OPTIONS=--no-experimental-webstorage ./node_modules/.bin/playwright test --config=playwright.r2-lifecycle.config.ts e2e/drawing-workspace-shell.spec.ts e2e/drawing-workspace-p5.spec.ts e2e/drawing-workspace-p6.spec.ts --project=chromium --grep 'drawing-workspace-shell.spec.ts|manual P5 PDF|active PDF page|non-current PDF mode|failed PDF capability|disabling PDF compare|mounted PDF inspector|mounted IFC viewer|cold split|P6 preview smoke' --reporter=line
```

The6repeat command selects only shell `architectural object` tests with `--repeat-each=3`. The final browser inspection was a fresh reload of that build, not the user's old4173 preview.

Earlier iteration logs are retained, clearly separate from final acceptance. The early26/26 run preceded the key-leak fix; repetition then exposed failures. An earlier broad selection accidentally included an unconfigured P6 Auth sentinel and also failed a stale Result-default expectation. Neither is silently counted as passing. The final positive selection is preview-only and excludes authenticated configuration sentinels; Firefox/WebKit were not available and are not qualified. Application build warnings include unsigned theme-cookie and bundle warnings; build success is not a clean security/performance audit.

Independent review accepted the task delta, the two P5 inspector corrections and the final keyboard correction with no remaining findings. The reviewer explicitly corrected the earlier carryover-only hypothesis after the fresh-context RED.

## Boundaries and continuation

- Actual Auth/PostgreSQL/M1 acceptance was not rerun in this UI/fixture unit. The preceding [lifecycle/restore unit](2026-09-06-workspace-lifecycle-restore.md) records its own23/23 authenticated and5database passes; do not relabel those as this unit's runs.
- The synthetic preview still uses a static checkpoint and deterministic fake operation acknowledgements. Arbitrary multi-operation/reload persistence and every live-room transition are not certified by these shell passes. Preserve user outboxes and the protected-state guard.
- No60fps, cold-start2.5s, p95500ms, production deployment, recipient CAD qualification or imported-DWG delivery claim is made.
- Next is the separate imported-DWG resave job/control plane, then immutable artifact publication and authorized download/browser handoff, then the exact-approved-revision DWG/PDF/BOQ package. Reuse the accepted compiler/protocol; do not redo them. See the [continuation map](2026-09-06-universal-workspace-continuation-map.md).

The branch remains `codex/universal-workspace-m1`, HEAD `9f5f56d93db325ff935772252f9d4fb64d69f98c`. The index SHA-256 remains `ab8778babeb2aa04f070a609cbd1240760b3158cb5aa1e6ced3da34bb9a9f84c`. No staging, commit, push, deployment, paid resource, remote data mutation or modification of the user's original drawing bytes occurred. The original4173 listener remains PID80284 and returns HTTP200; it was not rebuilt. Only the owned5198 verification server/tab were used for this unit.

After verification the owned5198 server exited and its temporary browser tab was closed. A fresh listener check found no5198 listener and the same4173/PID80284. Task-file `git diff --check` passed; the index hash remained unchanged.
