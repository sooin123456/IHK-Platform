# Frontend completion audit — current evidence

This audits the original **18 pages / 12 panels / 10 exception states / architecture, IFC and civil journeys**. The later 30-page product proposal is an expansion, not a substitute completion criterion. Backend persistence, actual AI, production DWG conversion and deployment are outside this frontend-only goal. Mock workflows must remain explicitly labelled.

Authoritative worktree: `/Users/h/Documents/GoAgent/.worktrees/universal-workspace-m1/platform`.
Preview: `http://127.0.0.1:4181/workspace-preview/flow`.

## Fresh checks in this audit

- Historical record fix: returned R1 no longer resolves to current approved R2. Frozen review/source/object detail, missing-revision refusal, reload and seven civil module record returns verified on desktop5419/mobile30288. Existing project record/submission search regression passed; model and typecheck94957 exit0. This verifies record navigation, not completion of all original-viewer/40-view requirements.

- Local project member plans56243/8005 exit0: normalized project-specific directory, reload/isolation, no sample-person leakage, readonly configuration, direct project-card entry/mobile. Sample member and organization settings regressions pass; project-management regression also passes. Typecheck56267 and model86128 pass. Named submission actor/assignee integration remains open; these are simulated plans, not real account access.

- Supporting-file UI85095 exit0: extension rejection, duplicate detection, draft/reload metadata, wrong-original rejection, exact download bytes, acceptance guard, correction/resubmission carryover, old-history preservation and mobile. Package/single-submittal regressions1719 pass,5 model tests097416 pass, typecheck82092 passes. No backend upload or security/content scan claim.

- Related-drawing submission package25378 exit0 verifies recipient-label/selection drafts, up to same-project candidate filtering, source return to primary submission, changed related-drawing acceptance guard, correction/resubmission with preserved snapshots, unrelated-project preservation and mobile. Existing lifecycle/document-switch/queue scripts also pass. Model71418 has3 passing tests; typecheck81851 passes. Named recipient is a label, not an authenticated assignee; arbitrary supporting-file upload remains open.

- Project management25412 exit0: rename/discipline, empty-name guard, reversible archive confirmation/cancel/reload, unchanged drawings, new-drawing guard and restore to renamed overview. Entry/template backup regressions pass. Typecheck/schema45276 passes. Archive is frontend list organization, not a server ACL or data deletion.

- Extended business-scope31669 exit0 verifies18 routes at1440/390 with no horizontal overflow, plus existing quantity approval/correction/stale-revision and submittal document-switch regressions. The narrower83777 entry below is superseded for viewport coverage.

- Business project scope83777 exit0 covers18 route consumers, field document switch/reload, isolated mutation with full store preservation, explicit all-project recovery and mismatched selected document. Typecheck35519 and existing actual-PDF civil handover/entry/project-backup regressions83578 pass. No claim of backend authorization or all return-path combinations.

- Project drawing/source lists44664 exit0: filters exclude other-project drawings and linked sources, preserve search/project on workspace return and reload, offer explicit all-documents recovery, and retain global100-file capacity. Mixed actual PDF import and existing local-document regressions pass. Unassigned import files remain an explicitly labelled common preparation pool; cross-project source cloning and other workflow-list scoping remain open.

- Entry/project overview14068 exit0: new-user home, returning-user projects, explicit home, opt-in examples, filtered project overview, workspace return/reload, invalid project recovery. Project-container, sample navigation and existing local/sample-scope regressions in the same run pass. Typecheck37241 passes. This does not prove project scoping of every other workflow list.

- After project-container changes, inventory65559 completed exit0:28 routes at1440/390 and12 local-empty routes at both sizes, with no page errors/horizontal overflow. Presentation evidence only; not full journey or40-view completion.

- Local project containers now separate project creation from drawing creation; multi-drawing membership/search/reopen, portable backup/fresh-tab restore, missing-project guard and unassigned assignment pass in browser65559. Typed schema rejects dangling/duplicate references; legacy snapshots still decode; typecheck and two model tests96150 pass. Existing sample projects/template backup99377 pass. Project aggregate dashboards/rename/archive and default entry routing remain incomplete.

- Shared field-photo enlargement now verified: keyboard open, Escape/exact focus return,2× scroll extent, bounded mobile modal (83570), plus comparison/draft/local-field regressions42863 and typecheck10433. Source-preserving view only; synchronized comparison alignment/full scope remains incomplete.

- Actual-PDF connected journey now includes drawing changes request, object correction, explicit stale quantity-basis reconfirmation and resubmission. Desktop50129 passes preservation of original changes record and approved replacement plus correct downstream payment/handover revision. Mobile74280 follows. The observed stale-basis block was intentional and not relaxed.

- Stronger civil journey evidence replaces the previous approval-fixture limitation: desktop44092/mobile35831 pass actual generated PDF rendering, UI overlay/manual quantity entry, both independent approval flows and the full downstream field/daily/payment/material/scoped-delivery/handover sequence. Only unrelated empty document/base scenarios seeded. Dynamic IDs/source hash preserved; no unrelated mutation. Branches for design changes, structured photo/section comparison and actual DWG/IFC remain unproven, so full goal remains active.

- Fixed frontend field role resetting Viewer→Author on drawing return (41406 RED). Field browser31221 now verifies retained Viewer/disabled actions across return+reload then explicit Author selection; draft and connected civil regressions pass, typecheck81108 passes. Prior field-role persistence gap is closed for this surface only.

- Unsubmitted field-photo metadata now survives reload/document switching; reconnect is still required for actual bytes. Browser34292 RED→26274 GREEN verifies draft attachment through eventual record submission plus existing field flow. Long-name/bounded-field/clearing model checks pass. Full source/backend persistence and global frontend completion are not implied.

- Field authoring text/date/location/object drafts now survive refresh and document switching; long notes preserve the existing1000-character capacity within500-character session entries. RED70605→GREEN67631; existing field/photo comparison regressions and typecheck66371 pass. Unsubmitted photo attachments and role persistence remain separate gaps; no full-completion claim.

- Same-location field comparison now implemented with optional observation dates and exact record inspection entry. New browser81527 passes wrong-source photo rejection, two-image reconnect, same-location filtering and reload/mobile; typecheck27280 and67 selected tests pass. Existing field/inspection/civil regression35398 follows. Source-aligned visual diff/zoom and full source-authoring journey remain outside this evidence.

- Current full presentation audit:73212 exit0 verifies28 views×desktop/mobile plus12 local-empty routes×both widths;44941 exit0 verifies12 panels×both widths and10 exception states/recovery actions×both widths. Standards is now included; organization stays within settings, scene management within presentation. No pageerrors/horizontal-overflow failures. These checks do not validate every business action or close the40-view scope.
- Next concrete uncovered journey step: same-location two-record/photo comparison before inspection, with explicit source identity and missing-photo reconnect. Full source-authoring/approval through delivery remains unproven despite the completed approved-source civil segment. No completion claim.

- Scene list now supports persistent ordering used by the player and confirmed deletion with selected-view reset. New and existing scene/player browsers59187 pass; typecheck/model test13881 pass. This addresses list management, not real render output/media export/full scope.

- Expanded civil journey now includes material order/receipt/install, selected-document delivery and untouched unrelated-document assertion. Missing material/delivery selector propagation reproduced (64309) and fixed; connected browser and existing material regression pass (25209). This does not replace unverified source-authoring/approval/mobile journey portions.

- Civil site-to-handover segment now has a single continuous UI test with an unrelated first document: record → failed inspection → correction → reinspection → accepted daily → confirmed payment → received asset handover. It exposed and fixed shared-menu selection loss. Browser68668 plus existing payment/daily regressions, typecheck40049 and65 workflow tests pass. Approved design/estimate are fixture prerequisites, not UI steps; full original/expanded journey remains incomplete.

- Organization/profile, plan proposal and integration state screens added within settings. Browser1077 verifies local persistence, role guard and connection lifecycle; expanded16210 adds invalid-seat and service-isolation checks. Existing member regression31949 and typecheck74739 pass. Mobile screenshot inspected. This closes the absent first-pass view, not usage/invoices/integration mapping or entire frontend scope.

- Payment evidence now selects the exact daily-report ID rather than every report on its date. Expanded browser reproduces the old same-date ambiguity and verifies payment→exact report→drawing→exact report→reload→payment with reviewer/draft preserved (28017 exit0); existing daily lifecycle also passes. Typecheck44982 and2 domain tests pass. This does not prove the full extended civil journey.

- Reusable property/classification standards now have browser evidence for schema drafting, two-document application, immutable versions, exact-object inspection/return and real JSON backup restore/reuse in a fresh browser context. Scoped margin fix restores the canvas-priority gate without relaxing it. Standards, existing canvas-priority and template-backup browsers pass (39039); typecheck and67 selected tests pass (73356). Canvas screenshot inspected. Company/bulk/migration scope and full40-view/journey acceptance remain incomplete; prior27-view inventory does not include this new view.

- Expanded inventory process15552 finished exit0:27 views (18 original +9 additions) × desktop/mobile, plus12 local-empty routes per width. Headings, local/sample identity and horizontal-overflow checks pass. This is not27/40 requirement completion or full business-action coverage.

- Added carbon view with approved-only quantity selection, manual kgCO2e/unit factors, explicit source/version/unit completeness, stage-specific drafts, frozen assessments and same-stage/object/unit comparison. Browser verifies incomplete and wrong-unit blocking, preview24→36 comparison+12, reload, quantity return and mobile;66 selected tests/typecheck pass. Mobile inspected. Existing budget/payments and architecture/IFC/civil browsers pass. No certified LCA/EPD lookup/AI factor claims. Conversion/library/export integration and full goal remain open. Inventory extended to27 views; fresh result pending separately.

- Added `handover` under existing Delivery navigation: asset metadata register, duplicate-code validation, approved-drawing/closed-inspection readiness, frozen package preparation and recipient correction/linked replacement/receipt simulation. New browser verifies draft/reload, old/new manual-label isolation, role gates, expanded asset list→approved snapshot location→feedback return and mobile.67 selected model tests/typecheck pass; screenshot inspected. Existing transmittal regression passes. Actual manual attachments are explicitly not claimed; warranty/maintenance/removal/selected-package export and full goal remain open.

- Post-payment regression process97053 finished exit0: existing daily-report, budget, architecture/IFC/civil journey browsers all pass. This verifies those specific existing flows, not the unresolved full product scope.

- Added local contract/payment-request review view. Versioned terms and fixed approved quantity + accepted daily/closed-inspection snapshots support request/correction/resubmission/confirmation without changing drawing/quantity approvals or implying actual payment. New browser covers draft/reload, role gates, amount cap, quantity/daily direct return and mobile;68 selected tests/typecheck pass. Screenshot inspected. Per-line certified amounts, contractual deductions, exact same-date report focus and broader handover remain open; full frontend goal not complete.

- Fresh expanded inventory finished exit0:18 original +6 added views at1440px/390px, plus12 local-empty route checks at each width. Includes new daily-report empty/local heading. Presentation evidence only; report lifecycle is covered separately above/below.

- Added `daily` (일일 작업 보고) under existing Field navigation. Frontend captures report date/weather/workers/work/cumulative self-reported progress/next action with frozen observation and inspection evidence; correction resubmits as a linked new record and reviewer confirmation does not approve drawings/quantities. New browser test verifies drafts/reload, Viewer roundtrip, role gates, date filtering and mobile; its stronger screenshot audit exposed and fixed rapid filter-clear/role-change URL loss.67 selected model tests, typecheck, inspection browser and three original journeys pass. Mobile populated history re-inspected. Full frontend goal, payment linkage and as-built handover remain open.

- Added field-record inspection/correction/reinspection/closure UI. Three explicit example checks, role-gated transitions, append-only per-record history, source-location guard and separate drafts use existing local session persistence. New model and browser tests cover correction cycles, all-N/A refusal, stale/role blocking, codec roundtrip, draft/reload/Viewer return and mobile.67 selected tests/typecheck, original local-field browser and three scenario journeys pass. Mobile history screenshot inspected. This is not legal inspection, named assignment or complete field management; the full goal remains open.

- Fresh original18+five-added desktop/mobile inventory, twelve panels and ten exception recovery checks pass. A new two-document browser test exposed submittal ID carry-over on document selection; the fix clears the old document-local selection atomically while retaining separate opinion drafts and queue role return. New switch test and existing submittal queue/lifecycle tests pass. The fixed40-view coverage map is in `2026-09-11-frontend-screen-coverage.md`; route count is not completion. Full frontend goal remains open.

- Saved sample scenes now have modal presentation with bounded previous/next, separate presenter/editor selection, readonly configuration, reload and Escape/focus return. Fresh browser validates uncommitted editor preservation and44px mobile controls; existing scene and WebGL regressions,66 selected model/render tests/typecheck pass. Screenshot re-inspected. Media output, scene management, actual models and full frontend completion remain open.

- Added explicit-sample 3D presentation scenes (twenty-third major view) using the existing WebGL renderer. Fresh model/browser evidence covers valid camera metadata, orbit capture, settings/reload/switch, unchanged workflow state, no-op selection removal and mobile. Existing WebGL camera preservation/reset regression,66 selected tests and typecheck pass. Actual IFC, photoreal rendering, media output and scene-management/full coordination requirements remain incomplete; this is not completion of3D or the full goal.

- Added approved-estimate budget comparison (twenty-second major view) with sequence-pinned plan, budget/reserve validation, fixed approved totals, input/result separation and quantity comparison handoff.70 selected model/render tests and focused browser evidence cover unapproved exclusion, invalid reserve, reload, exact approved sequence and direct return, missing approval and mobile layout. This does not complete contracts/project budget allocation/progress payments or the broader frontend goal.

- Added a cross-document distribution/receipt register (twenty-first major view) from existing local delivery state. Fresh model/browser evidence covers document-local sequence separation, current versus superseded access, search/status/reload, recipient acknowledgement and filtered return, historical comments and mobile layout.69 selected tests, typecheck and existing real-PDF quantity/drawing approval→delivery→recipient regression pass. This is not formal server distribution or receipt proof; official numbering/dates/multi-document packages and broader scope remain open.

- Tasks now has a role-routed submittal queue with fresh model/browser evidence for unresubmitted correction selection, document-ID isolation, exact-item open, new-item selection after resubmit and Viewer→review→queue context.67 selected tests and both queue/lifecycle browser verifiers pass; mobile image inspected. A pre-hydration selector reset was reproduced and corrected using an explicit recovery state. This closes the initial queue-discovery gap only, not named users, search/paging, formal distribution or full frontend completion.

- Added local drawing submittal review page (twentieth major page) and existing Review navigation entry. Fresh browser evidence covers real menu entry, submission, correction/resubmission history, acceptance without drawing/q approval, reload, current Viewer return and actual drawing edits blocking stale acceptance.120 selected model/render tests, final typecheck and three original scenario journeys pass; mobile image inspected. This is local metadata/role simulation only. Multi-file/named-role/queue/filter/frozen-visual/formal-transmittal requirements remain incomplete; original scope and40-view expansion are not complete.

- Personal template rename/removal now has fresh browser evidence for draft/reload, blank-name refusal, saved rename, exact confirmation/cancel, removal persistence and original/copy preservation. Existing saved/builtin/template-draft/portable-backup browsers,66 selected model/render tests and final typecheck pass. Mobile confirmation visually inspected. This supersedes the personal management gap below; company/version/property/block workflows remain incomplete and the full goal is not achieved.

- Personal saved drawing templates now have fresh evidence for selected-page capture, source/evidence exclusion, independent copies, session reload and real JSON download→new-context explicit restore→editable reuse. Restore confirmation now discloses template count and replacement.119 selected model/render tests, typecheck, template backup/reuse and existing storage-denied backup browsers pass; mobile image inspected. This is personal same-tab reuse only: company/version/property/block library workflows and template management remain incomplete. The newer40-view competitive proposal expands rather than replaces the original scope and has not been fully implemented or audited.

- Schedule settings/calculation descriptions are now collapsible; desktop drawing/editor width is2:1. Fresh layout, real-PDF, dependency and calendar browser tests plus typecheck pass. Applied date and invalid saved-date warnings remain discoverable; toggle preserves unsubmitted input. Mobile screenshot still shows a long stacked editor, so this does not prove complete usability or the full frontend goal.

- Schedule now has a real-PDF/source-free2D location view with planned-state colors, keyboard/pointer object selection and view/page/day/object continuity. Fresh two-page PDF browser, prior calendar/dependency browsers,71 selected model/render tests/typecheck pass. Color evidence does not claim actual completion or3D/4D; those remain incomplete.

- Single-predecessor finish-to-start schedule links now have model/browser evidence for persistence, cycle/overlap refusal, stale/missing dependency, upstream-change warning and manual correction. Calendar/return regression and70 selected model/render tests/typecheck pass. This begins dependency coverage, not full scheduling: multiple predecessors, work calendars, long horizons, historical actuals and4D remain open.

- Schedule now has an explicit per-document calendar basis and synchronized date/day navigation. Browser evidence covers two-document isolation, basis shifts preserving plan/progress, invalid-date refusal, reload, exact drawing return and mobile; UTC boundary tests and typecheck pass (69 selected tests). This closes missing date labeling only: the 30-day window, no work calendars/dependencies/4D/historical actuals and wider scheduling gaps remain.

- Added a separate nineteenth page for local object-linked relative scheduling. Focused browser evidence covers save/reload, plan/report comparison, exact Viewer location return, invalid period, stale location/rebind, navigation and mobile. 68 selected model/render tests/typecheck and original18-page/twelve-local-route/two-width plus three-scenario regression sweep pass. This begins the expanded scheduling requirement; calendar/predecessors/4D/historical actuals and broader schedule workflow remain incomplete. Original scope is not replaced by this addition.

- Explicit Changes → current/frozen quantity evidence workspace → Changes callbacks now preserve drawing revision/target/page/object/visibility as well as quantity comparison selection. Fresh extended quantity browser journey proves both returns and reload after a real quantity approval sequence plus frozen drawing fixture; historical drawing browser test, 67 selected model/render tests and typecheck pass. This supersedes the callback gap recorded immediately below, without implying all multi-document/history/focus combinations are complete.

- Historical drawing comparison now persists page/object/identical-object filter in URL with the revision pair. Fresh historical/overlay browser evidence covers reload, browser back, list focus recovery, invalid-page recovery, real PDF and unchanged records. 67 selected model/render tests and typecheck pass. Explicit quantity-to-workspace return callbacks do not yet retain this drawing context; that cross-route gap remains open.

- Template start selection/name now persist through the existing session draft map. Browser evidence covers navigation/reload, create-and-reset, explicit discard and unknown-template recovery preserving the name. Existing office/site/civil template regressions, 66 selected model/render tests and typecheck pass. Library company/version/property requirements remain incomplete; start-draft continuity is no longer an open gap.

- Cross-document field-location index now groups building/civil/free-text prefixes without merging duplicate record IDs from different documents. Aggregate exploration is a sibling of the existing selected-document authoring region. Fresh browser checks pass exact-document return/reload, stale-location blocking, missing-prefix empty state and explicit reset preserving selected document, mobile width, original local-field and photo-review-to-recipient regressions. 114 selected model/render tests and typecheck pass. This is same-tab aggregation, not project/server scope; assignment, large-list navigation and the broader frontend scope remain open.

- Live Library now has office/site/civil schematic starters with exact generated-object previews and scope descriptions. Civil normalized polyline creation/reload and existing independent-copy edit/reopen browser tests pass;113 model tests/typecheck and mobile visual inspection pass. Company-managed template/block/version/property workflows remain missing; the Library requirement is not complete.

- Local canvas-first default layout now has measured blank/PDF evidence, not only sample evidence: blank<=500px, PDF surface<=550px at1440×900, object placement and mobile width pass. Metadata/help remain available. 112 model tests, typecheck, local inspector, quantity-delivery and share regressions pass. Expanded-panel/long-file-name/full keyboard layout matrix remains open.

- 2026-09-11 full regression sweep: original18 sample pages×2 widths,12 empty-local routes×2,12 panels×2,ten exceptions×2 and three scenario journeys passed. Visual inspection then drove compact workspace chrome; measured sample canvas start moves from y712.89 to<=460 with>=400px visible at1440×900. Post-change typecheck, panel/journey and local inspector regressions pass. This is a usability/layout improvement, not full frontend completion; local large-toolbar parity and30-page extension remain unaudited.

- Scenario pricebook now has local UTF-8 CSV file selection, column mapping, first-row inspection, explicit apply→existing preview→version save. Quoted fields, duplicate column block, invalid file/CSV, reload and mobile tested; 112 model tests/typecheck and prior immutable-rate/Viewer browser regression pass. Unsaved mapping recovery and real company scope remain incomplete; no backend/upload/XLSX engine implied.

- Grouped-estimate search and exact expanded group survive object-workspace return/reload. Two-group browser fixture distinguishes overall/visible totals, covers empty/reset and unavailable group guard; mobile inspected. 111 model tests, typecheck and quantity-delivery regression pass. This closes the specific search/expansion return gap, not all keyboard/history, BOQ classification or full Estimate requirements.

- Estimate now provides grouped registered/demo rate items, provenance, subtotals, stale/unlinked exclusions and exact-object→estimate return. 111 model tests, typecheck, populated mobile view and quantity-delivery regression pass. Code mapping/trade hierarchy and selected-group/filter restoration remain incomplete; grouping alone does not close the Estimate requirement.

- Layer-only review now reports added/removed/name/visibility/lock/order changes separately from geometry using the chosen historical snapshot or current layers. Model fixtures cover order/add/remove and default compatibility; browser verifies name/visibility/lock, historical isolation and populated mobile layout. 110 model tests, typecheck and PDF overlay regressions pass. Cross-route/page/object selection persistence remains unverified; the full goal is not complete.

- Historical drawing-pair comparison now has exact approved-target geometry/source and missing-target blocking. URL selector restore, rapid two-selector updates, real PDF rendering and current/same/missing target browser checks pass; original overlay/page/focus regression passes. Layer-only comparison and full selection/return-context matrix remain open; this does not close the entire Changes requirement.

- Local invitation increment: new shares require recipient-email simulation and acceptance before drawing/feedback. Browser evidence covers mismatch, accept/reload, decline/reload and share isolation; mobile decline view inspected. Actual PDF sharing, expired-access decisions and multi-document request inbox regressions pass. 108 model tests and typecheck pass. Legacy shares retain no-record behavior explicitly. This is frontend state navigation, not authentication; broader completion remains unproven.

- `verify-workflow-inventory.mjs`: 18 pages × desktop/mobile = 36 presentation checks passed. Each checks heading, nonempty content, no pageerror and no horizontal overflow. Screenshots saved as `/tmp/1hk-inventory-{width}-{page}.png`. This does not exercise every control or validate usability.
- `verify-workflow-panel-inventory.mjs`: 12 panels × desktop/mobile = 24 presentation checks passed, including Escape close and no internal overflow. Only request creation is exercised as setup; other business actions need their own tests.
- `verify-workflow-exceptions.mjs`: extended and rerun at 1360px and 390px. All 10 states render without horizontal overflow; permission denial, exact-source recovery, retry, required/stale recovery, AI bypass, offline draft retention and expired/empty exit actions pass at both widths. This remains a sample-workspace check, not every page/state/role combination or a keyboard-focus audit.
- `verify-workflow-journeys.mjs`: architecture, IFC and civil sample journeys passed correction/resubmission, approval, delivery configuration, reload and new-revision preservation. These use fixed sample objects, not actual IFC/civil files, and are not evidence of local-document parity.

## Page requirements and gaps

All rows below have fresh presentation evidence. “Gap” distinguishes incomplete integration or missing verification from absent rendering.

| Page | Existing frontend path / evidence source | Remaining gap |
|---|---|---|
| Home | Start/continue choices in page presenter | Explicit new/existing-user routing matrix and browser history audit |
| Projects | Sample projects and independent local documents | Large list/search/empty selection matrix; local vs sample clarity |
| Tasks | Sample workflow; local drawing and quantity-review inboxes with exact-document destinations | Local general findings/RFI aggregation and assignment integration |
| Overview | Local document counts, separate drawing/quantity state, field recheck counts, material events and exact-document links; explicit sample scope remains | Role-prioritized exact-object next-action queue, server/project grouping, richer current-vs-historical delivery status |
| Documents | Explicit sample/local separation; searchable local work list with exact-ID return; mixed-file manifest retains hash/format/result and exact PDF link | Per-file settings and unified handoff to DWG/IFC mock partial-state flow |
| Start | Blank/template/PDF entry; mixed selection persists pending/completed entries; interrupted-file reselection validates metadata and known hash; separate format wizard simulates partial/failure/retry/cancel | Batch attempt history, explicit replacement and larger-list UX |
| Workspace | Sample 2D/3D/split; real local PDF plus editable overlay; calibrated measurements, same-tab verified source reuse, compact approved snapshot and findings/comparison return | Unified local/sample mode navigation, broader page/object/role matrix and large drawing UX remain incomplete |
| Quantities | Measured import → object quantity → review → original location → matching delivery → independent recipient verified | Broader multi-object/unit/correction/history matrix; not engineering-certified calculation |
| Estimate | Grouped rate/version/unit subtotals; unlinked/stale exclusions; search, exact-object return and expanded-group reload | Deliberate BOQ code mapping/trade hierarchy, amount composition and full keyboard/history matrix |
| Rates | Versioned scenario pricebook; CSV paste and UTF-8 file/column mapping with explicit preview and registration | Unsaved file/mapping recovery, broader input-error matrix and scenario-vs-company scope clarity |
| Changes | Approved/current and approved/approved quantity and geometry comparisons; layer changes; URL revision selection | Page/object/filter persistence and cross-route return context; no PDF pixel diff |
| Issues | Local object comments and formal RFI with create/respond/close, evidence return, search and drafts | Named-person mentions/assignment, reopen/reassign, due actions and complete keyboard/history matrix |
| Reviews | Sample review flow; local drawing and quantity inbox handoffs | Clearer three approval domains across pages and full navigation/draft matrix |
| Delivery | Local frozen drawing/quantity package; package-bound output phase/attempt/source survives reload and moves read-only into history; cancel/failure/retry and recipient confirmation verified | Format-specific output diagnostics/attempt history and broader return-to-edit correction context |
| Library | Sample templates and independent blank layouts | Richer company-standard/block/semantic-object library workflow |
| Settings | Local member-role plans; sample invitations; local frozen share/recipient with invitation response, expiry/access requests and owner inbox | Cross-workflow role/account simulation consistency and full draft/focus/role matrix; no real authentication |
| Field | Local observations, structured location/filter, actual photo previews/hash reconnect; frozen review evidence and reviewer→approver→recipient photo journey | Project-wide location tree, assignment, photo captions/capture context and correction/revision breadth |
| Materials | Local approved quantity selection, separate order/receipt/installation plus cancellation/return/uninstall events; actual PDF reconnect/hash rejection/pixels and compact approved drawing/object roundtrip | Supplier/product mapping, explicit revision carry-over, project aggregation; not progress payments |

## Panel requirements

All twelve render and close in the current inventory. Scope limitations remain explicit:

| Panel | Remaining verification or integration |
|---|---|
| Scale | Local calibration, saved restore, quantity handoff, readonly review location and measured-recipient journey verified; broader page/role/history matrix remains |
| Compatibility | Simulated processing/partial/failure/retry/cancel is connected; local file-level manifest/persistence remains |
| Properties | Sample panel and local inspector separately implemented; full mode/state parity unaudited |
| Model | Sample geometry/identity only; actual IFC not required for mock frontend but import/partial-state UX must be complete |
| Formula | Local calibrated measurement → stored evidence → quantity → frozen review is verified; sample formula and local multi-unit/formula experiences still differ |
| Rate | Local source snapshots and CSV file-column mapping exist; unsaved mapping recovery and scope consolidation remain |
| Compare | Local historical quantity/geometry/layer comparisons exist; page/object/filter and cross-route state-preservation matrix remain |
| Request | Local drawing/quantity inbox handoffs exist; general comments and assignment remain |
| Review | Drawing and quantity state machines exist; cross-route focus/history matrix unverified |
| Approval | Domains separate locally; sample combined confirmation needs clearer scope explanation |
| Findings | Local source/quantity findings link to exact object; broader task aggregation remains |
| Share | Local frozen sharing, guest/PDF reconnect, invitation accept/decline, feedback, expiry, request→owner deny/allow and cross-document inbox/search/return verified; account/role unification and draft/focus matrix remain |

## Exception requirements

Current desktop and mobile sample evidence covers all ten named states: **empty, loading, unsupported, required, stale, missing, permission, expired, offline, ai**, including the recovery actions described above. Required completion evidence still includes keyboard focus, form preservation at every relevant transition, and missing-source behavior across local document routes. Local PDF hash reconnect and corrupt-file recovery have separate browser tests; they do not prove every file-format failure path. AI failure must never prevent basic work; no actual AI runtime is implied.

## Integration problem and next sequence

The main gap is not a missing menu count: `WorkflowBlankWorkspace` and the sample `Workflow` panels still expose different capabilities. Keep separate data identities (never replace user documents with sample evidence), but close the UX gaps in the same local workflow.

1. **Local integration:** overview/findings, field location input/filter/review draft, and material approval/event/source flows use selected local documents. Local object comments now have fresh persistence, exact-target, search-return, Viewer and stale-position browser evidence. Formal RFI/assignment/replies/mentions and cross-document location aggregation remain open. Photo and delivery scope requires its own targeted evidence; comment tests do not prove it.
2. **Navigation breadth:** expand multi-object/unit/page/role/history coverage around the verified measurement/review/delivery path while retaining the distinction from engineering-certified calculation. Local RFI has verified author creation, assigned-role answer, author closure and exact-object return, with schema persistence and stale-context guards. Two-document browser evidence now covers role work queues, search/phase/assignee filters and URL return/reload retention. Named-person assignment, due-date actions, reassign/reopen history, draft/focus retention and project-wide aggregation remain open.
3. **Import/output and sharing continuity:** multi-file manifests, persisted simulated job/request states, independent guest entry and correction return context. RFI compose/response drafts now survive local session reload and exact-object roundtrip with role/context isolation and success-only consumption. Stale draft discovery, discard/reattachment and backup restoration UI coverage remain open.
4. **Remaining library and geometry comparison flows**, then keyboard/back-forward/draft matrix and a full requirement-by-requirement completion audit.

Recent concrete implementation evidence is recorded in `2026-09-10-frontend-workflow-progress.md` and the targeted browser scripts. Passing presentation inventories does not close these integration gaps.

Do not infer completion from 36 + 24 successful presentation checks. This audit establishes missing frontend integrations and an execution order, not a production readiness claim.
